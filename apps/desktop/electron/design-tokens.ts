/**
 * The single design value the Electron shell needs before the renderer paints.
 * Everything else comes from src/styles/tokens.css (DESIGN.md#tokens).
 */
export const TOKEN_BG = "#141517"; // --bg
