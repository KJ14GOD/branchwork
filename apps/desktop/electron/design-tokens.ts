/**
 * The design values the Electron shell needs where CSS custom properties
 * cannot reach. Everything else comes from src/styles/tokens.css
 * (DESIGN.md#tokens); each constant here mirrors one token there.
 */
export const TOKEN_BG = "#141517"; // --bg
export const TOKEN_PREVIEW_CANVAS = "#ffffff"; // --preview-canvas
export const TOKEN_ACCENT = "#f2efe9"; // --accent (dark theme; the live-actor marker)
