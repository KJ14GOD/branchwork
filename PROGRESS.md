Purpose: The single honest statement of what exists right now. Anyone reading only this file should know exactly what Novus can and cannot do today.
Authoritative for: current implementation status, evidence, known gaps, the current milestone.
Not authoritative for: what Novus should become (PRODUCT.md), how it should be built (ARCHITECTURE.md), how it should look (DESIGN.md), why choices were made (DECISIONS.md).
Update when: any capability changes state, in the same change that alters the capability. Status claims require an Evidence line.
Last reviewed: 2026-08-05

# Progress

## Current milestone

**Two people operating one real mission (D-034 … D-037).** Live today in the desktop app: sign in with GitHub, open a local project, and direct real Claude Code in a dedicated mission worktree under a credentialed local runner. The turn's changes are recorded as git-derived evidence, a follow-up direction resumes the same harness session, and a second authenticated person can join by invitation from another desktop, see the identical durable mission, queue an attributed direction, take the control baton through an explicit request-offer-accept handshake, and have their direction executed by the first person's machine. Closing and reopening either client reconstructs participants, directions, control history, checkpoints, and evidence.

**Terminal and runtime trust (D-043 … D-046)** lands on top of it, proven in the app on 2026-08-03: an interactive terminal on a real PTY that opens in the mission worktree and nowhere else and renders what real programs actually print; a dock that is the terminal alone (D-049), with setup, run, and check output read in the evidence panel's Output section on the machine that produced it (D-050); a setup flow a person reviews, saves, and runs; a declared check invoked on a click whose evidence is bound to the revision it tested; project-stated deadlines with a timed-out outcome that is not a failure; readiness as a declared signal rather than an assumption; secret values supplied at the machine that has them; and a loopback-only preview bridge. The command a participant authorizes is pinned to what they saw, so a turn editing the project's configuration cannot change what their click runs.

**Supervised autonomy (D-062, D-064)** lands on top of both and is proven live on 2026-08-04 against real `claude 2.1.221`: the harness stops before an act it may not take, the question reaches the room, the person holding the baton answers it in the window, and the turn continues — with the permission policy pinned by Novus rather than read from a file the agent can write, and the project's own `CLAUDE.md` handed back explicitly through `--append-system-prompt-file`. Two clients are proven in the app for the same path. Around it, since 2026-08-03: a mission is filed away rather than deleted (D-063), open missions are a window-level working set (D-061), a failed check is run again from its own row (D-057), and the shell was reworked into one top row, a full-height rail, and an evidence panel that is not a menu (D-065 … D-068) — the last of which has visual evidence only up to the 2026-08-05 screenshot set; see the row and [Known gaps](#known-gaps).

**Competing approaches and the decision between them (D-074, D-075, D-079, D-080)** land on top of that, **proven in the app on 2026-08-06**: a second lane forked from the *shared* checkpoint — shown to the person and pinned, never a later revision nobody looked at — with a stated intent, its own branch, worktree, runner credential and baton; lanes that are genuinely operable, as tabs in the room's own strip with a switcher, a per-lane state line, per-lane files, terminal, checks and output, and a composer that names its target and routes every direction to the lane on screen; one surface comparing each lane's own evidence with no score, rank or recommendation anywhere; and a decision that records a person's rationale, the risks they accepted and what was unverified at that moment, followed by a **prepared** pull request nobody has opened.

Cloud execution, Codex, live pull-request tracking, and receipts-as-snapshots remain Not started.

## Status rules

- Every status is one of: **Not started**, **Partial**, **Implemented**, **Blocked**.
- **Implemented** and **Partial** require an Evidence line: a command with observed output, or a link to an artifact a human can inspect. Documentation is never evidence of functionality.
- Evidence comes in tiers, and a claim never borrows a higher one than it has earned:

  | Tier | What it means |
  | --- | --- |
  | **Tested** | A deterministic suite exercises the path. Real processes, real git, real PostgreSQL where the suite says so — but a fake provider or a scripted harness stands in for the outside world. Never live proof. |
  | **Proven in the app** | An Electron end-to-end run drives the real interface in a real window. What a person can actually reach, as opposed to what the modules underneath can do. |
  | **Proven live** | Exercised against the real GitHub or the real Claude Code binary, with the observed result recorded. |
  | **Partial** | Some of the capability is real and some is not, and the line says which. |
  | **Not started** | No implementation. |

- A deterministic suite passing against a fake provider is **Tested**, never "live". A module working is not the same as a person being able to reach it: code that no interface exposes is **Partial** at best, and the line says what is unreachable.
- The Golden V0 workflow ([README.md](README.md#the-golden-v0-workflow)) is the milestone gate for the first implementation: it is not done until two real clients execute all twenty steps.

## Application capabilities

Unless a line says otherwise, its deterministic evidence is the gate run of **2026-08-06** (the approaches slice, D-079/D-080): **17 contract, 176 control-plane against real PostgreSQL, and the desktop main-process suites** pass. The Electron end-to-end evidence is from the same day: **all six deterministic Electron specs were re-run to green on 2026-08-06**, one spec at a time rather than as one `pnpm run e2e` invocation — `e2e/decision.spec.ts` (1/1), `e2e/approval.spec.ts` (3/3), `e2e/multiplayer.spec.ts` (1/1), `e2e/runtime.spec.ts` (16/16), `e2e/slice.spec.ts` (5/5), `e2e/navigation.spec.ts` (8/8) — plus the **opt-in live Claude Code runs**, which were not re-run.

Getting those specs green was itself part of the 2026-08-06 slice: four of them had gone stale against decisions that landed without their tests being rewritten, exactly as PROGRESS warned. The draft-flow assertions D-077 removed (`new-mission` rail row, `draft-base`, drafts in the strip) were rewritten to drive the ask-dialog in `navigation.spec.ts`, `runtime.spec.ts` and `slice.spec.ts`, under D-077's own entry as the required discussion; worktree paths keyed by mission were rekeyed by workstream (D-074) in `approval.spec.ts`, `runtime.spec.ts` and `slice.spec.ts`; and two specs that clicked the workspace drawer's Overview tab — which now *toggles* — were corrected. One real regression was found and fixed on the way: a verification check with no lane and no execution attribution fell out of the lane-scoped state entirely instead of counting toward the lane the mission started with.

| Capability | Status |
| --- | --- |
| Authentication and sessions | Partial — GitHub OAuth desktop flow (D-027), first-party revocable sessions, hashed tokens, OS-backed credential storage. Unconfigured OAuth fails fast with a named 503 surfaced in the setup card. **Live GitHub round-trip proven 2026-08-01**: the user signed in through the real system-browser flow and the durable user row exists (`users.login = KJ14GOD`, github_id present). A session now resolves its organization deterministically (oldest membership) and no longer decides mission access; remaining Partial because sign-in is untested on Windows/Linux and there is no packaged build |
| Organizations and membership | Partial — personal org auto-provisioned at first sign-in; a mission invitation adds the redeemer as an ordinary org member, never an owner (D-036). Multi-membership works: mission access is scoped to the mission's own organization plus a participant row, so a person in two organizations reads and acts correctly. No org management surface, no org switching |
| GitHub repositories run locally | Partial — **Tested, not proven live.** The control plane mints a short-lived, repository-scoped installation token for the authenticated runner only, and the host clones the repository into its own area with the credential injected per operation over a pipe. The token reaches no event, no command, no durable row, and is absent from `.git/config` afterwards; a user session cannot obtain one. A second workstream shares the checkout and takes its own worktree — and its branch is fetched into that checkout rather than assumed present, which a real session found the hard way: the runner asked whether it held the *repository* when a worktree asks whether it holds *this workstream's branch*, so every mission after the first failed with `invalid reference` (D-051). Nothing downstream of the clone has a case for GitHub. A second path to that same message was found and closed here: publishing what a project declares *creates* the worktree, and discovery announced even when the branch had not arrived — because `ensureCheckout` throws the first time but returns silently for the whole of its 15 s → 5 min backoff, so one transient fetch failure produced `invalid reference` on every fifteen-second pass, swallowed into a single console warning (D-060). Evidence: `repo-clone.test.ts` (8), `workspace-clone.test.ts` (15) — including six callers contending for one worktree (D-058) and a mission whose branch could not be fetched, both proven by seeding the failure — against a real HTTP git remote that genuinely demands a credential — **but a fake one, not github.com**. The previous entry said "Implemented" while eight places in the interface still refused any non-local provider, so no user could reach any of it; those gates are gone (hosting is now "does this machine hold the checkout", not "which provider"), and what remains missing is the live proof against a real GitHub repository — see [Known gaps](#known-gaps) |
| Repository connection (GitHub App) | Partial — the GitHub App exists (manifest flow, one-click create + install; app `novus-kartik` installed on the user's account 2026-08-01) and the live adapter is proven for **listing and exact base resolution** against the user's real repositories. Live branch creation is implemented with the same idempotency mapping but has not yet created a real branch. **Local repositories** (D-032): registration with machine-bound identity, folder paths never leaving the machine, desktop-side git, reported outcomes as claims — proven live end to end |
| Repository branches, checkpoints, and workspace synchronization | Partial — one server-named mission branch per workstream pinned to an exact 40-hex base SHA, idempotent creation, failed/pending/created as durable state with retry. **Checkpoints are real**: an attributed commit at each turn boundary with its SHA, parent, per-file state and counts, withheld-secret count, branch, and reporting environment, all derived from git (D-037). Workspace synchronization with a remote (D-025) is still not started |
| Mission creation and lifecycle | Partial — creation with goal + success criteria + repository + exact base revision; mission, Mission Admin participant, workstream, and the creator's first control lease commit in one transaction; creation-key idempotency proven under concurrent duplicates. The mission's primary state is now a **projection** over durable workstream, execution, and evidence state rather than a stored word, and the list and the room compute it the same way. Terminal lifecycle states (Completed, Cancelled) do not exist |
| Invitations and participants | **Implemented for the V0 shape** — issued and redeemed from the interface (D-039): the inspector's Overview creates one and shows its token exactly once, the rail offers Join with invitation. Mission-scoped, single-use, expiring, revocable; only the token hash is stored and the usable token is returned exactly once; redemption is authenticated and creates both the org membership and the participant row in one transaction. Replay, revocation, expiry, and wrong-mission redemption are each rejected by name. Evidence: `authority.test.ts`, plus the two-client run in which a second desktop joins and reads the identical mission |
| Roles and server-enforced capabilities | Partial — the four mission roles exist and capabilities are computed as role ∪ lease in one place and enforced on every mutating command; a non-participant receives 404, never 403, so a mission identifier is not a capability. The interface renders from the server's capability list. `approval.respond` is implemented and enforced as a lease capability in no role list (D-062), and `mission.archive` as Mission Admin's alone (D-063). `review.*`, `pr.manage`, `workspace.sync`, `mission.close`, `force_interrupt` and the pause/resume verbs have no implementation: the features they gate do not exist, and `force_interrupt` deliberately stays absent because Stop already reaches a wedged turn the same way (D-073) |
| Control leases, requests, handoffs | **Implemented for one workstream** — one durable lease per workstream with a single holder, compare-and-swap transitions, claim of an unheld lease, requests that stay open and visible, offers to a named participant, accept/decline/withdraw, immediate transfer at an idle boundary and deferred transfer at a runner-declared boundary while running, Mission Admin revocation, and every transition event-recorded. The previous controller loses privileged authority the instant the transfer completes. Lease TTL expiry is implemented: a lease is released by transfer, revocation, or a 30-minute heartbeat lapse, where the heartbeat is the holder's own read of the mission (D-051) |
| Direction lifecycle | **Implemented** — durable, attributed, ordered direction with submitted / queued / applied / superseded / rejected / cancelled. Applied is written only on the runner's acknowledgement, never on send. A non-controller's direction queues visibly with its author; the controller applies, rejects, or supersedes it; the author may cancel while it waits. Queued direction survives relaunch |
| Workstreams and executions | Partial — durable executions with harness, model, effort, runner, starting direction, state, timestamps, harness session, exit outcome, failure reason, last acknowledged sequence, and latest checkpoint. At most one active execution per workstream is enforced by a partial unique index, not by an in-memory map. States reached in practice: requested, starting, running, needs_approval, stopping, completed, stopped, interrupted, failed. `needs_approval` is entered for the first time by approval routing (D-062); `needs_direction` and `paused` exist in the model and are still never entered. **A mission may now hold more than one workstream** (D-074): the index that forbade it is dropped, each lane owns its executions, and at most one execution per *lane* is still enforced by the partial unique index |
| Harness adapters (Claude Code, Codex) | Partial — **Claude Code proven live again 2026-08-02 through the new runner**: a direction submitted in the app started a real headless `claude` process in a dedicated worktree, its stream was parsed into attributed events, its change landed as an attributed checkpoint commit, and a **follow-up direction resumed the same harness session and recalled the first turn**. Six model ids offered, each verified live against the CLI 2026-08-02, now defined once in `@novus/contracts` and consumed by the renderer, the IPC boundary, and the adapter. Hardened: spawn-error handling, bounded stderr, final unterminated stdout line, defensive parsing, process-tree termination with SIGKILL escalation, authentication and non-zero-exit classification, and a checkpoint failure that can never report completion. **What a turn cost is now carried** (D-071): the CLI's own `usage`, `total_cost_usd`, `duration_ms` and `num_turns` are read off its final line, reported as `harness.usage`, summed onto the execution, and stated on the trace's machinery line — an opaque claim that bills nothing and stops nothing, with a figure the harness never reported left null rather than zero. **The harness's own subagents are grouped, not impersonated**: `--forward-subagent-text` is passed, and text or tool activity carrying a `parent_tool_use_id` is indented inside the turn's technical disclosure rather than taking the speech position, with no branch, checkpoint, controller or workstream of its own. An optional flag degrades where a pinned one fails: a CLI that refuses `--forward-subagent-text` by name is retried once without it, while a CLI that refuses to route approvals still fails the execution outright (D-062). **Proven live 2026-08-05** against `claude 2.1.223` through Novus's own turn path (`live-instructions.test.ts`, opt-in, 2/2): real usage figures arrive, and the project's `CLAUDE.md` reaches the model — answered from the system prompt with **no tool call in the turn**, so the file cannot have been read. Codex is not started and renders disabled |
| Workspace runtime — setup, run, verification | Partial — **Tested; parts proven in the app** (D-040 … D-046). `.novus/settings.toml` and `settings.local.toml` are parsed, validated, and layered by presence; a broken file fails with the file, the problem, and the TOML line. A project is inspected for manifests, lockfiles, Makefile/justfile/Taskfile, `.env.example` variants and `.worktreeinclude`, and Novus **proposes** commands that never execute without confirmation. Gitignored files are copied only after per-file consent, only when Git confirms they are ignored, refusing traversal, symlink escape, directories, and dependency output. Three environments are constructed explicitly: a harness credential cannot reach a project command, and a project command cannot see a worktree's absolute path. Ports are allocated per workstream and persisted. Commands are supervised in the worktree with process-group termination; a run command outlives the harness turn. **New in this slice**: the runner publishes what a project declares and the control plane pins that snapshot to each authorization, so the command a participant saw is the command that runs and a remote participant can read the same list (D-043); finite commands carry the project's own bounded deadline with `timeout` as a named ending distinct from a non-zero exit, a signal, and a cancellation; a run command has none, and a coding-agent execution never touches this supervisor; readiness is a declared signal, so a process that started is *starting* until its health URL or port answers and honestly *unreachable* if it never does, without being killed for it (D-045); a secret value can finally be supplied, replaced, and forgotten from the setup dialog, refused below the length Novus can redact, and refused outright where the operating system offers no encryption (D-044); a loopback-only bridge opens a preview that a live process actually reported. **Proven in the app** (`e2e/runtime.spec.ts`, 2026-08-03): a person reviews the proposal, saves it, runs setup — the witness file appears in the worktree and readiness reaches `ready` — then invokes a declared check on a click and gets `passed` with exit code 0 bound to the worktree's exact HEAD, and a second declared check that exits 1 is recorded `failed` with its output, never as verified (screenshots `50-` … `54-`). Evidence also: `workspace-policy.test.ts` (22), `workspace-processes.test.ts` (22), `workspace-runtime.test.ts` (12), `workspace-agent.test.ts` (17 on disk now, 5 at that run), control-plane `workspace.test.ts` (19) |
| Interactive terminal | Partial — **Proven in the app, and tested against real PTYs.** A real `forkpty` in the Electron main process, started in the mission worktree and refusing the user's own checkout by name, under the terminal environment D-041 defines and with no harness credential in it. Input, resize, multiple tabs named after the repository and numbered from one (D-049), close, bounded scrollback, and process-tree termination on close and on quit — including a job that left the shell's own group, which a group signal alone misses. It is unreachable from anywhere but this machine: there is no shell kind in the runner protocol and no control-plane route, so the restriction is structural rather than a check. Rendering is `@xterm/xterm` (D-046): the custom renderer it replaces stripped escape sequences and, through a regex that read `\b` as a word boundary rather than a backspace, deleted the last character of every word — it could not show colour, place a cursor, clear a screen, or run `vim`, `less`, or a pager, and no screenshot of it had ever been looked at. **Proven in the app** by `e2e/runtime.spec.ts` (2026-08-03, 13/13 in ~41s): a real window opens the dock straight into a live shell that already has the keyboard — typed into without clicking first — with no view switch and no rename control on screen (D-049), and `pwd` shows the mission worktree and not the folder the user added; a `printf` with an SGR sequence is *painted* in colour rather than stripped, and `clear` genuinely clears; Ctrl-C interrupts a `sleep` and the prompt returns; tabs are named from the repository and numbered from one — `{repository} 1`, `{repository} 2`, asserted against the fixture's own repository name — and `+` selects and focuses the one it made; a second tab is closed, and switching back to the first tab shows that session still on screen rather than a blank pane — an emulator builds its DOM once, so the pane that was detached to make room for the second one came back empty; a session keeps running while the dock is hidden and its scrollback returns when it is shown; a second desktop that does not host the workspace finds the control disabled with the reason in words *and* is refused by name when its renderer asks the main process directly; a relaunch shows no sessions at all rather than a dead tab. Evidence also: `workspace-terminal.test.ts` (17 tests, real shells, including the derived name and its numbering) and `workspace-runtime.test.ts` (the repository label, from `owner/name` and from a path). Screenshots `45-` … `49-`, `55-`. **Not proven**: no Windows or Linux run, and no packaged build |
| Workspace files in the room | Partial — **Proven in the app** (`e2e/runtime.spec.ts`, 2026-08-03). The evidence panel's first section lists the worktree's own files, folders disclosing in place, `.git` and dependency directories never listed, each row badged with its extension. Selecting one opens it over the room's canvas with the trace returning untouched on close; markdown gets Preview and Edit, and an edit saves into the worktree. Reading and writing are local IPC with no control-plane route, every path resolved through `realpath` against the worktree and refused if it leaves — asserted by asking the bridge directly for `../../../../etc/passwd` and three other escapes, all refused, and by `workspace-tree.test.ts` (12 tests) against real symlinks pointing at real files outside. Preview renders to React elements and never builds markup from file content, so repository text cannot reach the window that holds the bridge (D-048); a source file renders with line numbers and single-lexer syntax highlighting emitted as tokens for React rather than as markup, for the same reason — **built, and not yet asserted by any test**. **New in this slice**: a file that looks like it holds a credential is not listed, not readable, and not writable, by the one policy checkpoints already used, enforced in the main process at the single chokepoint every route passes through, so a renderer asking for the path directly is refused rather than merely not offered it; `.env.example` and its spellings stay visible; `.git` is refused as a *file*, which in a linked worktree holds the absolute path of the repository; and a binary above the readable size is refused rather than truncated, which is what happened when the text check sat behind the size check (D-052). Evidence: `workspace-tree.test.ts` (18), `secret-policy.test.ts` (9). **Not built**: creating or deleting files, and editing anything but markdown |
| Local process output | Partial — **Proven in the app.** Setup, run, and verification output is bounded, redacted, and still there after the process ended, with its exit code or its named termination reason, on the machine that produced it and nowhere else (D-045). It never travels to the control plane: what a remote participant reads is the bounded result attached to a check's evidence. It is read in the **evidence panel's Output section**, with a three-way switch for setup, the app, and checks, shown only where the workspace actually is (D-050) — the dock is the terminal alone (D-049), and the open question that left behind is closed. Evidence: `e2e/runtime.spec.ts` runs setup through the interface, reads the ended process back through the bridge (`exited`, exit code 0), and then reads it again *on screen* in the panel, where it says `exited 0` (screenshots `52-`, `58-`). Evidence also: `workspace-policy.test.ts`, `workspace-processes.test.ts` |
| Running a check again | **Proven in the app** (D-057). A ledger row that failed, errored, or proved a revision the worktree has moved past carries **Run again**; one that passed and still proves the current revision does not, because nothing is being asked. It is the same declared command by name, authorized by the server against `workspace.command` like any other, and run against the snapshot pinned when it was authorized (D-043). Every run is a new record — the failure that prompted it keeps its own attribution, revision, exit code, and output, and both stay on screen, because a ledger that overwrote a failure could not answer whether it ever failed. One run at a time per check. **Proven in the app** (`e2e/runtime.spec.ts`, 2026-08-04): a failed check is run again from its row, a second record appears with a distinct id, its own requester and its own revision, and the rendered ledger shows both (screenshot `62-`) |
| Filing a mission away | **Proven in the app** (D-063). A mission is archived, never deleted: one column set and cleared, everything it is left exactly where it was, still readable by anyone who could read it before, and restorable. Filed away means put away: nothing lists an archived mission in the rail, which carries only a count at its foot that opens a dialog when something is there at all — a permanent Archived section with Restore beside every row would make putting something away and getting it back equally easy. **There is no delete verb at all** — not hidden, not admin-only, absent, and a test asks for one and requires a 404, because "we chose not to expose it" and "it does not exist" are different guarantees. An attempt that was never given a command cannot start and is not work: it is ended as *interrupted* on the way out rather than blocking the filing, because a mission must not be wedged out of the product by a row nobody can act on. Otherwise refused while an execution is live or an approval is waiting, as two separate refusals: the waiting question is checked first, because *answer it* is the useful instruction and *stop the execution* is not. No repository branch is deleted and no worktree is removed — the mission branch is the record and lives in the user's own repository. The list filters on the column and the Archived view is that query inverted, so the two cannot disagree; reading a mission does not consult it, so filed away is not hidden. The runner discovers work through the same default list, so an archived mission stops being enrolled, cloned and worktree'd — safe only because archival is refused while an execution is live. `mission.archive` is Mission Admin's and is **not** lease-granted; `mission.close` is deliberately untouched and still unimplemented, because ending a mission's work is a different act. **Proven in the app** (`e2e/approval.spec.ts`, 2026-08-04): archiving is refused in words while the harness waits, then the rail's own control files it away, its tab closes, the second client sees it leave the list and appear in Archived, every direction, event and approval decision is still there, Restore brings it back, and a relaunch keeps it filed (screenshot `79-`). Restore now disables the other rows and labels the active row *Restoring…* until the refreshed list returns, preventing duplicate clicks — added after that run and **not in the evidence set**. Evidence also: `archive.test.ts` (10), including the refusals, the role that may not, the non-participant told it does not exist, idempotency, and the absent delete route |
| Approval routing | **Proven live** (D-062). Claude Code runs under `--input-format stream-json --permission-prompt-tool stdio` with stdin held open: it stops before an act it may not take, writes a `control_request`, and blocks until Novus answers on the same channel. The permission policy is pinned by Novus rather than inherited — `--setting-sources ""` and `--permission-mode manual` — because a `.claude/settings.json` in the worktree is a file *the agent can write*, and reading it to decide what the agent may do is not a boundary; measured, the identical Write is performed silently without those flags and routed with them. One approval answers one act: no allowlist, no "always allow", and Claude's own `permission_suggestions` are parsed and discarded. `approval.respond` is a lease capability in no role list, settled by compare-and-swap so a second answer is refused rather than delivered twice. No raw tool input is stored — the durable summary is composed from named fields, path-masked and value-redacted (D-052) and bounded twice, so a `Write`'s file body never leaves the machine. A pending request is a safe boundary, declared by the runner, so control can transfer while the harness waits. Stop prefers the protocol interrupt, keeping the session resumable, with the process-group kill as a bounded fallback; which one ended the turn is recorded. **Proven live** against real `claude 2.1.221` (2026-08-04): a real Write request reached the room, the card rendered it, *Approve once* was clicked **in the window**, the harness was unblocked, `HELLO.md` was written and committed, and the next turn resumed the same session (screenshot `78-`). **Proven in the app**, two clients (`e2e/approval.spec.ts`): the non-controller sees the question, is told who holds the baton, and is refused by the server when their client asks anyway; control transfers while the harness is blocked; the new holder answers and is recorded as having answered; a denial leaves the file uncreated and the execution *completed*, not failed (screenshots `74-` … `77-`). **The project's own instructions are handed back** (D-064): pinning the settings had stopped the worktree's `CLAUDE.md` loading, and Novus now reads that file and passes it with `--append-system-prompt-file`, resolved through `realpath` into the worktree, non-empty, regular, under 100 KB, and re-read every attempt. Safe while settings stay pinned because the two files differ in kind — a settings file grants authority and hooks that run before the permission check, `CLAUDE.md` grants nothing and every tool call still reaches the router. **Measured against the real CLI** and recorded in D-064: a codeword in `CLAUDE.md` is unknown to the model with settings pinned and known once the file is passed. Evidence also: `approvals.test.ts` (16), `execution-approval.test.ts` (11, against a real process with a stub `claude` on PATH asserting the exact argv — including the instruction file, its absence, and a path that leaves the worktree), `live-approval.test.ts` (3, opt-in against the real binary, including a committed `.claude/settings.json` saying `acceptEdits` that cannot switch the routing off), `harness-stream.test.ts`, `workspace-agent.test.ts`, `contracts.test.ts`. **Proven live again 2026-08-05** against `claude 2.1.223`, through Novus's own turn path rather than a hand-rolled CLI call: the codeword in the worktree's `CLAUDE.md` comes back and the turn opens no file at all, so the system prompt is the only place it can have come from (`live-instructions.test.ts`, opt-in). **Not carried**: a project's `.claude/skills`, measured absent under the pinned policy and deliberately left out — the flag that would load them re-admits hooks and MCP servers (D-072, and [Known gaps](#known-gaps)) |
| The window has one top row | Partial — **the shape below is proven in the app up to the 2026-08-05 evidence set, and the changes after it are not** (D-066 … D-068). Mission tabs sit in the window's own row beside its controls — rail toggle at the far left, tabs next, `+` directly after the last one, the room's controls at the right — so the window has one chrome edge instead of a mostly-empty row above a band of tabs. The wordmark is gone. Every tab names its project always, not only when a second project is open, because a label that comes and goes as siblings close makes a tab's meaning depend on its neighbours. The rail is the whole left column with its own strip inset past the window's buttons and its switch at the edge it moves (D-068), and carries **Home** and **Search** fixed above its lists — search finds a mission by name across projects, while files are filtered where the files are. The evidence panel is three sections (All files, Changes, Checks) with the workspace's own two (Overview, Output) in a region at its foot, and it no longer repeats who holds the baton in a header of its own. The top row clears the window's own buttons, and the strip has no plane or edge of its own — a tab is a word on the window and only the one being read takes a surface (D-067). Evidence: the end-to-end assertions rewritten to pin the new behaviour in `e2e/navigation.spec.ts`, `e2e/runtime.spec.ts` and `e2e/slice.spec.ts`, and the screenshot set regenerated with them, **reviewed by eye** at 1440, 1000 and 760 (screenshots `9-`, `62-`, `71-` … `73-`). **Not in that set**: three later changes to the same surfaces — the rail's switch aligned with the room's, search opened as a palette rather than a form, and the workspace region reworked into a foldable, resizable drawer read alongside the diff instead of replacing it. Each was inspected by eye during iteration under D-067's proportional rule; none has a regenerated screenshot or a recorded end-to-end run |
| The room reads as a transcript | **Proven in the app** (D-065). Within a turn the agent's reply is the largest text and the only `--text-1`, the direction that prompted it sits at `--text-2`, and everything about the turn — speaker, model and effort, technical activity, the checkpoint line — recedes to `--type-meta` at `--text-3`. Speech is content, the rest is apparatus, and `--type-speech` is the scale's first new step. The rule that ran down the side of every turn is gone: grouping is the indent of speech under its speaker and the space between blocks. Diff arithmetic carries `--ok` and `--danger` as text, the same pair the diff body uses, so one change reads identically in the trace and the panel — the sign says which way it goes and the colour agrees with it. Both side columns are draggable, keyboard-operable, double-click to default, bounded at both ends, and remember their width on that machine. The Archived dialog is composed on the dialog's own head/body/actions axis, each row one primary value with its context beneath and one action at the end. **Reviewed by eye** at 1440 and 760 across the regenerated evidence set (screenshots `20-`, `62-`, `80-`) |
| Open missions as a working set | **Proven in the app** (D-061). A strip above the room — starting at the room's own left edge, with the rail a full-height column beside it rather than underneath it — carries only the missions this person has open, and is not a second list: opening from the rail opens a tab or selects the one that mission already has, a mission is open at most once, missions from different projects sit in it together, and a project with nothing open puts nothing in it. Closing a tab closes a tab — no execution stops, nothing is discarded or archived, the harness goes on working and the rail goes on reporting it, which is why the mission list is now re-read on a timer rather than once at mount. Open missions and the selected one survive a relaunch; drafts do not, because a draft is unsent local text with no mission behind it; and a restored mission the server no longer shows this person is dropped from local state quietly, while `offline` explicitly is not treated as gone. **Creation reworked 2026-08-06 (D-077), and the e2e evidence for it is stale**: every entry point now opens one ask-dialog — words create and start the mission, nothing closes it with no draft, no tab, no row — the rail's permanent New mission row is gone, a project row is a pure disclosure that never moves the room, and the project name reads as a heading over its quieter missions. The dialog is the real Composer wearing a dialog (D-078), so model and effort chosen there persist exactly as the room's do; mission rows in the rail are now inset 6px chips under sharp full-bleed headings. `e2e/navigation.spec.ts`'s draft-flow tests were rewritten against the ask-dialog on 2026-08-06 under D-077's entry and the spec passes again (8/8): the + and ⌘T and the strip's + all open the one dialog, Esc leaves nothing behind anywhere, and the words create the mission in a tab named by the goal they derive. Mission tabs are the window's and file tabs are the room's, the latter only while a file is open, each mission keeping its own; the room's way back reads *Mission* rather than the goal a fourth time. **Proven in the app** (`e2e/navigation.spec.ts`, 8 tests, 2026-08-04) at 1440, 1000 and 760 wide — including two missions from one project and two from different projects open together, a background mission that keeps running with its tab closed, a restored mission that no longer exists and one this person may not see, both dropped without harming the shell (screenshots `63-` … `73-`, reviewed). Evidence also: `working-set.test.ts` (16 tests) proving the rules without a window |
| Missions in one project | **Proven in the app** (D-055). A project row in the rail discloses its missions and ends with New mission; `⌘T` is the same act. The room is the selected mission and repeats nothing: the strip across the top is for open files, absent entirely while none is open, so a single mission has no chrome above it and an open file takes its tab beside the room's own. Nothing in the room calls a mission a workstream any more — the word had been on the tab row, its `aria-label`, the `+` tooltip, the draft tab, two empty states and the new-room sentence, and a mission contains exactly one workstream that nothing in the interface can add to. **Proven in the app** (`e2e/runtime.spec.ts`, 2026-08-04): two missions under one project are listed once, in the rail, each selectable with the room following; the centre has no tab strip at all; New mission says Mission and opens a draft that says the first direction creates this mission; `⌘T` does the same; and the room's full text contains no occurrence of *workstream* (screenshot `61-`). Evidence also: `slice.spec.ts`'s rail-disclosure and relaunch assertions, moved from the tab row to the rail. **Not built**: more than one workstream in a mission, and therefore the lane headers [DESIGN.md](DESIGN.md#component-behavior) specifies for that case |
| Execution providers and cloud workspaces | Not started |
| Runner heartbeat and lease expiry | Partial — a sweep ends an execution whose runner has been silent past a 90-second recovery threshold, as an explicit *interrupted* outcome with an attributed event, and a returning runner cannot resurrect it. A control lease expires after its holder has been silent past its 30-minute time to live, leaving the workstream with no controller and claimable through the existing request path. **A pending approval survives that expiry** (D-073): the question stays pending, the harness process is never touched, the expiry event records that a question was waiting, the state line says *no one holds the baton — claim it to answer*, and whoever claims the lease answers it once with their name on it while the previous holder is refused. Evidence: `reliability.test.ts` (2 tests — the question, the execution and the absent stop command after expiry; then the claim, the answer, the single carried command, and the refusal of the departed holder). Expiry removes future authority only: an execution already authorized keeps running (D-034). **Corrected 2026-08-03**: the previous entry said Implemented on the strength of `reliability.test.ts` calling the sweep directly, and nothing renewed a lease's heartbeat — `touchLease` had no caller — so every lease expired thirty minutes after it was created however present its holder was, and every direction after that queued behind a controller who no longer existed. The heartbeat is now the holder's own read of the mission, which is what the room polls while it is open (D-051). Evidence: `reliability.test.ts`, 11 tests, including a lease backdated past its TTL that survives its holder's read and does not survive a bystander's. A runner's liveness has always been renewed by its polling and is unaffected. Still Partial: neither path has been observed against a machine that actually died in the app |
| Stopping an execution | **Proven in the app** (D-053). A participant with `execution.stop` stops a running harness turn: the command bypasses the workstream's command chain instead of queueing behind the thing it interrupts, reaches the turn while it is still in the active set, and terminates the harness process *group* gracefully and then forcibly. It is matched to the execution it names, so a stale stop re-offered after a relaunch cannot kill whichever turn is running now; it is remembered, so a stop that lands before its turn has spawned prevents the turn rather than being swallowed; and a stopped execution's queued direction is discarded with it rather than starting a second harness process in the worktree. Only the harness is affected — a dev server a participant started outlives it (D-045). The room says *Stopping* while the interrupt is in flight and stops offering a control that has already been pressed. Refused server-side for a role without the capability, with no state change and no command enqueued. A terminal execution accepts no further projection, so a late transcript event cannot go on talking, overwrite the workstream's resume point with the session the turn was killed in, or commit a checkpoint after the stop. **Proven in the app** (`e2e/runtime.spec.ts`, 2026-08-04): a real window shows *Running — Claude Code is working* with Stop, the click lands while the harness is mid-turn, and the trace ends *Stopped* rather than *Turn completed*, with the control gone (screenshots `59-`, `60-`). Evidence also: `workspace-agent.test.ts` (5 stop tests through the agent, two of which fail against the previous code), `runner.test.ts` (the refusal, the late-event guard), `execution.test.ts` (the turn's own kill path). **The crash window is closed** (D-070): a command is recorded as begun *before* the work starts, so a machine killed mid-turn comes back, declines to run it a second time, acknowledges it failed, and reports the execution *interrupted* — where Restart is the human's choice. Evidence: `runner-agent.test.ts` (2 tests, both failing against the previous code — one that no turn starts and the execution is reported interrupted, one that the memory survives a second relaunch) |
| Runner protocol (events, commands, reconnection) | Partial — the host desktop registers as a workstream's runner and receives a credential stored only as a hash, scoped, expiring, and revocable; only that credential may write harness-, runner-, execution-, workspace-, or verification-attributed events, which closes D-033. Commands are durable, ordered, idempotency-keyed, and acknowledged, and a workspace command now carries the immutable snapshot it was authorized against (D-043). The runner keeps its own at-most-once record of what it has begun, written before the work rather than after it, so a machine killed mid-command settles it as interrupted instead of running it again (D-070). Events carry a monotonic per-execution sequence, are de-duplicated server-side, and are buffered in a local outbox with backoff, bounded storage, and a gap marker on overflow. The transport is authenticated polling, not the persistent connection D-021 chose. There is deliberately **no heartbeat verb**: liveness is a side effect of that polling, which refreshes the runner's last-seen time, and the control plane's sweep reads it — the previous claim that "there is no heartbeat watchdog" was stale and wrong about the consequence, and is corrected in the row below |
| Presence | Not started — connection state is inferred from the runner's last-seen time only |
| Changes (diff) presentation | Partial — per-file change rows with path, state, and addition/deletion counts, and the real unified diff fetched on demand, all derived from git. Rename and binary detection exist. Inline comments and review threading do not |
| Verification evidence | Partial — **participant-run verification is executable and proven in the app** (`e2e/runtime.spec.ts`, 2026-08-03), end to end: the Run control asks, the control plane authorizes `workspace.command` and pins the declared snapshot, the runner executes it in the worktree, and the check lands with origin `participant`, who asked, start and completion, duration, exit code, how it ended, bounded redacted output, and the exact checkpoint SHA read *before* the command started. A check whose revision the workspace has moved past is presented as stale rather than as current proof, and stale checks do not count toward readiness. A check that timed out or was cancelled is recorded `errored` with the ending named — never a pass and never a plain failure (D-043). The previous entry said both "a participant can now run a declared check" and "not yet executable" in the same cell; the first was correct. Harness-observed checks are still recorded only from a structured tool call correlated with its own tool result, and assistant prose never becomes a check. The room says "No checks observed" when nothing ran. Re-running a failed check from its own ledger row now exists (D-057, row above). CI ingestion and manual confirmations do not |
| Competing approaches | **Tested** (D-074) — a second lane is forked from a recorded checkpoint of the one beside it, with a required intent the server refuses empty, its own server-allocated branch, its own worktree, its own runner enrolment and credential, and its own baton held by whoever created it. Forking is `approach.create`, held by Mission Admin and Operator and never granted by the baton; a Contributor is refused. The index that made a second workstream impossible is dropped, direction and control name the lane they act on, and a check now records which lane it proves so staleness is per lane rather than per mission. **Proven in the app** (`e2e/decision.spec.ts`, 2026-08-06): in a real window the room offers *Try another approach* only once the lane has something to fork from, the dialog's primary action stays dead until an intent is typed, the new lane gets its own branch, and directing it produces a checkpoint in **its own worktree** — asserted on disk, along with the absence of the mission-keyed directory the old build used (screenshot `83-`). **The fork point is the shared checkpoint (D-079, 2026-08-06)**: forking beside a lane that is itself an approach starts from that lane's recorded origin rather than its own later work, the dialog shows the mission goal and the exact shared checkpoint with "changes made only in {lane} stay there", the creation request pins the revision the person was shown and is refused `origin_moved` when it has moved, and lanes are named Current work / Alternative / Alternative 2 rather than `main` / `Approach 2`. **Approaches are now operable, not only comparable (D-080, proven in the app 2026-08-06)**: the room's strip carries one tab per lane with a blue/amber identity dot and a Compare tab; the canvas is the selected lane's own room with "Alternative · isolated workspace · forked at {sha}" under the title; a compact *Approaches 2 ▾* switcher states the shared checkpoint and each lane's own facts with Open; the rail says "2 approaches" under the mission's single row; the composer's foot names its target and **every direction carries the lane on screen** — the server resolves the named lane's own lease, refuses a foreign lane 404 and a role without the verb 403; `missions.get` is lane-scoped so control, capabilities, workspace, **state and overlays** are the selected lane's own (a fresh Alternative reads as its own empty room, not its sibling's finished work — found on the first screenshot of this slice and fixed); files, terminal, setup, secrets, checks and output all act in the active lane's worktree; and the selected lane survives a relaunch with the composer target intact. Evidence: `e2e/decision.spec.ts` re-run 2026-08-06 (1/1, ~10s) driving all of it in a real window — the composer direction sent while the Alternative tab is active checkpoints in the Alternative's worktree, and a reload comes back reading the Alternative with the receipt still on screen (screenshots `83-` … `89-`, reviewed at 1440); `approaches.test.ts` (17 — the routing set: a direction naming a lane lands in that lane's row, events, execution and checkpoint; the lane-scoped read returns that lane's own control and state and refuses a foreign lane; a viewer is refused on a named lane; the rail's lane count), `working-set.test.ts` (19 — the stored lane restored, changed per tab, malformed lanes dropped), `runner-agent.test.ts` (two lanes on one machine get two enrolments and two worktrees, on their own branches). **Not yet in the evidence set**: narrow and medium window widths for the new lane chrome |
| The Decision Room | **Tested** (D-074) — one surface per mission comparing every lane in the same shape: name, intent, state, baton, changed files with the diff's own arithmetic, checks passed and failed, **what was not verified**, the directions and approvals and stops people spent on it, what the harness said it cost, and the revision it is all about. Columns render in creation order, and a test asserts the order does not move when one lane's checks pass and the other's fail — the shape a helpful sort would take. Files more than one lane changed are named as contested and resolved by nobody. **Proven in the app** (`e2e/decision.spec.ts`, re-run 2026-08-06): with one lane the room offers no comparison at all; with two, the columns render in creation order with the approach's intent on it, *Not verified — everything, no check has run against this* in words, and the contested file named with **Inspect contested changes**. The rendered text is asserted to contain no *recommend*, *winner* or *score* (screenshot `84-`). **Refined the same day**: the header reads *Compare approaches* with *Shared checkpoint · {sha}* beneath it when every fork genuinely started from one revision, the way out reads **Keep exploring**, and with three or more lanes the columns stay two — a row of lane chips picks the compared pair, one click swapping one column, proven in the app with a third approach forked mid-run (screenshot `90-`). Fixed on the way: `branch/report` and `branch/retry` answered with the mission's *first* lane rather than the lane they were asked about, so a created approach came back wearing the baseline's identity — visible only once a second lane existed |
| Recording a decision | **Tested** (D-075) — a decision names the chosen lane and the exact checkpoint, requires a rationale in the person's own words (refused empty by the route *and* the database), carries the risks they accepted, and captures the checks that were unresolved **at that moment** rather than recomputing them later. It is `review.approve`, the capability PRODUCT.md's table has always carried and nothing implemented. A later decision supersedes an earlier one and neither is erased; asking for a revision withdraws the current one and records why. The mission then reads *Decision recorded*, whose whole job is to say nothing has been published. Evidence: `approaches.test.ts` (5 decision tests) and **proven in the app** (`e2e/decision.spec.ts`, re-run 2026-08-06): the confirm control is disabled until a rationale is typed, the dialog shows **the evidence it decides on** — the lane's changes, its checks, and the exact revision the decision will pin — alongside what it is about to record as unverified, *before* the decision is made, and the state line afterwards reads **Decision recorded — {who} chose {approach} — not published yet**; a reload keeps the decision, the receipt, and the lane that was being read (screenshots `85-`, `86-`) |
| Publishing a decision | Partial — **prepared, never sent** (D-075). Novus assembles the pull request a decision would open — title, base and head branch, and a body carrying the goal, the rationale, the evidence, the accepted risk and a *Not verified* section — as a projection over durable state, rendered read-only with a copy action. `publishable` is false where the repository is a folder on a machine rather than a host that could receive it. Nothing contacts GitHub, no pull request is opened, no merge happens anywhere, and the surface says so in words. Evidence: `approaches.test.ts`, and **proven in the app** (`e2e/decision.spec.ts`, 2026-08-06): the receipt renders the prepared request with its branches, its body, a copy action, and the sentence *Nothing has been sent* (screenshot `87-`). **Not started**: opening or tracking a real pull request |
| Review and revision requests | Partial — `review.approve` is implemented as the two acts PRODUCT.md's table names: recording a decision, and asking for a revision instead, both attributed and event-recorded (D-075). `review.comment`, inline comments, and review threading do not exist |
| Pull-request creation and tracking | Not started — see *Publishing a decision* for what is prepared and deliberately not sent |
| Receipts | Partial — a decision's receipt is rendered from durable state: what was chosen, by whom, when, at which revision, the rationale verbatim, the accepted risk, what was unverified, and the approaches that were not chosen, kept with their own intents. **Not built**: the receipt as a durable snapshot over an event range (ARCHITECTURE.md#persistence), a mission-level receipt, and export |
| Missions surface | Partial — the project-first shell (D-032): projects sidebar with a needs-attention lens, an Add project dialog with search, keyboard navigation, and a scrollable result region proven against 120 repositories at three window sizes |
| Mission Room surface | Partial — full workstream title, a persistent state line naming the state and the next action, the controller named in words rather than marked (D-047), a participant stack, one coherent direction trace per direction with human and harness identities distinguished, collapsed technical activity, checkpoint and changed-file evidence, an evidence ledger that stays honest when nothing ran, and a compact composer sharing the trace's content axis. Changes and Verification dock to the right edge as a toggleable column beside the trace rather than over it, a full-height column owning the window's right edge, shown and hidden by one control in the top row, and the projects rail discloses a project's missions from anywhere on its row (D-039, D-055). The panel's own participant-and-control header is gone, because who holds the baton is the room's sentence and was being said twice (D-066). **Approvals have a surface** — the question, what it would do, and Approve once / Deny, proven live and in two clients (D-062, row above) — and when nobody holds the baton the state line says so and the card offers Request control (D-073). A turn that has reported nothing for ten minutes says *no progress reported since {time}* rather than going on claiming to work. Pause/resume and review have no surface because they have no implementation |
| Review surface | Not started |
| Design token system and primitives | Partial — tokens.css carries the DESIGN.md values on rendered screens: the Light/Dark/System theme system, 6px buttons, sanctioned brand glyphs (D-029, D-030), and read-only local-harness detection on setup. **Reworked on rendered screens 2026-08-03 (D-047)**: the ground drops to near-black so surfaces have somewhere to rise from, text and edges strengthen, status becomes words rather than a coloured dot beside them, the baton stops being a mark and stays a sentence, and the dock stops carrying views at all — it is the terminal alone (D-049) — so only the evidence panel says *Verification*. A terminal tab is a glyph, a name, and a close. **Chrome quietened 2026-08-06 (D-076)**: one button size across every variant at the 32px target floor, a 1px focus ring with text fields showing focus on their own edge rather than wearing a white slab, dialogs as one plane instead of three, and a real `Dialog` primitive so `Esc` and a click outside close every one of them. Reviewed by eye on the approach dialog at 1280 wide; the other dialogs inherit it and have not been re-screenshotted. The scale has since gained `--type-speech` for what the agent said (D-065) and the token set a `--traffic-inset` for the window's own buttons (D-067, D-068). The light palette remains provisional — still not screen-proven. Several named primitives are still composed ad hoc rather than extracted, and the newest surfaces named in the top-row row are not in the screenshot set |

## Documentation

| Item | Status | Evidence |
| --- | --- | --- |
| Canonical documents (README, PRODUCT, DESIGN, ARCHITECTURE, PROGRESS, DECISIONS, AGENTS, CLAUDE symlink) | Implemented | Files exist at repository root; `CLAUDE.md` is a symlink (`ls -la CLAUDE.md`); `scripts/gate.sh` exits 0 (run 2026-08-02) |
| Executable repository gate | Implemented | `scripts/gate.sh` exits non-zero on seeded violations and 0 on the current tree (run 2026-08-02; D-016, D-026); it runs build, typecheck, lint, and the deterministic suites. **That 2026-08-02 run is still the last recorded pass**: verification since has been proportional under D-067, so the gate has not been observed against the current tree |
| Decision record | Implemented | [DECISIONS.md](DECISIONS.md) contains the numbered product and design decisions through D-069, including the workspace-runtime set (D-040 … D-046), files in the room (D-048), the dock as terminal (D-049), output in the evidence panel (D-050), the two uncalled verbs (D-051), the credential policy (D-052), Stop reaching the harness (D-053), missions in the rail (D-055), the approval-protocol probe (D-056), running a check again (D-057), repository recovery (D-060), the working-set and transcript decisions (D-061 … D-065), shell composition (D-066 … D-068), mission search (D-069), the at-most-once command memory (D-070), what a turn cost and the harness's own workers (D-071), the refusal to load project skills (D-072), and the stall overlay with the question that outlives its answerer (D-073) |
| Novus UI quality workflow | Implemented | Project skill at `.claude/skills/novus-ui/SKILL.md` points to `DESIGN.md` and requires a Surface Taste Brief plus rendered screenshot review |
| Canonical-truth reconciliation | Implemented | Run 2026-08-05, against the committed tree at `bccf441` and the evidence on disk rather than against commit messages. Corrected: approval routing was still called Not started in the milestone and "not built" in three rows and the verbs table, while it is proven live against real `claude 2.1.221` and proven in the app for two clients; the repository's `CLAUDE.md` was still recorded as unreachable after D-064 restored it through `--append-system-prompt-file`; `needs_approval` was recorded as never entered; re-running a failed check was listed as non-existent in the verification row while the row above it proves it; the evidence header's suite totals were presented as current when five slices have landed since that run; and the shell rows claimed the whole top-row surface as proven when the last three UI commits (the rail's switch, search as a palette, the workspace drawer) landed after the screenshot set with no regenerated evidence. Earlier run 2026-08-03. Four contradictions the previous revision carried are corrected against executable behaviour, not against commit messages: "GitHub repositories run locally — Implemented" claimed a capability eight places in the interface still refused, and is now Partial with those gates removed and the missing live proof named; "there is no heartbeat watchdog" was stale — the sweep is wired into the real server and ends a silent runner's execution in ≤105s — and the two rows now say the same thing; "Verification is observation-only … it cannot run a check itself" was false, and the row that said both "a participant can now run a declared check" and "not yet executable" in one cell is resolved to the first; and the terminal, listed as **Not built** while 17 real-PTY tests passed on disk, now has its own row saying what is tested and what is proven. A tiered evidence rule is stated in [Status rules](#status-rules) so "a suite passes" can no longer be written as "it works". Earlier reconciliation run 2026-08-02: The contradictions recorded against the previous milestone are resolved: local execution is stated as the first shipped surface in README, PRODUCT, and ARCHITECTURE rather than a footnote; repositories are GitHub **or** local in the domain model; the runner is the host desktop first; the local-repository question closed by D-032 no longer appears as an open extension point; disconnection no longer ends authorized work; and the mission's primary state is documented as a projection, not a stored field |
| Vertical slice — desktop workflow, live | Implemented | `pnpm run e2e` (2026-08-01, still passing): the actual Electron app signs in, creates a mission, closes fully, relaunches, restores its session from OS-backed storage, and reconstructs from PostgreSQL |
| Workspace runtime — terminal, setup, verification, through the interface | Implemented | `pnpm --filter @novus/desktop exec vitest run --config e2e/vitest.config.ts e2e/runtime.spec.ts` — **re-run 2026-08-06, 16/16 in ~35s**, after its draft-flow setup was rewritten against the ask-dialog (D-077) and its worktree paths rekeyed by workstream (D-074). Original run 2026-08-03, 13/13 in ~41s. A real Electron window, a real PTY running a real shell, real project commands in a real worktree. Proves: the dock opens straight into a focused shell with no switch and no rename control (D-049); a session opens in the mission worktree and not the user's own checkout; colour is painted and a clear genuinely clears; Ctrl-C interrupts; tabs named `{repository} {n}`, a `+` that selects and focuses what it made, switching back to an earlier tab with its session still on screen, and close; setup, the app, and checks each reachable from their own surface; a session survives the dock being closed from its toggle; a second desktop is refused the terminal both in the interface and by name at the bridge; setup runs from a reviewed proposal and reaches `ready`; a declared check runs on a click, passes with exit code 0 bound to the worktree's HEAD, and a failing one is recorded `failed` with its output; a relaunch shows no sessions rather than a dead tab. Screenshots `45-` … `58-` in `apps/desktop/e2e/evidence/`. The spec has since grown to 16 tests — missions in the rail (D-055), Stop (D-053) and running a check again (D-057) were added to it on 2026-08-04 and are recorded on their own rows; the 13/13 figure belongs to the 2026-08-03 run and has not been superseded by a recorded whole-spec pass |
| Two-client multiplayer — desktop workflow (fake harness) | Implemented | `pnpm --filter @novus/desktop exec vitest run --config e2e/vitest.config.ts e2e/multiplayer.spec.ts` — **re-run 2026-08-06, 1/1 in ~33s** (one correction: the workspace drawer opens on Overview already, and the spec's tab click was folding it away). Original run 2026-08-02, 1/1 in ~29s. Two real Electron clients, two separately authenticated people, one control plane. Kartik opens a local project, directs a turn, and invites Maya; she redeems from a second desktop **with no checkout of her own**, is refused the mission before redemption, cannot replay the invitation, reads the identical participants, directions, and checkpoints, submits a direction that queues attributed to her, requests control, is offered it, accepts, and takes the baton at the idle boundary. Kartik is then denied `direction.apply` and `control.offer` by the server. Maya applies her queued direction and **Kartik's runner executes it**. Both clients relaunch and reconstruct. Screenshots `20-` … `27-` in `apps/desktop/e2e/evidence/`. The harness is the deterministic fake: this is proof of the pipeline, not of Claude Code |
| Live Claude Code through the runner | Implemented | `NOVUS_LIVE_CLAUDE=1 pnpm --filter @novus/desktop exec vitest run --config e2e/vitest.config.ts e2e/live-claude.spec.ts` — re-run 2026-08-03 against `claude` 2.1.220, 1/1 passed in ~23s, still green after the runtime slice. Original run 2026-08-02 (~50s). The real `claude` binary, no fake harness. Observed in the durable record: two executions on session `89462e53-…`, the second with `resumed_session = true`; checkpoint `27b246bf…` with outcome `committed`, one file changed, `+1`; `file_changes` row `HELLO.md added +1 −0` with its stored diff; `verification_checks` count `0`. The test independently verified the claim against the repository itself — `git show <mission-branch>:HELLO.md` contains `hello` and the branch head equals the recorded checkpoint SHA — and the follow-up turn, told not to use tools, answered `HELLO.md`, which is only possible if the session was genuinely resumed. Opt-in because it spends the machine owner's Claude quota |
| Harness feasibility — documentation-level | Implemented | Official-docs verification for Claude Code and Codex recorded with sources in D-017 |
| Harness feasibility — hands-on, local + clean-Linux container | Partial | Run 2026-08-01. Claude Code 2.1.220 stream-json, `--resume`, SIGTERM semantics, allowlist enforcement; Codex 0.145.0 `exec --json`, resume, app-server handshake. Clean Ubuntu 24.04 install paths verified for both |
| E2B provider spike — live run | Implemented | Run 2026-08-01, results in `spikes/e2b/spike-results.json` (D-024) |
| Repository slice — desktop workflow (fake provider) | Implemented | Run 2026-08-01 on `slice/repository-missions`, still passing: repository selection, base pinning, branch allocation, relaunch reconstruction, and the failure/retry path through the real Electron app |

## Critical verbs, and what proves each one is wired

Three faults have reached real sessions because a verb was written, tested, and
never called (D-051). The check that would have caught all three is a test that
enters where the product enters, so every verb below names one. A unit test that
imports the function and calls it is not in this column, on purpose — that is
the kind of test all three faults already had (D-054).

| Verb | Entered through | Evidence |
| --- | --- | --- |
| Secret storage | the bridge's `supplySecret` | `workspace-policy.test.ts` — supplied, listed by name with no value, refused below the length Novus can redact, refused where the OS offers no encryption |
| Runner heartbeat | the runner's own poll | `runner.test.ts` — liveness is a side effect of polling; a runner silent past the threshold has its execution ended by the sweep |
| Lease heartbeat | reading the mission | `reliability.test.ts` — a lease backdated past its TTL survives its holder's read and does not survive a bystander's (D-051) |
| Repository clone and fetch | the runner agent's discovery pass | `workspace-clone.test.ts` — a second workstream's branch is fetched into a repository this machine had already cloned (D-051) |
| Command dispatch | the runner agent against a stubbed control plane | `workspace-agent.test.ts` — setup, run, stop, and verification each produce their real effect |
| Stop execution | a `stop_execution` command through the agent | `workspace-agent.test.ts` — two of its five tests fail against the pre-D-053 code |
| Check execution | an authorized command through the agent | `workspace-agent.test.ts`, and `e2e/runtime.spec.ts` on a click |
| Workspace setup | an authorized command through the agent | `workspace-agent.test.ts`, and `e2e/runtime.spec.ts` through the dialog |
| Checkpoint creation | `captureCheckpoint`, which the turn calls | `evidence.test.ts` — including a commit that genuinely fails, where the room is told what changed rather than nothing (D-054) |
| Event publication | the agent's outbox to a stubbed control plane | `workspace-agent.test.ts`, `outbox.test.ts` — ordering, de-duplication, backoff, gap marker |
| Approval response | Approve once / Deny in the room, answered back over the harness's own stdio channel | `e2e/approval.spec.ts` — two clients, the wrong one refused, the baton moving while the harness blocks; `execution-approval.test.ts` — a real process with a stub `claude` asserting the exact argv; `live-approval.test.ts` — the real binary, opt-in |
| Project instructions | the turn's own spawn, on every attempt | `execution-approval.test.ts` — `--append-system-prompt-file` is present with the worktree's `CLAUDE.md`, absent when there is none, and refused when the path leaves the worktree (D-064) |

## Known gaps

- **The live GitHub runtime proof is written but has not been run.** `apps/desktop/e2e/live-github.spec.ts` selects a real repository through the interface, creates a mission, waits for the runner to fetch it, checks the worktree is on the mission branch at the exact recorded base, runs a declared command and a check on it, then independently asserts that no installation token is in `.git/config`, anywhere under the machine's Novus state, in any event payload, or in any recorded check — and that a user session cannot ask for one. It is opt-in and cannot be otherwise: repository access comes from a GitHub App installation bound to a real organization, and that binding is created by a person signing in through their own browser, so the run borrows a profile that is already signed in. Until somebody runs it, "GitHub repositories run locally" stays Partial and the clone path is **Tested**, not proven live.

  ```
  NOVUS_LIVE_GITHUB=1 NOVUS_LIVE_PROFILE="$HOME/Library/Application Support/Novus" \
  NOVUS_LIVE_REPO=owner/name \
  pnpm --filter @novus/desktop exec vitest run --config e2e/vitest.config.ts e2e/live-github.spec.ts
  ```
- **The desktop suite's intermittent failure was a real bug, and is fixed.** It runs files one at a time, and 239 desktop tests passed on a quiet machine while `workspace-clone.test.ts` failed intermittently under load. The gate used to run every step into `/dev/null`, so a failure named the command and nothing else; that was fixed first, and the output then said `fatal: cannot change to '.../worktrees/msn_…': No such file or directory` — a worktree deleted while its creator was still using it. Two callers ask for the same worktree by design (the runner's turn path, and a terminal or file listing the person just opened), and preparation read "is it there" and then deleted what it found, so the loser removed the winner's worktree. The user-visible form is *"This workspace could not be created"*, reported from a real session and read at the time as a GitHub problem. Preparation is now serialised per worktree path (D-058). Proven by seeding the failure — bypassing the serialisation makes the new six-caller test fail with that exact message — and by four consecutive clean runs of a file that had failed one in three.

  Three more failures of the "only under load" family have since been caught **with** their output and fixed, which is the standard this line holds things to. A full `pnpm run e2e` failed the *first* terminal test at its poll ceiling while every later terminal test in the same run passed in under five seconds: the readiness helper decided a shell was ready after ~600 ms of silence, and a cold login shell — this machine's loads conda — emits its startup in bursts whose gaps exceed that window under load, so the keystrokes went into a shell that was not listening. The command is retried now, which addresses swallowed input rather than slowness. The same thing then happened in the *unit* suite, in `workspace-terminal.test.ts`, with the same remedy. The third was not a test problem at all: `workspace-agent.test.ts` failed teardown with `ENOTEMPTY`, because `shutdown()` returned while work queued on a workstream's lane was still running `git` inside the repository — publishing what a project declares reads the repository and can create a worktree. Shutdown now drains those lanes, which also makes quitting the app honest rather than merely quick.

  **The first of the three is not closed.** It recurred on 2026-08-05 in a full `pnpm run e2e` run immediately after a build: the retry ran out of attempts rather than the shell ever answering, and the same spec passed alone on the next run. The retry budget is raised, and that is a mitigation and not an explanation — retrying more often is not the same as knowing why a cold shell under load sometimes never reaches its prompt at all. Nothing has captured that yet, so this stays open.

  **And the family is wider than the terminal.** On 2026-08-05 a full `scripts/gate.sh` run failed three tests in `workspace-agent.test.ts` — a check that never completed inside its 30 s ceiling, and then two `beforeEach` hooks that timed out behind it — while the same file passed alone in 35 s immediately before and the gate passed on the next run with no change in between. So the shape is now known to be: real processes under a loaded machine, first failure cascading into the hooks after it. Still an explanation nobody has, and still open; what is new is that it is not specific to cold shells.
- **A project's `.claude/skills` do not reach the agent**, and that is a deliberate refusal rather than an oversight (D-072). Measured against `claude 2.1.223`: with the settings pinned the session lists 16 skills and none of the project's; with `--setting-sources project` the project's skill appears — and three MCP servers appear with it, alongside the hooks that flag has always re-admitted. `--plugin-dir` is the right *shape* and still refused, because a plugin can carry hooks and MCP servers too; the version that could work is Novus composing a skills-only directory out of the worktree under the containment `CLAUDE.md` already has, which is a slice of its own. What a project can do today is state its conventions in `CLAUDE.md`, which is carried and grants nothing.
- **The harness turn has no wall-clock bound**, which is D-034's intent, but the same chaining means a wedged `claude` process holds the lane: every subsequent workspace command from every participant queues behind it indefinitely rather than failing loudly. **Now visible rather than silent** (D-073): ten minutes with nothing reported carries the *Execution stalled* overlay and the state line says *no progress reported since {time}*, and the recovery is the Stop every non-viewer already holds. Still open: the queue behind it is not shown, and `force_interrupt` remains unimplemented as a distinct verb because Stop already does what it would do.
- **Approval routing pins the permission policy; the project's instructions are handed over explicitly.** `--setting-sources ""` is what stops a `.claude/settings.json` in the worktree deciding what gets asked — and the agent can write that file, so it cannot be an input. It also stopped the repository's `CLAUDE.md` loading, which is now restored with `--append-system-prompt-file` (D-064): measured against the real CLI with tools disabled, a codeword in `CLAUDE.md` is unknown to the model with settings pinned and known once the file is passed. Safe because the two files differ in kind — a settings file grants authority, `CLAUDE.md` grants nothing, and every tool call still reaches the permission router.
- **A controller cannot hand the baton back.** PRODUCT.md's lease model has a `released` state — no recipient, claimable by anyone — and there is no verb that reaches it: control moves by offer, by revocation (which is a force-take, not a release), or by the 30-minute expiry. So a considerate controller who is done cannot say so, and the only path to an unheld lease is silence. The consequences of an unheld lease are now handled and proven (D-073); the way *into* one deliberately is not built, and adding it is a capability-table change rather than an implementation detail.
- **The runner transport is polling.** D-021 chose control-plane WebSockets and D-035 records polling as the shipped transport with identical semantics. Event latency is bounded by the two-second poll.
- **The needs-attention lens reads one lane per mission.** A mission's row in the rail and the lens compute state from the default lane (or the lane the viewer's own tab is reading), so a *background* Alternative waiting on an approval is visible inside the room and in its own tab, but does not surface a closed mission in the lens (D-080). Fixing it means a mission-level "any lane needs a person" projection, which does not exist yet.
- **CI results are not ingested.** Novus can run a check a project declared, and re-run one from its own ledger row (D-057); it has no second attributed source of verification, so a row's origin is always Novus and the control can always offer to run it again.
- **Redaction removes values Novus was handed, and detects nothing.** Closed in this slice: the harness transcript is now value-redacted as well as path-masked, and proxy passwords are harvested out of the forwarded variables and folded into the same set (D-052). What remains open is the shape of the protection, not a bug in it. Novus can only remove values it holds, at least eight characters long, in the literal form it holds them — so a credential inside a supplied `.env` *file* rather than supplied as a *value*, one a project prints, one an agent mints mid-turn, an encoded or line-wrapped form of a held value, and a value split across two delivery chunks all survive. Path patterns cover conventional names only; a credential in `notes.txt` is invisible to them, and a template filled in with real values is exempted by name with its contents never read. **Novus does not detect arbitrary secrets and nothing here should be read as claiming it does.**
- **One repository per mission**, and no terminal mission lifecycle: a mission cannot be completed or cancelled. (One workstream per mission is no longer a constraint — D-074 — but a *decision* does not end a mission either: it records a choice and says plainly that nothing has been published.) It can now be *archived* (D-063), which is a different thing — filing the record away, not ending the work — and `mission.close`, which would end it, remains unimplemented.
- **Evidence diffs are stored inline** in PostgreSQL rather than in the artifact store D-022 selected, bounded to 150 files and 12 000 characters per file per checkpoint (D-037). A turn touching more files reports the first 150; the commit SHA remains the complete record.
- **A migration bug that only a second start would show, found and fixed 2026-08-06.** `schema.sql` widened the runner-command vocabulary in two places, and the earlier one re-added the list *as it stood when it was written*. So any database holding a command kind added later — the first `respond_approval` row a real deployment writes the moment somebody answers an approval — could never migrate again: the rerun dropped the good constraint and re-added a narrower one, which Postgres refused against the existing rows, rolling the whole migration back and leaving the control plane refusing to start. The vocabulary is now widened in exactly one place. Proven by seeding the failure: a scratch database carrying a `respond_approval` row fails to re-migrate against the old file and succeeds against this one.
- **Worktrees are never removed** after a mission ends, and there is no cleanup policy for terminal or archived missions.
- The desktop app runs from `pnpm dev` only; there is no packaged, signed, downloadable build, and no Windows or Linux run has happened.
- The light theme is still not proven on rendered screens; every screenshot is dark, macOS, and at three window sizes only.
- **Some renderer strings are not in the screenshot set.** The 2026-08-06 spec runs regenerated the navigation, runtime, slice and decision screenshot sets with the rail's aligned switch, the search palette, and the workspace drawer in place, and every one of those specs passes against the current shell; of the regenerated set, the approaches screenshots (`83-` … `92-`) were reviewed by eye, the rest were not re-reviewed individually. Three lines from the harness slice are not there either, and two of them cannot easily be: what a turn cost appears only when a real harness reports figures (the scripted one reports none, which is why screenshots `74-` … `80-`, regenerated 2026-08-05, correctly show a machinery line with no cost on it), *no progress reported since {time}* needs ten minutes of silence, and *no one holds the baton — claim it to answer* needs a lapsed lease. Each is proven by its own test; none has been looked at on a screen. They were inspected by eye during iteration, which D-067 allows, but no screenshot in `apps/desktop/e2e/evidence/` shows them and none of the end-to-end specs that assert those surfaces has been re-run since — so the top-row row is Partial rather than proven.
- Egress default-deny via provider IP rules is proven unworkable (D-024); the egress proxy that replaces it does not exist even as a specification.
- The repository-continuity contract (D-025) is only partly implemented: mission branches, base pinning, and checkpoints exist; remote-update detection, explicit synchronization, conflict presentation, and replacement-workspace reconstruction do not.

## Blocked

- The authenticated-harness leg of the provider spike is blocked on explicit opt-in to transmit provider API keys to a disposable sandbox and on an Anthropic API key that is not present on this machine. Only the account holder can provide these.

Next step: the opt-in live GitHub runtime proof, which is the one thing standing between "GitHub repositories run locally" and a live claim; then a regenerated evidence set for the shell changes that landed after it, so the top row can be proven rather than described; then the demo path, rather than expanding uncontracted review or cloud features.
