Purpose: The single honest statement of what exists right now. Anyone reading only this file should know exactly what Novus can and cannot do today.
Authoritative for: current implementation status, evidence, known gaps, the current milestone.
Not authoritative for: what Novus should become (PRODUCT.md), how it should be built (ARCHITECTURE.md), how it should look (DESIGN.md), why choices were made (DECISIONS.md).
Update when: any capability changes state, in the same change that alters the capability. Status claims require an Evidence line.
Last reviewed: 2026-08-12

# Progress

## Current milestone

**Two people operating one real mission (D-034 … D-037).** Live today in the desktop app: sign in with GitHub, open a local project, and direct real Claude Code in a dedicated mission worktree under a credentialed local runner. The turn's changes are recorded as git-derived evidence, a follow-up direction resumes the same harness session, and a second authenticated person can join by invitation from another desktop, see the identical durable mission, queue an attributed direction, take the control baton through an explicit request-offer-accept handshake, and have their direction executed by the first person's machine. Closing and reopening either client reconstructs participants, directions, control history, checkpoints, and evidence.

**Terminal and runtime trust (D-043 … D-046)** lands on top of it, proven in the app on 2026-08-03: an interactive terminal on a real PTY that opens in the mission worktree and nowhere else and renders what real programs actually print; a dock that is the terminal alone (D-049), with setup, run, and check output read in the evidence panel's Output section on the machine that produced it (D-050); a setup flow a person reviews, saves, and runs; a declared check invoked on a click whose evidence is bound to the revision it tested; project-stated deadlines with a timed-out outcome that is not a failure; readiness as a declared signal rather than an assumption; secret values supplied at the machine that has them; and a loopback-only preview bridge. The command a participant authorizes is pinned to what they saw, so a turn editing the project's configuration cannot change what their click runs. **Since 2026-08-09 the preview is a surface inside Novus** (D-098): a person imports a project, starts it, watches the declared readiness signal answer, and clicks and types in the running app without leaving the room — proven in the app, on its own row below.

**Supervised autonomy (D-062, D-064)** lands on top of both and is proven live on 2026-08-04 against real `claude 2.1.221`: the harness stops before an act it may not take, the question reaches the room, the person holding the baton answers it in the window, and the turn continues — with the permission policy pinned by Novus rather than read from a file the agent can write, and the project's own `CLAUDE.md` handed back explicitly through `--append-system-prompt-file`. Two clients are proven in the app for the same path. **Since 2026-08-11 the supervision has a chosen temperature (D-115, proven in the app)**: a lane wears a permission profile — Plan, Ask every time, Accept edits, Auto, Don't ask — set by role under warnings, applied by Novus's own router with every policy answer on the record, and never by handing the CLI a mode; there is deliberately no bypass, and no profile touches server authorization. **And since 2026-08-12 a project's own skills and MCP servers reach the agent the same governed way (D-118, D-119, both proven in the app)**: published for review by the machine that holds the worktree, enabled by a person at the exact content digest they read — skills by Mission Admin or Operator, servers by Mission Admin alone — composed into artifacts Novus authors (a skills-only plugin directory; a strict MCP config that is the CLI's entire MCP world), stated on the turn's own record carried or dropped by name, with hooks structurally absent and every MCP tool call still a person's question under every profile short of Don't ask. **And since 2026-08-13 Home is the board (D-120, proven in the app)**: every active mission in computed columns — Needs you first — with the card's one action opening the mission at the thing that is asking, the working set's open-at gap closed, and Home changed from a purge to a glance. **The same day the terminal lifecycle landed (D-121, proven in the app)**: a mission is completed or cancelled by a person's own `mission.close`, completion gated on a standing decision and a resolved pull request, the receipt snapshotted deterministically at close, the terminal room frozen into that receipt, and the board's fifth column — Complete — finally earned. Around it, since 2026-08-03: a mission is filed away rather than deleted (D-063), open missions are a window-level working set (D-061), a failed check is run again from its own row (D-057), and the shell was reworked into one top row, a full-height rail, and an evidence panel that is not a menu (D-065 … D-068) — the last of which has visual evidence only up to the 2026-08-05 screenshot set; see the row and [Known gaps](#known-gaps).

**Competing approaches and the decision between them (D-074, D-075, D-079, D-080)** land on top of that, **proven in the app on 2026-08-06**: a second lane forked from the *shared* checkpoint — shown to the person and pinned, never a later revision nobody looked at — with a stated intent, its own branch, worktree, runner credential and baton; lanes that are genuinely operable, as tabs in the room's own strip with a switcher, a per-lane state line, per-lane files, terminal, checks and output, and a composer that names its target and routes every direction to the lane on screen; one surface comparing each lane's own evidence with no score, rank or recommendation anywhere; and a decision that records a person's rationale, the risks they accepted and what was unverified at that moment, followed by a **prepared** pull request nobody has opened.

**Shared sessions inside an approach (D-083)** land on top of all of it, **proven in the app on 2026-08-07**: one lane can now hold several conversations — implement, add tests, review — sharing its branch, worktree, files, checks and baton while each keeps its own transcript and its own harness continuity. A session is created words-first like a mission, titled by its first words; the workspace takes turns, with a sibling session's direction queued visibly and dispatched by the control plane itself when the running turn completes; leaving a session stops nothing; and a reload restores the conversation being read. **On 2026-08-08 the owner redirected the presentation on sight of the built result (D-084)**: a mission's structure — approaches, their sessions, Compare — now nests as a tree under the active mission in the rail, the room's lane and session tabs and both switchers are gone, and a decision is the state line's sentence plus the Compare surface rather than three buttons. The slice's routing audit also found and fixed four places where a request that arrives through a *row* — a stop, a queued direction's resolution, an approval's answer, an enrolment-time dispatch — was resolved against the mission's first lane instead of the row's own.

**A decision now ships through GitHub (D-099, 2026-08-09, proven in the app against the fake host)**: the receipt on Compare pushes the decided revision itself to the host — a real authenticated `git push` through a write-scoped per-operation credential, with the draft refused until the remote serves exactly the decided checkpoint — then opens the draft pull request, requests reviewers, reflects the host's comments and their resolution read-only, marks ready on a person's own claim, and records a human's merge with their name as an external event. **And completion moved in-house the same day (D-100, owner-directed, Conductor's model named)**: the pull request is operated entirely from the room — the aggregated readiness gate (the host's checks with required flags, review decision, conflicts, branch behind/ahead, unresolved comments, beside Novus's own verification and accepted risks), commits and diffs unified or split with inline comments, the host's threads resolved from Novus and **sent to a chat as a prefilled direction the chat genuinely takes**, editable title and labels, and the completion verbs: **Merge** with the repository's own allowed methods behind a confirmation that restates every named blocker and records exactly what was accepted, Update branch, Close without merging, Delete remote branch after resolution, Archive after merge. Nothing ever merges silently or automatically — what the host itself cannot do is refused in its own words, and every other blocker is a person's deliberate, recorded acceptance. Proven in the app end to end against the fake host (`e2e/pr.spec.ts`, screenshots `108-` … `113-`), and the adapter's whole chain — push, draft, review, ready, readiness, **merge, and branch deletion — proven live** against real GitHub (`…/novus-live-pr-scratch/pull/2`, squash-merged by `app/novus-kartik` on the explicit call, the merge commit verified independently with `gh`).

**And since 2026-08-15 the preview's pixels are durable evidence (D-122, D-123, proven in the app)**: a person — or a coding agent, through a governed tool whose every call a person approves — captures screenshots and recordings of the running application's validated preview; each artifact carries verified bytes (the store confirms length and SHA-256 before anything reads as evidence) and machine-true provenance (live process, validated origin, declared readiness, the worktree's git HEAD at capture with dirtiness stated, who or what initiated it); evidence renders as restrained thumbnail rows, opens at reading size with its provenance ledger and the honest claim that a picture never proves correctness, attaches beside checks and the tracked pull request and into decisions without ever becoming a verdict; the terminal receipt freezes the exact references and a relaunch reconstructs them through freshly minted temporary grants, while a person without mission access is told nothing exists. The store is D-022's boundary realized: the local adapter for the local-first deployment and every deterministic suite, the S3 adapter written and **not proven live**.

Cloud execution, Codex, live ingestion of a real pull request's comments into a mission's record, and receipt export remain Not started.

## Status rules

- Every status is one of: **Not started**, **Partial**, **Implemented**, **Blocked**.
- **Implemented** and **Partial** require an Evidence line: a command with observed output, or a link to an artifact a human can inspect. Documentation is never evidence of functionality.
- Evidence comes in tiers, and a claim never borrows a higher one than it has earned:

  | Tier | What it means |
  | --- | --- |
  | **Tested** | A deterministic suite exercises the path. Real processes, real git, real PostgreSQL where the suite says so — but a fake provider or a scripted harness stands in for the outside world. Never live proof. |
  | **Proven in the app** | An Electron end-to-end run drives the real interface in a real window. What a person can actually reach, as opposed to what the modules underneath can do. |
  | **Proven live** | Exercised against the real GitHub or the real Claude Code binary, with the observed result recorded. |
  | **Partial** | Some of the capability is real and some is not, and the line says which. |
  | **Not started** | No implementation. |

- A deterministic suite passing against a fake provider is **Tested**, never "live". A module working is not the same as a person being able to reach it: code that no interface exposes is **Partial** at best, and the line says what is unreachable.
- The Golden V0 workflow ([README.md](README.md#the-golden-v0-workflow)) is the milestone gate for the first implementation: it is not done until two real clients execute all twenty steps.

## Application capabilities

Unless a line says otherwise, its deterministic evidence is the gate run of **2026-08-07** (the sessions slice, D-083): **19 contract, 193 control-plane against real PostgreSQL, and the desktop main-process suites** pass. **The whole Electron end-to-end surface was re-proven in one batch on 2026-08-15** (the artifact phase, D-122/D-123): all **fifteen** deterministic specs green together — 56 tests in ~470s, the new `artifacts` (8) among them, with the seven live opt-ins skipped as always. One correction on the way, in the D-120 helper family's own spirit: `slice.spec`'s `openProject` read the twisty once and clicked once, which under this phase's heavier detail raced the restore path's own disclosure and toggled *closed* the project it wanted open — reproduced alone, not only under load — and the helper now converges (click until the per-project fact reads open) rather than one-shotting; a first batch had failed exactly there and only there. Before it, **2026-08-13** (the D-120 night): all **fourteen** deterministic specs green together — 47 tests in ~331s — `slice` (5), `navigation` (8 — two rewritten against the Home board, deliberately, under D-120's entry: the canvas with nothing open is now the board, and the way into a new mission is the repository row's own `+`), `runtime` (16), `multiplayer` (1), `approval` (5), `sessions` (1), `decision` (1), `parallel` (1), `workers` (1), `preview` (3), `pr` (1), `theme` (1), `skills` (2), `home` (1) — with the opt-in **live Claude Code turn re-run green the same night** (`live-claude.spec.ts`, 1/1, ~24s, the real installed CLI through the runner, checkpoint verified against the repository). One helper family was corrected on the way: seven specs' project-disclosure helpers treated a count of visible mission rows as "this project is disclosed" — a wrong proxy that held only while the list outran the restore path's own disclosure, and D-120's heavier list flipped that race, surfacing one such failure per batch in a rotating spec. Every site (`slice`, `runtime`, `approval`, `decision`, `sessions`, `pr`, `parallel`) now asks the twisty's `aria-expanded`, the per-project fact, and the batch of **2026-08-13 after D-121 runs 48/48** — the D-121 re-run of the full set, with `home.spec` grown to 2. The prior batch evidence stands beneath: from **2026-08-08, re-run once more after D-089** (the overview landing, the explicit-only tab row, the working drag): all seven deterministic specs green in one batch (35 tests, ~175s) — `e2e/decision.spec.ts` (1/1, rewritten from tabs to tree rows), `e2e/sessions.spec.ts` (1/1, likewise), `e2e/approval.spec.ts` (3/3), `e2e/multiplayer.spec.ts` (1/1), `e2e/runtime.spec.ts` (16/16), `e2e/slice.spec.ts` (5/5), `e2e/navigation.spec.ts` (8/8) — plus the **opt-in live Claude Code runs**, which were not re-run. From the 2026-08-07 runs, honestly kept: `slice` and `navigation` each failed once in a loaded batch — one at a rail row not yet rendered, one at a room still saying *Loading mission* — and passed alone immediately after, the known only-under-load family ([Known gaps](#known-gaps)); navigation's instant-read assertion is now polled to the same end state, requiring nothing less. The 2026-08-09 pull-request slices (D-099 then D-100) grew `pulls.test.ts` to 9 (real PostgreSQL, the two-tier merge gate included), `workspace-push.test.ts` (4, real smart-HTTP remote), `pull-presentation.test.ts` to 7, and `e2e/pr.spec.ts` (1/1, ~18s at last run, the in-house flow through merge, deletion, and archive; screenshots `108-` … `113-` regenerated 2026-08-09 after the confirm dialogs were recomposed onto the dialog anatomy — head, sub, segmented method picker, quiet fact rows — with the merge confirm and readiness ledger reviewed by eye), and re-ran `e2e/decision.spec.ts` and `e2e/sessions.spec.ts` green as the D-099 regression check (2/2, ~39s).

Getting those specs green was itself part of the 2026-08-06 slice: four of them had gone stale against decisions that landed without their tests being rewritten, exactly as PROGRESS warned. The draft-flow assertions D-077 removed (`new-mission` rail row, `draft-base`, drafts in the strip) were rewritten to drive the ask-dialog in `navigation.spec.ts`, `runtime.spec.ts` and `slice.spec.ts`, under D-077's own entry as the required discussion; worktree paths keyed by mission were rekeyed by workstream (D-074) in `approval.spec.ts`, `runtime.spec.ts` and `slice.spec.ts`; and two specs that clicked the workspace drawer's Overview tab — which now *toggles* — were corrected. One real regression was found and fixed on the way: a verification check with no lane and no execution attribution fell out of the lane-scoped state entirely instead of counting toward the lane the mission started with.

| Capability | Status |
| --- | --- |
| Authentication and sessions | Partial — GitHub OAuth desktop flow (D-027), first-party revocable sessions, hashed tokens, OS-backed credential storage. Unconfigured OAuth fails fast with a named 503 surfaced in the setup card. **Live GitHub round-trip proven 2026-08-01**: the user signed in through the real system-browser flow and the durable user row exists (`users.login = KJ14GOD`, github_id present). A session now resolves its organization deterministically (oldest membership) and no longer decides mission access; remaining Partial because sign-in is untested on Windows/Linux and there is no packaged build. **The first-run room recomposed (D-147, 2026-08-18)**: centered to the window on flexible spacers, the greeting at the new `--type-display` step, a hairline before the Theme row — before/after `e2e/evidence/189`/`190`. **And woken up (D-148, same day)**: one entrance choreography, probe findings typing themselves in the mono voice, the subtitle addressing the signed-in person, and Finish setup always present — disabled until GitHub connects (`191`–`193`, frames frozen via the Web Animations API over the real stylesheet); a live signed-out capture still pends the next e2e run |
| Organizations and membership | Partial — personal org auto-provisioned at first sign-in; a mission invitation adds the redeemer as an ordinary org member, never an owner (D-036). Multi-membership works: mission access is scoped to the mission's own organization plus a participant row, so a person in two organizations reads and acts correctly. No org management surface, no org switching |
| GitHub repositories run locally | Implemented — **Proven live on 2026-08-16** (`e2e/live-github.spec.ts`, 5/5, screenshots `60-` … `62-`): a real Electron window under the operator's signed-in identity selected `KJ14GOD/novus-live-pr-scratch` through the interface, created a mission, the runner fetched it through a minted per-operation credential, the worktree sat on the mission branch at the exact recorded base, a declared setup command and a verification check ran through the Run control, the relaunch reconstructed it with the checkout reused — and the after-the-fact sweep found the installation token nowhere: not in `.git/config`, not in any file under the machine's Novus state, not in any event payload or recorded check, and a user session's ask for one was refused. Underneath, as previously **Tested**: The control plane mints a short-lived, repository-scoped installation token for the authenticated runner only, and the host clones the repository into its own area with the credential injected per operation over a pipe. The token reaches no event, no command, no durable row, and is absent from `.git/config` afterwards; a user session cannot obtain one. A second workstream shares the checkout and takes its own worktree — and its branch is fetched into that checkout rather than assumed present, which a real session found the hard way: the runner asked whether it held the *repository* when a worktree asks whether it holds *this workstream's branch*, so every mission after the first failed with `invalid reference` (D-051). Nothing downstream of the clone has a case for GitHub. A second path to that same message was found and closed here: publishing what a project declares *creates* the worktree, and discovery announced even when the branch had not arrived — because `ensureCheckout` throws the first time but returns silently for the whole of its 15 s → 5 min backoff, so one transient fetch failure produced `invalid reference` on every fifteen-second pass, swallowed into a single console warning (D-060). Evidence: `repo-clone.test.ts` (8), `workspace-clone.test.ts` (15) — including six callers contending for one worktree (D-058) and a mission whose branch could not be fetched, both proven by seeding the failure — against a real HTTP git remote that genuinely demands a credential — **but a fake one, not github.com**. The previous entry said "Implemented" while eight places in the interface still refused any non-local provider, so no user could reach any of it; those gates are gone (hosting is now "does this machine hold the checkout", not "which provider"). The spec itself needed modernizing to run at all — it predated D-074's lane-keyed worktrees, D-077's ask dialog, and D-120's Home board — and its run surfaced one still-open observation, recorded in Known gaps |
| Repository connection (GitHub App) | Partial — the GitHub App exists (manifest flow, one-click create + install; app `novus-kartik` installed on the user's account 2026-08-01) and the live adapter is proven for **listing and exact base resolution** against the user's real repositories. Live branch creation is implemented with the same idempotency mapping but has not yet created a real branch. **Local repositories** (D-032): registration with machine-bound identity, folder paths never leaving the machine, desktop-side git, reported outcomes as claims — proven live end to end |
| Repository branches, checkpoints, and workspace synchronization | Partial — one server-named mission branch per workstream pinned to an exact 40-hex base SHA, idempotent creation, failed/pending/created as durable state with retry. **Checkpoints are real**: an attributed commit at each turn boundary with its SHA, parent, per-file state and counts, withheld-secret count, branch, and reporting environment, all derived from git (D-037). Workspace synchronization with a remote (D-025) is still not started |
| Mission creation and lifecycle | Partial — creation with goal + success criteria + repository + exact base revision; mission, Mission Admin participant, workstream, and the creator's first control lease commit in one transaction; creation-key idempotency proven under concurrent duplicates. The mission's primary state is now a **projection** over durable workstream, execution, and evidence state rather than a stored word — since D-080 the room's word is the *selected lane's* own, and since 2026-08-08 the list's is the mission-level combination over every lane with PRODUCT.md's fixed precedence (attention, then running, then waiting; `attention.test.ts`), so a background lane waiting on a person is never hidden behind whichever lane ran last — and since D-093 (2026-08-08) an attention-demanding mission's list row also **names where**: the lane whose state decided the word, and the blocked conversation with its title where the attention is an execution's fact (`Mission.attention`, null when nothing is asked; `attention.test.ts`). Terminal lifecycle states (Completed, Cancelled) do not exist |
| Invitations and participants | **Implemented for the V0 shape** — issued and redeemed from the interface (D-039): the inspector's Overview creates one and shows its token exactly once, the rail offers Join with invitation. Mission-scoped, single-use, expiring, revocable; only the token hash is stored and the usable token is returned exactly once; redemption is authenticated and creates both the org membership and the participant row in one transaction. Replay, revocation, expiry, and wrong-mission redemption are each rejected by name. Evidence: `authority.test.ts`, plus the two-client run in which a second desktop joins and reads the identical mission |
| Roles and server-enforced capabilities | Partial — the four mission roles exist and capabilities are computed as role ∪ lease in one place and enforced on every mutating command; a non-participant receives 404, never 403, so a mission identifier is not a capability. The interface renders from the server's capability list. `approval.respond` is implemented and enforced as a lease capability in no role list (D-062), and `mission.archive` as Mission Admin's alone (D-063). `pr.manage` is implemented and enforced (D-099): pushing the branch, opening the draft, requesting reviewers, and marking ready are Mission Admin's and Operator's, refused 403 for a contributor and 404 for a non-participant, and there is deliberately no capability that merges because no verb for it exists. `force_interrupt` is implemented and enforced (D-111, 2026-08-11, Tested): held by the controller's lease and Mission Admin's role, it declares a `stopping` turn dead only after its stop went unanswered past a one-minute grace (or the machine went quiet) — refused 409 in words otherwise (*Stop it first*; *give it a minute*), 403 for a contributor, an attributed `execution.interrupted` with the person's name when it lands, the lane freed and provable by the next direction dispatching (`runner.test.ts`, 39, three new). `policy.set` is implemented and enforced (D-115, 2026-08-11): Mission Admin's and Operator's, never lease-granted — a Contributor holding the baton is refused — with `dont_ask` tiered to Mission Admin alone and refused until acknowledged (`policy.test.ts`, 9). `mission.close` is implemented and enforced (D-121, 2026-08-13): Mission Admin's alone, never lease-granted, with completion's own gates judged server-side — the terminal-lifecycle row below carries the evidence. `review.comment`, `workspace.sync` and the pause/resume verbs still have no implementation: the features they gate do not exist (D-073's refusal of force_interrupt is narrowed by D-111 to exactly the case where Stop has demonstrably failed) |
| Control leases, requests, handoffs | **Implemented for one workstream** — one durable lease per workstream with a single holder, compare-and-swap transitions, claim of an unheld lease, requests that stay open and visible, offers to a named participant, accept/decline/withdraw, immediate transfer at an idle boundary and deferred transfer at a runner-declared boundary while running, Mission Admin revocation, and every transition event-recorded. The previous controller loses privileged authority the instant the transfer completes. Lease TTL expiry is implemented: a lease is released by transfer, revocation, or a 30-minute heartbeat lapse, where the heartbeat is the holder's own read of the mission (D-051) — of the *mission*, on any lane, since 2026-08-08: reading one approach renews every lease the reader holds in that mission, because before that a controller of a background lane who sat watching a sibling lane lost their baton to the sweep while present, while a read by someone who does not hold the baton still renews nothing (`attention.test.ts`). **An unanswered offer now expires** (D-092, 2026-08-08): `expires_at` is set at creation (ten minutes), carried on `HandoffOfferSchema`, swept from `open` to `expired` with a `control.offer_expired` event the trace renders in words, refused at the accept route even before the sweep has moved it, and counted down as text on the offer row ("expires in 7m") per DESIGN.md's *Handoff offered* overlay — in the regenerated screenshot `24-handoff-offer.png`, reviewed. Only an `open` offer expires; an accepted grant still completes at the boundary. Evidence: `authority.test.ts` (the wire expiry ≈ ten minutes, the sweep's lapse + event + freed slot, the pre-sweep accept refusal) |
| Direction lifecycle | **Implemented** — durable, attributed, ordered direction with submitted / queued / applied / superseded / rejected / cancelled. Applied is written only on the runner's acknowledgement, never on send. A non-controller's direction queues visibly with its author; the controller applies, rejects, or supersedes it; the author may cancel while it waits. Queued direction survives relaunch. **A direction now lands in one session of its lane** (D-083): absent names the lane's first, `newSession` creates one from the direction's own words, and a controller's direction for a session whose sibling is mid-turn queues visibly with the running session named and is dispatched by the control plane when that turn completes — never after a stop, failure, or interruption. **A lost completed-turn dispatch no longer strands the queue (D-112, 2026-08-11, Tested)**: the reliability sweep re-drives a never-carried queued direction over an idle lane whose last word was `completed` — the same dispatcher, re-checking baton and capability itself — while a queue behind a stop, failure, or interruption is still never auto-run (`reliability.test.ts`, 18, two new: the lost dispatch revived exactly once, the failed-turn queue left alone); and the working state line states the backlog as a quiet suffix — *· {n} directions queued* — so a controller's own queue is no longer invisible while a turn runs (`room-presentation.test.ts`, 33). Known gap, deliberate: the dispatcher takes the oldest queued direction and stops, so one undispatchable direction (scope overlap) head-of-line-blocks those behind it. Resolving or cancelling is judged against the *direction's own* lane's baton (fixed in this slice — it was the first lane's; `sessions.test.ts`) |
| Workstreams and executions | Partial — durable executions with harness, model, effort, runner, starting direction, state, timestamps, harness session, exit outcome, failure reason, last acknowledged sequence, and latest checkpoint. At most one active execution per workstream is enforced by a partial unique index, not by an in-memory map. States reached in practice: requested, starting, running, needs_approval, stopping, completed, stopped, interrupted, failed. `needs_approval` is entered for the first time by approval routing (D-062); `needs_direction` and `paused` exist in the model and are still never entered. **A mission may now hold more than one workstream** (D-074): the index that forbade it is dropped, and each lane owns its executions. **An execution now belongs to exactly one session** (D-083): `session_id` is NOT NULL and migrated, harness continuity is that session's own, and `workstreams.harness_session_id` survives only as a legacy mirror of the lane's first session's. **And an execution carries its access** (D-095): at most one active *write* execution per lane — the worktree's one honest writer, still a partial unique index — plus at most one active execution per *session* whatever its access, while `read` executions run alongside: started immediately by the baton holder, denied every permission request at the runner, no checkpoint, no boundary, invisible to the lane's state |
| Shared sessions inside an approach | **Proven in the app** (D-083; `e2e/sessions.spec.ts`, 2026-08-07, 1/1 in ~22s). A workstream holds one or more **sessions** — parallel conversations sharing its branch, worktree, workspace, files, checks and baton, each owning only its thread and its own harness continuity. Every workstream is born with one, and every existing workstream was migrated onto its lane's first (`session_id` NOT NULL on directions and executions, backfilled — proven by migrating a seeded pre-session database and re-running the migration against it). A session is **words-first** (D-077 one level down): *New session* opens an empty conversation, the first direction creates and titles it in the same transaction, abandoning it leaves nothing anywhere, and there is no `session.create` capability because creating a session *is* `direction.submit` — so a Viewer cannot. **Continuity is per session**, decided where the rows are: the control plane starts each turn with that session's own resume point and the runner needs no session awareness — and a fresh session's null resume no longer falls back to the workstream's legacy column, which would have silently resumed a *sibling's* transcript (found while wiring, fixed, asserted end to end: two sessions minted different harness session ids and each later turn resumed its own, `resumedSession` true against the right id, in the window). **The same fallback survived in one more payload and is now closed (2026-08-10, the D-109 audit)**: an `apply_direction` command named no session, so when it reached the runner after its turn had already ended, the fresh process it started fell back to the lane's *first* chat's transcript — the apply now states its conversation and that conversation's own resume point outright (`sessions.test.ts`, the steer test asserting both on the enqueued payload). **The workspace takes turns**: one live harness turn per lane stays the partial unique index; the running session's own direction steers its turn; a sibling session's queues visibly — the composer confirms *Queued — "{title}" is running; this applies when it finishes* — and a **completed** turn dispatches the baton holder's oldest queued direction by itself, never after a stop, failure or interruption, and never a direction an execution already carried (a guard the archive suite caught being needed). **Presented as the rail's tree since D-084 (2026-08-08, owner-directed relayout)**: one row per session of the selected approach, nested under the mission where its project is, with a background session's *· needs you* in words on its row, each session row rendered directly beneath its own approach's row (the first build put the selected first lane's sessions after the *last* approach row, where they read as its children — found by eye in live use 2026-08-08 and fixed the same hour; no spec pins in-tree row order), a quiet hover `+` on the new session's deepest visible parent (the selected approach's row, or the mission's own row while its one lane is invisible), and, since D-087 (2026-08-08, asked for by name), session tabs again as well: a tree row opens its conversation as a tab on the room's working row beside the approach tabs and files, several open side by side — since D-088 from any approach at once, each tab wearing its approach's identity dot, jumping the room to its lane when chosen, and drag-reorderable into the person's own order — and since D-089 (2026-08-08, on sight of D-088 live) the row holds **exactly what the person opened**: the implicit anchor is gone, every tab closes, switching approaches adds, removes and swaps nothing, and an approach tab or row lands on the approach's **overview** — intent, one line of its own facts, and its conversations as clickable links that open tabs — while a one-conversation lane still lands straight in the conversation. The D-088 drag never worked in the running app — Chromium refuses to start a wrapper's drag from an interactive child until the wrapper claims the gesture (`-webkit-user-drag: element`); found by the owner, fixed in CSS, and a throwaway probe drove a drag green in a real window (2026-08-08, then deleted; screenshots `99-approach-overview.png`, `100-session-tabs-reordered.png`) — **but that probe synthesized its input through the debug protocol, and the owner's real mouse still moved nothing**, an overclaim corrected the same morning by D-090: the reorder now happens live *during* the drag, browser-style — crossing a sibling's midpoint moves the tab at once, the tabs shifting are the feedback, the whole row accepts the release so no aim is needed — and **file tabs drag the same way**, files among files, which is what "let me move them around" had named in the first place. The dragged tab is carried in a ref because Chromium hides a drag's payload until the drop. Honestly stated: the deterministic suites and `working-set.test.ts` (28, open/close/reorder/persistence) are green on this build, synthesized-input probes are retired as evidence for gestures, and the live gesture awaits exactly one proof — the owner's own hand on a restarted app (both failed attempts ran a window started at 8:20, racing or predating the fixed builds); the pointer interactions stay unpinned per the standing no-tests call. The tree still lists every conversation, open or not, and a blocked turn stays blocked whatever is on screen (proven in the rewritten spec). The state line names the working session, a pending approval renders in every session's view with *asked in "{title}"* because it blocks the lane's one workspace, and the composer's foot names its whole target. A reload restores the selected session (`working-set.test.ts`, 24). No worktree is session-keyed, asserted on disk after six turns across two sessions. **And the lane answers "what has this cost so far?" on the same page (D-113, 2026-08-11, Tested)**: the overview's facts line adds the lane's turns, its summed harness time, and the harness's own cost figure — renderer arithmetic over the executions the room already holds, null never rendered as zero, nothing shown when nothing was reported (`room-presentation.test.ts`, 35, two new — the null-preserving sum, the all-silent lane; words-only addition to an existing text row, not re-screenshotted). **Every chat answers "what are you doing?" without being opened (D-094, 2026-08-08)**: each background row, tab, and overview link carries its own state word — *needs you* in warn as before, *working* and *queued · {n}* quietly, idle nothing — the overview link adds the chat's footprint (`{n} files`, from its own turns' checkpoints, latest per path) and *last heard {time}* while working, and when two chats' checkpoints have changed the same file the overview says so in one warn-toned sentence, evidence-only, never a prediction. Tested: `room-presentation.test.ts` (28, five new — state precedence, queue counting, per-chat file credit, overlap and its absence); the words are on screen in the regenerated `94-` and `95-` (reviewed), the overview's footprint row and overlap sentence are not in any capture — no deterministic frame holds two chats with checkpoints at capture time. Evidence: control-plane `sessions.test.ts` (15, real PostgreSQL — birth and naming, routing and refusals: foreign session 404, viewer 403, `sessionId`+`newSession` 400; per-session continuity with the workstream's legacy column mirroring only its first session's; turn-taking; and the four routing regressions recorded on the approaches row), `contracts.test.ts` (19), and `e2e/sessions.spec.ts` rewritten against the tree under D-084 and re-run green 2026-08-08 once more after D-094 (1/1, ~19s; `e2e/decision.spec.ts` 1/1 in the same batch), screenshots `93-` … `98-` regenerated, `94-` and `95-` reviewed — below 1200 the rail is an overlay, and the spec drives the tree through its toggle. **A chat now runs alongside the workspace's turn, read-only (D-095, 2026-08-08, proven in the app)**: sending in a sibling chat while a write turn runs asks the baton holder inline — Queue (the default), or *Run alongside · read-only* — and the alongside turn starts immediately, is denied every permission request at the runner with the reason on the harness's own channel (no card ever reaches the room), captures no checkpoint, declares no boundary a waiting handoff could complete on (refused server-side too, as an authority claim), and leaves the lane's state, stall watch, turn-taking dispatch, and idle-transfer checks reading write turns only; a question gone moot mid-ask submits the ordinary way rather than swallowing the click (found by the sessions spec through the poll's two-second lag); Stop carries the session so each of a lane's two live turns is stoppable by name, and the runner's active-turn map is keyed by execution because workstream keying let the second turn shadow the first. Evidence: `alongside.test.ts` (6, real PostgreSQL — immediate read dispatch beside a running write turn, the non-controller 403 in words, the one-turn-per-chat deferral, reads neither blocking new writes nor becoming the lane's state, the session-named stop, and a read turn's boundary moving no baton while the write turn's still does), `execution.test.ts` (+2 — deny-at-machine with no published approval and nothing written; no checkpoint and no boundary from any source), `e2e/approval.spec.ts` (+1, in a real window: a second chat's read turn answered to completion while the write turn sat blocked on its question, exactly one card, no read checkpoint, the file existing only after the approval; screenshot `101-`, reviewed), and `e2e/sessions.spec.ts` re-run green driving the choice row itself (it names the running chat; Queue keeps the entire pre-D-095 story). **Checks are now attributed to the chat whose checkpoint they ran at (D-096, 2026-08-09, Tested)**: a renderer join — check → revision → checkpoint → turn → conversation, no wire change — puts `{p}/{n} checks at its checkpoints` on the overview's chat rows and `· "{title}"'s checkpoint` on the ledger's provenance line while the lane holds several conversations; attribution by trigger, never by content, because the worktree a check exercised holds every chat's work so far, and a check with no claimable revision is attributed to nobody. Checks still *run* as lane evidence; readiness and staleness are untouched. Evidence: `room-presentation.test.ts` (32, two new — the join never crossing chats, the orphan revision belonging to nobody); not in the screenshot set, which has no frame holding two chats with checks at capture time. **Chats now own files, and provably disjoint owners write in parallel (D-097, 2026-08-09, proven in the app)**: the baton holder declares a chat's scope from the overview's quiet Scope control (prefilled from the chat's own touched directories, never from task text), the grant is event-recorded and pinned into each turn at dispatch; a scoped turn's Write/Edit/NotebookEdit are decided at the runner by the scope itself — auto-allowed inside, refused outside with the reason, no card either way — while **Bash always stays a human question**, because a shell command declares its targets nowhere; two chats with provably disjoint scopes hold two live write turns in the one worktree (the write-exclusivity index is replaced by the dispatch transaction under the mission's advisory lock; one matcher in the contracts package is every judge, and unprovable disjointness means overlap), an unscoped party on either side means exclusivity exactly as before; each scoped checkpoint commits only its own paths under a per-worktree capture lock, a running sibling's declared territory stays silently in flight, and paths in **nobody's** scope are drift — named on the checkpoint in the warn tone, never committed by the observer; the lane's baton transfers only at the *last* writer's boundary; scoped turns defer auto-verification to the drain, where a clean tree runs the declared checks once against the settled result and a dirty one reports `integration.blocked` with the paths — settling drift is a person's act, and an unscoped turn adopts it deliberately. Evidence: `contracts.test.ts` (25, three new — the matcher's `**`/`*` semantics, conservative disjointness including the no-prefix-overlaps-everything rule, the escape-refusing pattern schema), `scoped.test.ts` (6, real PostgreSQL — the baton-only grant with its event and the 403 in words, two live write turns at once, overlap deferring with the overlap named, exclusivity both ways around an unscoped chat, and the baton moving only on the last writer's boundary), `evidence.test.ts` (+3 — the scoped commit holding exactly its paths with drift named and left dirty on disk, clean-with-drift, and a sibling's declared territory staying silent), `execution.test.ts` (+2 — in-scope auto-allow with no card and no false prompt boundary, out-of-scope refusal with nothing written), and **`e2e/parallel.spec.ts` (1/1, in a real window)**: two scoped chats directed back to back, the second dispatching immediately, both `· working` in the rail tree at once, each turn's checkpoint holding exactly its own file with no drift, and the mission branch ending with both files on a clean tree (screenshot `102-two-writers-at-once.png`, reviewed — the tree, the overview's `owns server/**` / `owns ui/**` rows with their Scope controls, and the state line naming a working chat). **Not built, deliberately**: renaming a session; content-scoped check attribution — "chat A's tests" — which D-096 records as unknowable until a project declares its coverage; and simultaneous *unscoped* writers, which D-095's reasoning still forbids |
| Harness adapters (Claude Code, Codex) | Partial — **Claude Code proven live again 2026-08-02 through the new runner**: a direction submitted in the app started a real headless `claude` process in a dedicated worktree, its stream was parsed into attributed events, its change landed as an attributed checkpoint commit, and a **follow-up direction resumed the same harness session and recalled the first turn**. Six model ids offered, each verified live against the CLI 2026-08-02, now defined once in `@novus/contracts` and consumed by the renderer, the IPC boundary, and the adapter. Hardened: spawn-error handling, bounded stderr, final unterminated stdout line, defensive parsing, process-tree termination with SIGKILL escalation, authentication and non-zero-exit classification, and a checkpoint failure that can never report completion. **A turn now ends the way the harness said it ended (D-109, 2026-08-10, Tested)**: the result line's `subtype` — parsed and discarded since D-071 — is read, so a turn that stopped at its turn budget is `interrupted` with *direct it again to continue* (its session preserved) instead of the false *Work finished*; a clean exit with **no result line at all** is `interrupted` — *ended without reporting an outcome* — never a fabricated success; and a billing refusal is its own `billing` classification quoting the vendor's first line, checked before the authentication pattern so a paid-out machine is never told to sign in again. Archive's active-state list now derives from `ExecutionStateSchema` like the dispatcher's, ending a two-way drift (it listed unreachable `paused`, omitted `needs_direction`). Evidence: `execution-endings.test.ts` (3, through the real spawn path with a stub CLI per the D-056 pattern — max-turns → interrupted+resumable, silent end → interrupted, credit balance → billing without "Sign in"); `execution.test.ts` and `execution-approval.test.ts` re-run green (28). **What a turn cost is now carried** (D-071): the CLI's own `usage`, `total_cost_usd`, `duration_ms` and `num_turns` are read off its final line, reported as `harness.usage`, summed onto the execution, and stated on the trace's machinery line — an opaque claim that bills nothing and stops nothing, with a figure the harness never reported left null rather than zero. **The harness's own subagents are grouped, not impersonated**: `--forward-subagent-text` is passed, and text or tool activity carrying a `parent_tool_use_id` is indented inside the turn's technical disclosure rather than taking the speech position, with no branch, checkpoint, controller or workstream of its own. **And since D-107 (2026-08-10, Tested) the worker join is complete on the wire**: `harness.tool` records the tool call's own `toolUseId`, so a worker's tagged events connect to the exact Task call that spawned them, and a Task's tool result — parsed and discarded before — is recorded as `harness.worker.ended` with the result's own error flag and the worker's handed-back report; a result whose Task call was never seen emits nothing. Both recorded-and-nothing-else at the control plane. **And the room renders the join (D-107's presentation half, proven in the app 2026-08-10, recomposed the same day by D-108 on the owner's sight of the first build)**: workers are the turn's own rows on the milestone anatomy — a `workers` label, one quiet line per worker with its stated purpose, the last thing it did in mono, and a state word that is `working` only while the turn is live, silent when the turn settled without the end arriving — **Enter steps in, Esc steps out**, opening the worker's own view on the canvas (timeline, files its own calls named, its report or failure, no decision identifiers in the copy) with *Back to chat* returning to the conversation at the position the reader left; the technical disclosure went back to being the harness's own steps alone, two catch-all protocol rows (`execution.requested`, `boundary.reached`) were absorbed as saying nothing the direction row and approval card don't, a worker appears in no rail, tab strip, baton, or state line, and activity whose parent joins to no recorded spawn stays indented grouped activity exactly as before. Evidence: `harness-stream.test.ts` (26 — the spawn id on the Task row, completion and failure from the result's own flag, the unseen-result refusal, and every prior payload assertion updated for the new field), `trace-presentation.test.ts` (7 — the join, two concurrent workers kept distinct, honest state words including the settled-without-end silence, the unjoined fallback, the end-without-start refusal, files from the worker's own calls only), and `e2e/workers.spec.ts` (1/1 in a real window, ~8s: the fake harness emitting the real CLI's own line shapes through the production parser — rollup with `1 done · 1 failed`, the inspector with the worker's report, the failed worker's failure in its own view, and Back to chat both times; screenshots `120-`, `121-`, reviewed). An optional flag degrades where a pinned one fails: a CLI that refuses `--forward-subagent-text` by name is retried once without it, while a CLI that refuses to route approvals still fails the execution outright (D-062). **Proven live 2026-08-05** against `claude 2.1.223` through Novus's own turn path (`live-instructions.test.ts`, opt-in, 2/2): real usage figures arrive, and the project's `CLAUDE.md` reaches the model — answered from the system prompt with **no tool call in the turn**, so the file cannot have been read. Codex is not started and renders disabled |
| Workspace runtime — setup, run, verification | Partial — **Tested; parts proven in the app** (D-040 … D-046). `.novus/settings.toml` and `settings.local.toml` are parsed, validated, and layered by presence; a broken file fails with the file, the problem, and the TOML line. A project is inspected for manifests, lockfiles, Makefile/justfile/Taskfile, `.env.example` variants and `.worktreeinclude`, and Novus **proposes** commands that never execute without confirmation. Gitignored files are copied only after per-file consent, only when Git confirms they are ignored, refusing traversal, symlink escape, directories, and dependency output. Three environments are constructed explicitly: a harness credential cannot reach a project command, and a project command cannot see a worktree's absolute path. Ports are allocated per workstream and persisted. Commands are supervised in the worktree with process-group termination; a run command outlives the harness turn. **New in this slice**: the runner publishes what a project declares and the control plane pins that snapshot to each authorization, so the command a participant saw is the command that runs and a remote participant can read the same list (D-043); finite commands carry the project's own bounded deadline with `timeout` as a named ending distinct from a non-zero exit, a signal, and a cancellation; a run command has none, and a coding-agent execution never touches this supervisor; readiness is a declared signal, so a process that started is *starting* until its health URL or port answers and honestly *unreachable* if it never does, without being killed for it (D-045); a secret value can finally be supplied, replaced, and forgotten from the setup dialog, refused below the length Novus can redact, and refused outright where the operating system offers no encryption (D-044); a loopback-only bridge opens a preview that a live process actually reported. **Proven in the app** (`e2e/runtime.spec.ts`, 2026-08-03): a person reviews the proposal, saves it, runs setup — the witness file appears in the worktree and readiness reaches `ready` — then invokes a declared check on a click and gets `passed` with exit code 0 bound to the worktree's exact HEAD, and a second declared check that exits 1 is recorded `failed` with its output, never as verified (screenshots `50-` … `54-`). Evidence also: `workspace-policy.test.ts` (22), `workspace-processes.test.ts` (22), `workspace-runtime.test.ts` (12), `workspace-agent.test.ts` (17 on disk now, 5 at that run), control-plane `workspace.test.ts` (19). **The agent declares through the door (D-156, 2026-08-18, Tested)**: hit in the wild — an agent asked to declare a run command guessed `[run]` for an undocumented format and wrote an invalid file. The `novus` endpoint gains `declare_run_command`: schema-validated arguments, upserted by name, written by the same serializer a person's confirm uses, approval as the confirmation, an unreadable existing file refused in words rather than replaced — and the config error itself now teaches the `[[run]]`/`[[verify]]` shape for hand-editors (`artifact-mcp.test.ts` 10, `workspace-config.test.ts` 12, including the exact wild mistake). Not yet exercised by a live turn |
| Interactive terminal | Partial — **Proven in the app, and tested against real PTYs.** A real `forkpty` in the Electron main process, started in the mission worktree and refusing the user's own checkout by name, under the terminal environment D-041 defines and with no harness credential in it. Input, resize, multiple tabs named after the repository and numbered from one (D-049), close, bounded scrollback, and process-tree termination on close and on quit — including a job that left the shell's own group, which a group signal alone misses. It is unreachable from anywhere but this machine: there is no shell kind in the runner protocol and no control-plane route, so the restriction is structural rather than a check. Rendering is `@xterm/xterm` (D-046): the custom renderer it replaces stripped escape sequences and, through a regex that read `\b` as a word boundary rather than a backspace, deleted the last character of every word — it could not show colour, place a cursor, clear a screen, or run `vim`, `less`, or a pager, and no screenshot of it had ever been looked at. **Proven in the app** by `e2e/runtime.spec.ts` (2026-08-03, 13/13 in ~41s): a real window opens the dock straight into a live shell that already has the keyboard — typed into without clicking first — with no view switch and no rename control on screen (D-049), and `pwd` shows the mission worktree and not the folder the user added; a `printf` with an SGR sequence is *painted* in colour rather than stripped, and `clear` genuinely clears; Ctrl-C interrupts a `sleep` and the prompt returns; tabs are named from the repository and numbered from one — `{repository} 1`, `{repository} 2`, asserted against the fixture's own repository name — and `+` selects and focuses the one it made; a second tab is closed, and switching back to the first tab shows that session still on screen rather than a blank pane — an emulator builds its DOM once, so the pane that was detached to make room for the second one came back empty; a session keeps running while the dock is hidden and its scrollback returns when it is shown; a second desktop that does not host the workspace finds the control disabled with the reason in words *and* is refused by name when its renderer asks the main process directly; a relaunch shows no sessions at all rather than a dead tab. Evidence also: `workspace-terminal.test.ts` (17 tests, real shells, including the derived name and its numbering) and `workspace-runtime.test.ts` (the repository label, from `owner/name` and from a path). Screenshots `45-` … `49-`, `55-`. **Not proven**: no Windows or Linux run, and no packaged build |
| Workspace files in the room | Partial — **Proven in the app** (`e2e/runtime.spec.ts`, 2026-08-03). The evidence panel's first section lists the worktree's own files, folders disclosing in place, `.git` and dependency directories never listed, each row badged with its extension. Selecting one opens it over the room's canvas with the trace returning untouched on close; markdown gets Preview and Edit, and an edit saves into the worktree. Reading and writing are local IPC with no control-plane route, every path resolved through `realpath` against the worktree and refused if it leaves — asserted by asking the bridge directly for `../../../../etc/passwd` and three other escapes, all refused, and by `workspace-tree.test.ts` (12 tests) against real symlinks pointing at real files outside. Preview renders to React elements and never builds markup from file content, so repository text cannot reach the window that holds the bridge (D-048); a source file renders with line numbers and single-lexer syntax highlighting emitted as tokens for React rather than as markup, for the same reason — **built, and not yet asserted by any test**. **New in this slice**: a file that looks like it holds a credential is not listed, not readable, and not writable, by the one policy checkpoints already used, enforced in the main process at the single chokepoint every route passes through, so a renderer asking for the path directly is refused rather than merely not offered it; `.env.example` and its spellings stay visible; `.git` is refused as a *file*, which in a linked worktree holds the absolute path of the repository; and a binary above the readable size is refused rather than truncated, which is what happened when the text check sat behind the size check (D-052). Evidence: `workspace-tree.test.ts` (18), `secret-policy.test.ts` (9). **2026-08-17**: the Preview/Edit mode switch (and the diff's Unified/Split switch in the pull request view) referenced a `dock-view` class that no stylesheet defines, so both rendered as native OS buttons; they now use the existing `segment-tab` idiom — verified against the running app by screenshot (`e2e/evidence/159-file-modes-segment-tab-zoom.png`). **The 2026-08-17 markdown audit (before: `161`–`166`) is fixed (D-145, 2026-08-18)**: a document's `#` renders at the title scale (live: `171`), nested lists nest and tables and task items render as themselves (`172`), an ordered list keeps its source's start (`173`), a titled link renders as a link (`174`), an indented heading is a heading (`175`), and `.md-code`/`.md-pre` sizes come from tokens — verified by rendering the audit fixtures through the real component and stylesheet, and the heading scale in the running app. **Not built**: creating or deleting files, and editing anything but markdown |
| Local process output | Partial — **Proven in the app.** Setup, run, and verification output is bounded, redacted, and still there after the process ended, with its exit code or its named termination reason, on the machine that produced it and nowhere else (D-045). It never travels to the control plane: what a remote participant reads is the bounded result attached to a check's evidence. It is read in the **evidence panel's Output section**, with a three-way switch for setup, the app, and checks, shown only where the workspace actually is (D-050) — the dock is the terminal alone (D-049), and the open question that left behind is closed. Evidence: `e2e/runtime.spec.ts` runs setup through the interface, reads the ended process back through the bridge (`exited`, exit code 0), and then reads it again *on screen* in the panel, where it says `exited 0` (screenshots `52-`, `58-`). Evidence also: `workspace-policy.test.ts`, `workspace-processes.test.ts` |
| Preview surface — the running app inside Novus | **Proven in the app** (D-098; `e2e/preview.spec.ts`, 2026-08-09, 3/3 in ~16s, run twice). A real project whose run command is a real loopback HTTP server on the allocated port, with readiness declared as a port probe: started from `Run ▾` in a real window, *starting* until the probe answers and `ready` only then; **Open preview opens the app inside Novus** — a closable, lane-captured Preview tab whose reserved rectangle the main process's own `WebContentsView` fills. Interaction is real and asserted from inside the embedded page: a click writes `CLICKED`, typing mirrors `TYPED:hello`, Reload reloads without touching the process (same `processId` before and after), a same-origin link navigates, and a link to `https://example.com` is refused in the main process with the URL unchanged — `window.open` to a foreign origin opens nothing, and `typeof window.novus` inside the embedded page is `undefined`. The bridge asked directly — the way a compromised renderer would ask — refuses a foreign host, a credentialed authority (`http://localhost@evil.example`), `localtest.me`, `file:`, `0.0.0.0`, and a loopback address nothing reported, each in words; a stale address whose process ended is refused too (`preview-policy.test.ts`, 11). **Stop is the Run surface's verb and the preview says so plainly**: the rectangle becomes "The app was stopped." with *Run app again* as the one action, closing the tab never stops the process (asserted live), reopening reuses the same process and workstream (`processId` and `workstreamId` asserted), and after a rerun the surface offers reopening on the fresh process rather than pretending the old page is served. The head's state word is the process's own runtime vocabulary and a loaded page never promotes *starting* (`preview-presentation.test.ts`, 8). The Output section's App view now carries the run's preview address on its summary line, from what the `ProcessLog` already recorded. Screenshots `103-` … `107-` at 1440/1000/760, reviewed. **Honest limits**: a main-window capture cannot composite a native view, so the running app's pixels are the embedded page's own capture (`103b-`) beside the chrome capture (`103-`) — and no screenshot here is evidence of application correctness; one embedded view exists at a time; the view yields to any DOM that would overlay its rectangle (dialogs, the narrow-width dock). The visual-evidence slice this row once deferred exists since D-122/D-123 (2026-08-15): the preview's pixels become durable, provenance-bound artifacts — the four rows below carry the evidence. Since that slice the view also renders **unthrottled while hidden** (`backgroundThrottling: false`), so a page off the canvas keeps living and a capture photographs its live render. **Loopback spellings are one machine (D-157, 2026-08-18, Tested)**: hit in the wild — a server bound to `127.0.0.1` reported that spelling, the chip said `localhost`, and Open in browser refused a page the preview was rendering. `sameLoopbackAddress` now matches `localhost`/`127.0.0.1`/`[::1]` as one identity (scheme and port still binding) across the open, admission, and navigation gates, deliberately reversing the "different spelling is a different origin" test (`workspace-policy.test.ts` 25, `preview-policy.test.ts` 13 — the wild case verbatim). **A native rectangle needs a living keeper (D-158, 2026-08-18)**: owner-hit — the view painted over the opened inspector at stale width, and over the transcript after a renderer reload. The renderer keeper is now stateless (the whole truth every 300ms tick: hide, or these exact bounds — the wedged `hidden` flag removed), and the main process detaches a view whose placements go silent for 1.5s or whose host renderer starts loading. Verified by reproducing both broken states in the running app before the fix; the heartbeat lives in the Electron-scoped module unit tests deliberately cannot import, so its automated proof waits on the e2e preview suite's next regeneration — honest gap. **The ghost's true cause was zoom (D-160, same day)**: the owner's window was page-zoomed, the keeper measures page pixels, `setBounds` takes device-independent ones — equal only at zoom 1, which is why nothing automated ever saw it. `applyBounds` now converts by the live `getZoomFactor()`; diagnosed with a temporary on-screen keeper badge over the owner's own window, both diagnostics removed with the fix; a zoomed-window e2e spec is the named follow-up. **Overlays now freeze the frame rather than blanking the page (D-160 follow-up, same day)**: while any menu, dialog, or listbox intersects the reserved rectangle, the keeper swaps in a snapshot of the view's own pixels (a presentation-only `preview.snapshot` IPC — never evidence) and hides the native view; the live view returns when clear, nothing moves or reflows. Owner-directed after rejecting both hide-outright and shave-the-rectangle on sight; built and typechecked with D-161's restyle present, owner's live confirmation after reload pending |
| Durable visual evidence — artifact foundation and storage boundary | Partial — **Tested** (D-122, 2026-08-14; the capture surfaces are the next slices of this phase and have their own rows). The Artifact is real: durable rows carrying kind, MIME, byte size, SHA-256, generated object key (never on the wire), state (`pending → available \| interrupted \| failed`), generated label, initiator (person or agent), capture source, provenance (live process, validated origin, declared readiness, worktree revision with a dirty flag, matching checkpoint when one exists), producing runner and server-composed environment, and timestamps. D-022's storage boundary is realized as one interface with two adapters: the **local store** — blobs on the control plane's own disk behind expiring HMAC-signed URLs, which genuinely hashes what arrives and refuses a mismatch with nothing left behind — and the **S3 SigV4 adapter**, opt-in via `NOVUS_ARTIFACT_S3_*`, **written and not proven live** (no credentials or bucket exist here; a deterministic run against the local store is never live S3 proof). Store credentials live in the control plane alone; signed URLs are minted per authorized request, expire in minutes, and are never stored; an unconfigured store refuses by name. Completion moves a row to evidence only on the store's own confirmation of length and digest; viewing and attachment refuse anything not `available`/`interrupted`; `artifact.capture` (workspace.command's tier, lease-granted) and `artifact.attach` (review.approve's tier, role-only) are server-enforced; attachments are append-only relationship rows for checks and pull requests, decisions freeze chosen artifact ids at record time, and the receipt snapshot freezes the whole evidence set with reopening reconstructing it exactly. Evidence: `artifacts.test.ts` (20, real PostgreSQL and real HTTP against the local store — the happy path with provenance served back; kind/MIME/size refusals; the capture tier including a Contributor allowed only once the baton transfers through the real request-offer-accept flow; no-runner and foreign-session refusals; wrong bytes refused by the store and completion finding nothing arrived, then the same grant completing with the right bytes; short-length refusal; tampered, expired, and bare-guess grants all 403; failed uploads terminal with their reason and never viewable; duplicate completion landing once with exactly one event; the sweep failing an abandoned pending upload by name; viewing minting grants that serve the exact bytes, a viewer allowed, a stranger told nothing exists; attach/detach both ways with the check's outcome untouched; cross-mission and pending-artifact attachment refusals; PR attachment preserving exact ids; decisions refusing to cite non-evidence; the receipt freezing and reconstructing the set with a closed mission refusing new captures; the agent path under the runner credential attributed to the execution's own conversation with foreign/ended executions and cross-completion refused; and the unconfigured store's named 503). Regressions re-run green solo: `close.test.ts` (7), `pulls.test.ts` (10), `reliability.test.ts` (19), `approaches.test.ts` (18), contracts (39). **Not yet real**: no interface captures anything — screenshots, recordings, and every evidence surface are the following slices |
| Preview screenshot evidence | **Proven in the app** (D-123, 2026-08-15; `e2e/artifacts.spec.ts`, 3/3 in ~19s in a real window). A person runs the real fixture app, opens its preview, and **Capture screenshot** on the preview head — gated on `artifact.capture`, under the standing pixels warning — produces a durable artifact whose provenance is the machine's own facts: the live process and its validated origin, declared readiness in words, the worktree's HEAD read from git at the capture moment with dirtiness stated, the capturing person, the reporting runner, byte size and SHA-256. The Evidence section lists it as a restrained thumbnail row whose bytes genuinely render through the `novus-artifact:` protocol — the main process authorizes each request and spends a fresh expiring grant; no signed URL or object key ever reaches the renderer (asserted: the wire carries no object key, and the thumbnail's `naturalWidth` proves real pixels arrived) — and selecting it opens the artifact on the canvas at reading size with the provenance ledger and the honest claim, which the spec asserts verbatim. **An agent requests a capture the governed way (D-123)**: every turn's strict MCP config carries Novus's own `novus` endpoint — a loopback HTTP MCP server inside the Electron main process, per-turn bearer token, one tool, with the name reserved so no project declaration can impersonate it (`mcp.test.ts`, 9) — whose tool call rides the router as every MCP tool does (a person's question short of `dont_ask`); an *allow* mints a one-shot grant (`execution.test.ts`: person allows and policy allows reported, denies never), and the endpoint captures only when a live turn's token **and** an unspent grant stand together (`artifact-mcp.test.ts`, 7 — no-grant refusal, single spend, no cross-turn spending, released tokens dead). Proven in the window: the scripted harness asks as `mcp__novus__capture_screenshot`, the card is approved once, the endpoint performs the real capture-begin-upload-complete chain under the runner credential, the turn's reply carries the honest sentence, and the artifact lands attributed to the requesting execution and its own conversation with `createdByLogin` null. The refusal matrix is pinned in plain Node (`artifact-policy.test.ts`, 7 — no preview, foreign lane, loading, unreachable, crashed, stopped, dead process each in words) and the stopped-app case is proven in the window: after Stop, a capture is refused as stale and nothing pending or failed appears as evidence. The artifact's words are pinned pure (`artifact-presentation.test.ts`, 7 — state words and tones, evidence-or-not, honest absence, the claim never claiming correctness). Capture requires the preview **open with a loaded page**, visible or not — the view renders unthrottled while hidden, so what is photographed is the live page (D-123's re-cut). Screenshots `130-` … `132-`, with `130-` and `131-` reviewed by eye at 1440. **Honest limits**: no recording yet (the next slice); attachments to checks, decisions, receipts, and PRs are Slice D; the agent path is proven with the scripted harness through the production router and endpoint — no opt-in live Claude turn has invoked the real tool yet; and light-theme captures are not in the set |
| Preview recording evidence | **Proven in the app** (D-123, 2026-08-15; `e2e/artifacts.spec.ts`, 6/6 in ~27s in a real window). Not a screen recorder: the one recordable surface is the validated embedded preview, its frames handed to a hidden Novus-owned recorder page through the display-media handler the main process installs — the page can name no source and reach no pixels it was not handed — and encoded by Chromium's own recorder to WebM (VP9, VP8 fallback; D-123 records the compatibility choice). The main process owns the temp file (0700 dir, 0600 files, removed on every terminal outcome and swept at the next start), the incremental digest, the stated bounds (five minutes, and a size stop under the store ceiling), and the upload under D-122's grants; recorder ipc is accepted only from the capture session, so the app renderer cannot forge a chunk. **Proven in the window**: Start recording on the preview head begins a real recording whose elapsed time ticks beside the word; the state survives a canvas switch as `· recording` on the preview tab; Stop preserves it — an available `video/webm` artifact with real duration, a poster frame, and person attribution — and it **plays back inside Novus** in the artifact view's own player, `videoWidth > 0` asserted so the bytes through the protocol are a real decodable video; the temp area is empty after every outcome, asserted from the machine itself. **Cancel leaves nothing anywhere** — no artifact, no pending row, no temp file. **A recording the app dies under ends honestly**: stopping the app mid-recording settles it as `interrupted` with the reason in words, never as an ordinary success, and afterwards both capture and recording of the stale preview are refused in words. Screenshots `133-` (the tab's recording word), `134-` (playback, reviewed by eye). **Honest limits**: MediaRecorder's WebM carries no duration header — the recorded `durationMs` is the artifact's own metadata and the player's scrubber learns length only as it plays (a known Chromium shape, accepted in D-123); recordings are person-initiated only, by design; no interaction log is stored, by design; and the recorder needs the machine hosting the workspace, like every capture |
| Evidence attachments — checks, decisions, receipts, pull requests | **Proven in the app** (D-122, 2026-08-15). An immutable artifact becomes evidence *for* something, and every surface shows it in the one thumbnail anatomy. **Beside a check**: attached from the artifact's own view (an Attach-to Select of exactly what exists, gated on `artifact.attach`), rendered subordinate under the ledger row's own facts, detached from the same view — with the check's outcome untouched either way, because an attachment is support and never a verdict (`e2e/artifacts.spec.ts`, in a real window: a declared check run from the ledger, the screenshot attached beside it, the outcome still `passed`, detach emptying the row; screenshot `135-`, reviewed). **On a decision**: the record dialog offers the lane's completed evidence as optional checkbox rows and the checked ids freeze with the rationale — proven in the window by `e2e/decision.spec.ts` (a seeded artifact checked in the dialog, `decisions[].artifactIds` carrying exactly it, and the decision receipt showing *Visual evidence cited* with the row; the citing dialog is in the regenerated `85-`). **On the tracked pull request**: the placeholder is gone — the page's Checks segment shows the attached evidence with the boundary in words (inside Novus, participants only, no copy and no public link to GitHub), the exact ids preserved on the record; proven in the window by `e2e/pr.spec.ts` against the fake host (the artifact seeded over the real API because that mission runs no preview — the capture path is `artifacts.spec`'s own proof — attached to the open draft, the row on the page by exact id; screenshot `137-`). **Into the receipt**: `e2e/artifacts.spec.ts`'s finale decides citing two artifacts, completes the mission, **relaunches the whole application**, and the terminal room's receipt reconstructs the frozen set — all four completed artifacts with capture time, revision, `interrupted` where true, and where each was evidence at close — with thumbnails loading through freshly minted grants (`naturalWidth > 0` after the relaunch), the citation surviving as *Cited 2 visual artifacts*; and a second authenticated identity with no mission access is told nothing exists — mission and artifact alike, 404 (screenshot `136-`, reviewed; it caught a broken-image glyph on the poster-less interrupted recording, fixed by freezing `hasThumbnail` into the snapshot). Foundation rules Tested at the control plane in `artifacts.test.ts` (Slice A's row): attachment tiers, cross-mission refusal, non-evidence refusals, decision citation validation, receipt freezing and reconstruction. **Honest limits**: attaching targets checks and the open pull request — checkpoints are referenced through the revision an artifact already carries rather than a fourth attachment kind; the decision picker needs the Compare surface, which a single-lane mission still reaches only through the API (the pre-existing D-099 shape); and no external GitHub presentation exists deliberately — the authorization model cannot safely provide a public URL, and the page says so |
| Running a check again | **Proven in the app** (D-057). A ledger row that failed, errored, or proved a revision the worktree has moved past carries **Run again**; one that passed and still proves the current revision does not, because nothing is being asked. It is the same declared command by name, authorized by the server against `workspace.command` like any other, and run against the snapshot pinned when it was authorized (D-043). Every run is a new record — the failure that prompted it keeps its own attribution, revision, exit code, and output, and both stay on screen, because a ledger that overwrote a failure could not answer whether it ever failed. One run at a time per check. **Proven in the app** (`e2e/runtime.spec.ts`, 2026-08-04): a failed check is run again from its row, a second record appears with a distinct id, its own requester and its own revision, and the rendered ledger shows both (screenshot `62-`) |
| Filing a mission away | **Proven in the app** (D-063). A mission is archived, never deleted: one column set and cleared, everything it is left exactly where it was, still readable by anyone who could read it before, and restorable. Filed away means put away: nothing lists an archived mission in the rail, which carries only a count at its foot that opens a dialog when something is there at all — a permanent Archived section with Restore beside every row would make putting something away and getting it back equally easy. **There is no delete verb at all** — not hidden, not admin-only, absent, and a test asks for one and requires a 404, because "we chose not to expose it" and "it does not exist" are different guarantees. An attempt that was never given a command cannot start and is not work: it is ended as *interrupted* on the way out rather than blocking the filing, because a mission must not be wedged out of the product by a row nobody can act on. Otherwise refused while an execution is live or an approval is waiting, as two separate refusals: the waiting question is checked first, because *answer it* is the useful instruction and *stop the execution* is not. No repository branch is deleted and no worktree is removed — the mission branch is the record and lives in the user's own repository. The list filters on the column and the Archived view is that query inverted, so the two cannot disagree; reading a mission does not consult it, so filed away is not hidden. The runner discovers work through the same default list, so an archived mission stops being enrolled, cloned and worktree'd — safe only because archival is refused while an execution is live. `mission.archive` is Mission Admin's and is **not** lease-granted; `mission.close` is deliberately untouched and still unimplemented, because ending a mission's work is a different act. **Proven in the app** (`e2e/approval.spec.ts`, 2026-08-04): archiving is refused in words while the harness waits, then the rail's own control files it away, its tab closes, the second client sees it leave the list and appear in Archived, every direction, event and approval decision is still there, Restore brings it back, and a relaunch keeps it filed (screenshot `79-`). Restore now disables the other rows and labels the active row *Restoring…* until the refreshed list returns, preventing duplicate clicks — added after that run and **not in the evidence set**. Evidence also: `archive.test.ts` (10), including the refusals, the role that may not, the non-participant told it does not exist, idempotency, and the absent delete route |
| Approval routing | **Proven live** (D-062). Claude Code runs under `--input-format stream-json --permission-prompt-tool stdio` with stdin held open: it stops before an act it may not take, writes a `control_request`, and blocks until Novus answers on the same channel. The permission policy is pinned by Novus rather than inherited — `--setting-sources ""` and `--permission-mode manual` — because a `.claude/settings.json` in the worktree is a file *the agent can write*, and reading it to decide what the agent may do is not a boundary; measured, the identical Write is performed silently without those flags and routed with them. One approval answers one act: no allowlist, no "always allow", and Claude's own `permission_suggestions` are parsed and discarded. `approval.respond` is a lease capability in no role list, settled by compare-and-swap so a second answer is refused rather than delivered twice. No raw tool input is stored — the durable summary is composed from named fields, path-masked and value-redacted (D-052) and bounded twice, so a `Write`'s file body never leaves the machine. A pending request is a safe boundary, declared by the runner, so control can transfer while the harness waits. Stop prefers the protocol interrupt, keeping the session resumable, with the process-group kill as a bounded fallback; which one ended the turn is recorded. **Proven live** against real `claude 2.1.221` (2026-08-04): a real Write request reached the room, the card rendered it, *Approve once* was clicked **in the window**, the harness was unblocked, `HELLO.md` was written and committed, and the next turn resumed the same session (screenshot `78-`). **Proven in the app**, two clients (`e2e/approval.spec.ts`): the non-controller sees the question, is told who holds the baton, and is refused by the server when their client asks anyway; control transfers while the harness is blocked; the new holder answers and is recorded as having answered; a denial leaves the file uncreated and the execution *completed*, not failed (screenshots `74-` … `77-`). **The project's own instructions are handed back** (D-064): pinning the settings had stopped the worktree's `CLAUDE.md` loading, and Novus now reads that file and passes it with `--append-system-prompt-file`, resolved through `realpath` into the worktree, non-empty, regular, under 100 KB, and re-read every attempt. Safe while settings stay pinned because the two files differ in kind — a settings file grants authority and hooks that run before the permission check, `CLAUDE.md` grants nothing and every tool call still reaches the router. **Measured against the real CLI** and recorded in D-064: a codeword in `CLAUDE.md` is unknown to the model with settings pinned and known once the file is passed. Evidence also: `approvals.test.ts` (16), `execution-approval.test.ts` (11, against a real process with a stub `claude` on PATH asserting the exact argv — including the instruction file, its absence, and a path that leaves the worktree), `live-approval.test.ts` (3, opt-in against the real binary, including a committed `.claude/settings.json` saying `acceptEdits` that cannot switch the routing off), `harness-stream.test.ts`, `workspace-agent.test.ts`, `contracts.test.ts`. **Proven live again 2026-08-05** against `claude 2.1.223`, through Novus's own turn path rather than a hand-rolled CLI call: the codeword in the worktree's `CLAUDE.md` comes back and the turn opens no file at all, so the system prompt is the only place it can have come from (`live-instructions.test.ts`, opt-in). **Carried since 2026-08-12, deliberately** (D-118): a project's `.claude/skills` reach the agent as a Novus-composed skills-only directory a person enabled by digest — its own row below — with hooks and MCP servers still structurally outside (D-072's boundary, unchanged). `e2e/approval.spec.ts` re-run 2026-08-08, now 4/4 in ~31s with D-095's read-alongside story added to it, regenerating its screenshot set; `75-` and `101-` reviewed by eye. **Since D-115 (2026-08-11) what still asks is the lane's permission profile's residue** — its own row below; the wire, the pinned flags, and the one-act answer are unchanged, and under the default profile this row's story is exactly what happens |
| Permission profiles | **Proven in the app** (D-115, 2026-08-11). A lane carries a standing answer policy for the harness's permission questions — **Plan, Ask every time (the default), Accept edits, Auto, Don't ask** — applied by Novus at the runner's own router, never by handing the CLI a mode: every profile runs under D-062's pinned flags with the stdio channel open, and the profile only changes who answers. The ladder holds the existing claims in order: a read turn is denied whatever the lane trusts (D-095), plan refuses even an in-scope write, a scope's verdict on the edit tools stands whatever the profile says (D-097), then the profile's allows — `accept_edits` the path-naming edit tools, `auto` everything but a shell command (D-097's reason: it declares its targets nowhere), `dont_ask` everything — then the room. **Every profile-decided answer is recorded** as `approval.policy` with the same bounded, redacted summary a card would carry; the turn's machinery line names the profile it ran under; `executions.permission_profile` is pinned at dispatch, onto `start_execution` *and* `apply_direction`, so a change speaks from the next turn. Setting it is `policy.set` — Mission Admin and Operator, never the baton (proven: a Contributor *holding the lease* is still refused) — with `dont_ask` Mission Admin's alone, refused until the warning sentence is acknowledged, and the acknowledgement recorded verbatim on `policy.changed` (the D-100 pattern). **There is no bypass**: not a schema value, not a database word, not a flag — the contract test asserts no profile matches /bypass/ — because bypassPermissions turns off the very channel the record, the scopes, and the read containment ride; the picker says so in place. The composer's foot wears the profile as a chip beside model and effort, warn-toned only for Don't ask, with a menu explaining every mode in a sentence and disabling what the viewer's role cannot set; the lane overview's facts line carries the word for people with no composer; a fork starts at `manual`, never inheriting. The CLI's six modes were probed against installed `claude 2.1.228 --help` (2026-08-11): all six exist; plan's flag is passed for plan turns with the router as the guarantee, the other five never become flags. Evidence: control-plane `policy.test.ts` (9, real PostgreSQL — the tiers, the baton-holding contributor refused, the acknowledgement, bypass unparseable, pinning across a mid-turn change, foreign lane 404), `contracts.test.ts` (33, 8 new), desktop `execution.test.ts` (24, 8 new — each profile's answer at the real router with the fake harness through the production parser, containment and ownership outranking trust), `execution-approval.test.ts` (13, 1 new — the exact argv through a real spawned stub: plan's flag, manual for the rest, no mode words anywhere), `trace-presentation.test.ts` (11, 4 new — machinery word, apparatus rows, the room's sentence), and **`e2e/approval.spec.ts` 5/5 in a real window (~47s, 2026-08-11; re-run 5/5 after the D-116 re-cut)**: the chip read and changed, Accept edits writing the fake turn's file with zero cards and the grant in the record, a Contributor's chip disabled, Don't ask behind its warning with the acknowledged sentence in the durable event, and plan refusing with nothing written. **The picker was re-cut twice the same day on the owner's sight (D-116, then D-117)**: the five-row menu became a slider, and the slider became Codex's shape at the owner's direction — the question as the head, one glyph-led row per profile with a fragment of meaning, the current row checked, and the Don't ask row wearing the warn tone whole — with semantics, testids, tiers and the warning dialog untouched through both cuts; screenshots `122-permission-profiles-menu.png` and `123-dont-ask-confirm.png` were regenerated over a **working room** (the turn's transcript and its `· Accept edits` machinery line behind the control) and reviewed at 1440. That regeneration caught a capture hazard now recorded in D-116: a backgrounded Electron window's polling is throttled, so the spec brings the window forward and waits for the room to catch up before photographing — the runner, in the main process, was never throttled and the durable state was always right. **Plan is proven live** (2026-08-11, `live-plan.test.ts`, opt-in `NOVUS_LIVE_PLAN=1`, 1/1 in ~13s against real `claude 2.1.228` on the cheapest allowlisted model): a plan-profile turn asked to create a file created nothing — the worktree held exactly the fixture afterwards, no card was published, the checkpoint was clean, and the turn completed with a 541-character reply in which the model itself said it was in plan mode, could not create the file, and described what it would do instead. Observed and worth knowing: the real CLI's plan mode denied the write **internally** — zero `can_use_tool` requests reached the router — so the router's plan deny-wall, proven deterministically in `execution.test.ts`, is the backstop and was not needed on this run. **Honest limits**: the other opt-in live suites were not re-run. Org-level autonomy ceilings stay the extension point PRODUCT.md has always listed. A read-alongside turn's row keeps the column default (`manual`) because containment, not the profile, governs it. And `.btn-danger`, named by DESIGN.md's primitives since D-076 and referenced by the D-100 confirm dialogs, was never in the stylesheet until this slice's warning dialog needed it — those confirms' Merge/Close/Delete words now render in the danger tone, and their screenshots (`108-` … `113-`) predate that |
| Project skills | **Proven in the app** (D-118, 2026-08-12). A project's own `.claude/skills` reach the agent only after a person enabled them, and only as the exact bytes that person reviewed. The runner publishes a bounded manifest beside the declared commands — directory name, frontmatter description, size, SHA-256 — on the same `workspace.declared` event (D-043's pattern); the inspector's Overview lists it as quiet rows with the lane's standing answer in words (`enabled`, `changed since enabled` and `no longer in the project` in warn, nothing while off) and one text action gated on the new `skills.set` capability — Mission Admin's and Operator's, never the baton's, with a Contributor holding the lease still refused (the D-115 tier test, re-run for this verb). Enabling pins the digest: the route refuses an entry the manifest does not carry at exactly that digest ("changed since it was reviewed"), the change is one `skills.changed` event with both sets verbatim, and both turn commands carry the enabled list pinned at dispatch so a change speaks from the next turn. At spawn the runner composes a **skills-only plugin directory** in its own staging — plugin.json authored by Novus plus one SKILL.md per skill, realpath-contained, bounded, digest-verified with the verified bytes being the written bytes — passes `--plugin-dir`, reports carried and dropped (with reasons) on `execution.running`, and removes the staging with the turn; the trace renders both as apparatus, drops in the warn tone. Hooks, `.mcp.json`, commands, and agents are never written into the composed directory — asserted by enumerating it — so D-072's refusal is now structure. **Probed live against installed `claude 2.1.229` (2026-08-12)**: the composed directory's skill loads under the full pinned argv — `novus-project-skills:zephyr-codes` in the session's own init line and named by the model in its reply — with `mcp_servers` empty. Evidence: `contracts.test.ts` (38, 5 new — the name that can never be a path, digest pinning, bounded manifests, the defaulted event fields), control-plane `skills.test.ts` (8, real PostgreSQL — publish to the wire, the tiers with the baton-holding contributor refused, unknown-skill and stale-digest refusals in words, the recorded event, pinning across a mid-turn change, foreign lane 404), desktop `skills.test.ts` (7, real filesystem — discovery with symlink-escape refusal, composition writing exactly the manifest and approved bytes and nothing else, every drop reason), `execution-approval.test.ts` (15, 2 new — the composed directory on the real spawned argv holding exactly the approved bytes and removed with the turn; a stale digest passing no directory at all with the drop on the record), `trace-presentation.test.ts` (13, 2 new), `room-presentation.test.ts` (38, 2 new — the row states, the submit-set arithmetic), and **`e2e/skills.spec.ts` (1/1 in a real window, ~11s)**: the manifest published by the real runner from the real worktree, reviewed and enabled from the inspector, the `skills.changed` event durable with the reviewed digest, and the next turn's trace stating *Project skills carried: zephyr-codes* — screenshots `124-`, `125-`, reviewed at 1440. Regressions re-run green: control-plane `policy`/`workspace`/`sessions` (44), desktop `execution`/`workspace-agent`/`runner-agent`/`execution-endings` (56), `e2e/runtime.spec.ts` (16/16), `e2e/sessions.spec.ts` (1/1) — the two specs regenerating their screenshot sets in passing, not re-reviewed individually (the standing convention). **Honest limits**: only the SKILL.md is carried — a skill's supporting files are not composed yet, named in D-118; the operator's own user-level skills still load, a pre-existing fact of the CLI unchanged by this slice; a read-alongside turn carries no skills; and no opt-in live turn has exercised a skill through Novus's own turn path — the live proof is the direct CLI probe above. The MCP half, surfaced by D-118 as an unmade product decision, was directed by the owner the same day and built as D-119 — its own row below |
| Project MCP servers | **Proven in the app** (D-119, 2026-08-12, owner-directed — D-118 had surfaced it as an unmade product decision). A project's `.mcp.json` is governed the way its skills are, one tier up. The runner publishes each **reviewable** server beside the commands and skills — name, transport, command/args/env or url, SHA-256 of the canonical declaration — and what cannot be reviewed is never listed: a plaintext remote, a credentialed url, a path-shaped name are refused by the contracts schema itself, so the wire rejects a runner report carrying one (**the network restriction is admission**: https away from loopback, nothing else claimed). `mcp.set` is **Mission Admin's alone** — an Operator, who may enable skills, is refused 403 for servers, and the wire carries the tier. Enabling pins the digest; `mcp.changed` records both sets verbatim; both turn commands carry the pinned list; at spawn the runner re-derives, digest-checks, and writes a config **Novus authors**, passed with `--mcp-config` + `--strict-mcp-config` so the composed file is the CLI's entire MCP world — probed live against installed `claude 2.1.229`: exactly the named server loads (`connected`, its tool listed as `mcp__docs__echo`), nothing else. **Per-server permissions are two-tier**: activation is the Admin's standing grant, and at the router `mcp__*` tools are treated as Bash is (D-097's reason — the request declares no effects) — `auto` never answers one, only `dont_ask` does, on the record. A server process runs in the harness environment (D-041): no project secrets, no runner credential — stated, not new. Evidence: `contracts.test.ts` (39, 1 new — transport coupling, url refusals, defaulted event fields), control-plane `skills.test.ts` (12, 4 new — the Operator refused where skills allowed them, stale digest and unknown server in words, the wire refusing a plaintext remote wholesale, pinning across a mid-turn change), desktop `mcp.test.ts` (6 — discovery refusing the unreviewable, the composed config holding exactly the approved entries rebuilt from verified fields, every drop reason), `execution-approval.test.ts` (16, 1 new — `--mcp-config` + `--strict-mcp-config` on the real spawned argv with the exact file content, gone with the turn; a stale digest passing neither flag), `execution.test.ts` (26, 1 new — `auto` handing an `mcp__` tool to the room while `dont_ask` answers it on the record), `trace-presentation.test.ts` (14), `room-presentation.test.ts` (39), and **`e2e/skills.spec.ts` now 2/2 in a real window (~20s)**: the server published for review ("runs node mcp/docs.js"), enabled by the Admin, `mcp.changed` durable with the reviewed digest, and the next turn's trace stating *MCP servers carried: docs* — screenshot `126-`, reviewed. **Honest limits**: no live turn has exercised a real MCP tool through Novus's turn path (the live proof is the direct CLI probe; a permission-routed live MCP call is the natural next opt-in suite); a stdio server's own network egress is not filtered and the review says what runs instead; there is no per-server standing allow — deliberately, D-119's deferred dial — and no org-level MCP ceiling yet (the D-115 extension point) |
| The Home board | **Proven in the app** (D-120, 2026-08-13, owner-directed with the reference on sight — a Conductor-style board, taken as anatomy with our semantics, the D-100 move). Home renders every active mission in four **computed** columns — Needs you \| Running \| Waiting \| Decided, attention first — with no drag, no rank, and no card verb beyond the click. The list projection now reaches the decided states the room always had (`pull_request_open`, then `decision_recorded`, outranked by attention and running), and the list carries the board's facts as cheap indexed aggregates: `lastActivityAt`, `working` (the running mirror of D-093's attention, naming the running lane and chat), `controllerLogin` for single-lane missions only, churn (distinct paths + summed per-turn `+`/`−`, stated as churn), and a checks tally at the lanes' current heads only. **The card's click opens the mission at the thing that is asking** — the blocked conversation, the running chat, or Compare for a decided mission with siblings — which closes the open-at gap D-093 recorded, for the board and for the rail's attention lens alike (`openMissionAt`, composed from the working set's own verbs). **Home is a glance, not a purge**: it deselects rather than wipes, and open tabs survive the visit — a deliberate change of the Home control's old meaning, recorded in D-120. From the owner's brief, deliberately not used and told so: no "Needs decision" column (manufactured urgency — Principle 7), no "Complete" column (the terminal lifecycle is unbuilt; an empty fictional column is prohibited pattern 11), no Start/Resume/Request-review card buttons (starting is words — D-077; resuming is the click; review does not exist), no controller on multi-lane cards, no dragging ever. **Found by this slice's first screenshot and fixed at the source**: a created mission could card twice for up to five seconds — `handleCreated` appended while the poll delivered the same mission — a pre-existing rail flicker made visible; the append now dedupes by id and the spec asserts one card per mission. Evidence: control-plane `board.test.ts` (5, real PostgreSQL — the facts on the wire with absence as null, the running mirror, the current-heads-only tally emptying when the lane moves on, the two-lane baton unstated, decision → open-PR → attention precedence), `home-presentation.test.ts` (5 — column mapping, glance order, the card's colored line, the relative-time grammar), `working-set.test.ts` (22, 3 new — open-at landing, re-aiming without a second tab, remembered-place fallback), regressions `attention.test.ts` (10) and `slice.test.ts` (10) green, and **`e2e/home.spec.ts` (1/1 in a real window, ~15s)**: a blocked and a settled mission grouped correctly with attention words, churn arithmetic, and the baton on the cards, tabs surviving the Home visit, one card per mission asserted, and the Needs-you card's click landing in the blocked conversation with the approval card on screen — screenshots `127-`, `128-`, reviewed at 1440. **Honest limits**: within-column order is recency, not priority; there is no project filter yet; the board is not re-shot in the light theme; and a just-created mission's card can wear its creation-time state for up to one poll before the projection's word arrives — staleness, stated, never invention |
| Terminal mission lifecycle | **Proven in the app** (D-121, 2026-08-13, owner-asked by name). A mission's work now ends: `mission.close` — Mission Admin's alone, never the baton's — completes it or cancels it, and the ending is the one stored word every projection reads. **Completion means what it says**: refused without a standing decision ("Record a decision first, or cancel instead") and refused while a pull request is open, each in the server's own words; both endings borrow archival's refusals verbatim (the waiting approval first, live work in words, a stillborn attempt ended on the way out — D-063's pattern). **The receipt is snapshotted at close**, realizing ARCHITECTURE.md's Receipt row: a deterministic projection — participants, applied directions, every decision verbatim with superseded ones kept, churn, the ledger's final rows each stating whether it still proved a lane's head, a mechanical *remains uncertain* list, the pull request's last word — validated through the contract schema, stored with the event range it covers, the `mission.closed` event recorded first so the range includes the mission's own ending (a real bug the determinism test caught: the first cut's re-projection differed by one sequence number). **A terminal state never resumes**: direction and forking refuse in words, the composer is hidden, the fork control is gone, overlays empty, and an invitation redeemed after the end joins as Viewer whatever it offered (failure mode 13, enforced at redemption and tested). The room becomes the receipt — rendered from the snapshot, never recomputed — with the terminal sentence per DESIGN.md; ending lives in the inspector's Overview (Complete plain, Cancel behind a confirmation carrying the person's words); the board files closed missions under **Complete**, its fifth column. Evidence: `close.test.ts` (7, real PostgreSQL — the tier, both completion gates, archival's refusals, snapshot determinism by deep equality over unchanged rows, the cancellation's words on event and receipt, operating-verb refusals with archival still working after, the late invitation demoted to Viewer), `board.test.ts`/`attention.test.ts`/`archive.test.ts` re-run green (25), `home-presentation.test.ts` (7 — the Complete column and the finished card's words), and **`e2e/home.spec.ts` now 2/2 in a real window (~20s)**: a mission cancelled from the Overview with a typed reason, the room freezing into the receipt with the reason quoted and no composer, the state line reading *Cancelled by spike-user*, and the board filing it under Complete while the blocked mission stays Needs you — screenshots `127-` (all five columns at 1440, the owner's alignment and width notes applied), `129-cancelled-receipt.png`, reviewed. **Honest limits**: receipt export remains unbuilt (the D-022 artifact store is now built and Tested — D-122, its own row — and the receipt freezes artifact references since that slice); the receipt canvas is the snapshot's summary, not the full frozen trace (history stays one click away in the room's record); reopening-as-continuation has no affordance — a new mission is the way forward; and no completed-path e2e drives the decision→close flow in a window (it is Tested at the control plane; the cancel path is the one proven in the app) |
| The window has one top row | Partial — **the shape below is proven in the app up to the 2026-08-05 evidence set, and the changes after it are not** (D-066 … D-068). Mission tabs sit in the window's own row beside its controls — rail toggle at the far left, tabs next, `+` directly after the last one, the room's controls at the right — so the window has one chrome edge instead of a mostly-empty row above a band of tabs. The wordmark is gone. Every tab names its project always, not only when a second project is open, because a label that comes and goes as siblings close makes a tab's meaning depend on its neighbours. The rail is the whole left column with its own strip inset past the window's buttons and its switch at the edge it moves (D-068), and carries **Home** and **Search** fixed above its lists — search finds a mission by name across projects, while files are filtered where the files are. The evidence panel is three sections (All files, Changes, Checks) with the workspace's own two (Overview, Output) in a region at its foot, and it no longer repeats who holds the baton in a header of its own. The top row clears the window's own buttons, and the strip has no plane or edge of its own — a tab is a word on the window and only the one being read takes a surface (D-067). Evidence: the end-to-end assertions rewritten to pin the new behaviour in `e2e/navigation.spec.ts`, `e2e/runtime.spec.ts` and `e2e/slice.spec.ts`, and the screenshot set regenerated with them, **reviewed by eye** at 1440, 1000 and 760 (screenshots `9-`, `62-`, `71-` … `73-`). **Not in that set**: three later changes to the same surfaces — the rail's switch aligned with the room's, search opened as a palette rather than a form, and the workspace region reworked into a foldable, resizable drawer read alongside the diff instead of replacing it. Each was inspected by eye during iteration under D-067's proportional rule; none has a regenerated screenshot or a recorded end-to-end run |
| The room reads as a transcript | **Proven in the app** (D-065). Within a turn the agent's reply is the largest text and the only `--text-1`, the direction that prompted it sits at `--text-2`, and everything about the turn — speaker, model and effort, technical activity, the checkpoint line — recedes to `--type-meta` at `--text-3`. Speech is content, the rest is apparatus, and `--type-speech` is the scale's first new step. The rule that ran down the side of every turn is gone: grouping is the indent of speech under its speaker and the space between blocks. Diff arithmetic carries `--ok` and `--danger` as text, the same pair the diff body uses, so one change reads identically in the trace and the panel — the sign says which way it goes and the colour agrees with it. Both side columns are draggable, keyboard-operable, double-click to default, bounded at both ends, and remember their width on that machine. The Archived dialog is composed on the dialog's own head/body/actions axis, each row one primary value with its context beneath and one action at the end. **Reviewed by eye** at 1440 and 760 across the regenerated evidence set (screenshots `20-`, `62-`, `80-`). **The raw-markdown reply (audited 2026-08-17, `160`) is fixed (D-145, 2026-08-18)**: harness speech renders through the element-only Markdown component at the speech scale — bold, code spans, lists, and headings as themselves — verified live in the running app after reload (`e2e/evidence/170-transcript-rendered-markdown.png`) |
| Open missions as a working set | **Proven in the app** (D-061). A strip above the room — starting at the room's own left edge, with the rail a full-height column beside it rather than underneath it — carries only the missions this person has open, and is not a second list: opening from the rail opens a tab or selects the one that view already has, missions from different projects sit in it together, and a project with nothing open puts nothing in it. **D-085 tried views-as-top-tabs on 2026-08-08 and D-086 reversed it the same morning, both owner-directed on sight**: two dot-marked copies of one mission's name in the window strip read as two missions, so the top strip is missions-once again and the approaches moved one level down — colour-dotted tabs on the room's own strip, beside Compare and the open files, switching the lane in place, with the rail's tree unchanged as the structural map. The lane a tab was reading still survives a relaunch. Both turns are unpinned by specs per the standing no-tests call. Closing a tab closes a tab — no execution stops, nothing is discarded or archived, the harness goes on working and the rail goes on reporting it, which is why the mission list is now re-read on a timer rather than once at mount. Open missions and the selected one survive a relaunch; drafts do not, because a draft is unsent local text with no mission behind it; and a restored mission the server no longer shows this person is dropped from local state quietly, while `offline` explicitly is not treated as gone. **Creation reworked 2026-08-06 (D-077), and the e2e evidence for it is stale**: every entry point now opens one ask-dialog — words create and start the mission, nothing closes it with no draft, no tab, no row — the rail's permanent New mission row is gone, a project row is a pure disclosure that never moves the room, and the project name reads as a heading over its quieter missions. The dialog is the real Composer wearing a dialog (D-078), so model and effort chosen there persist exactly as the room's do; mission rows in the rail are now inset 6px chips under sharp full-bleed headings. `e2e/navigation.spec.ts`'s draft-flow tests were rewritten against the ask-dialog on 2026-08-06 under D-077's entry and the spec passes again (8/8): the + and ⌘T and the strip's + all open the one dialog, Esc leaves nothing behind anywhere, and the words create the mission in a tab named by the goal they derive. Mission tabs are the window's and file tabs are the room's, the latter only while a file is open, each mission keeping its own; the room's way back reads *Mission* rather than the goal a fourth time. **Proven in the app** (`e2e/navigation.spec.ts`, 8 tests, 2026-08-04) at 1440, 1000 and 760 wide — including two missions from one project and two from different projects open together, a background mission that keeps running with its tab closed, a restored mission that no longer exists and one this person may not see, both dropped without harming the shell (screenshots `63-` … `73-`, reviewed). Evidence also: `working-set.test.ts` (16 tests) proving the rules without a window |
| Missions in one project | **Proven in the app** (D-055). A project row in the rail discloses its missions and ends with New mission; `⌘T` is the same act. The room is the selected mission and repeats nothing: the strip across the top is for open files, absent entirely while none is open, so a single mission has no chrome above it and an open file takes its tab beside the room's own. Nothing in the room calls a mission a workstream any more — the word had been on the tab row, its `aria-label`, the `+` tooltip, the draft tab, two empty states and the new-room sentence, and a mission contains exactly one workstream that nothing in the interface can add to. **Proven in the app** (`e2e/runtime.spec.ts`, 2026-08-04): two missions under one project are listed once, in the rail, each selectable with the room following; the centre has no tab strip at all; New mission says Mission and opens a draft that says the first direction creates this mission; `⌘T` does the same; and the room's full text contains no occurrence of *workstream* (screenshot `61-`). Evidence also: `slice.spec.ts`'s rail-disclosure and relaunch assertions, moved from the tab row to the rail. **Overtaken 2026-08-07 and corrected here**: this row's tail claimed "more than one workstream in a mission" was Not built and that lane headers awaited it, while the approaches row below has proven multi-lane in the app since 2026-08-06 and D-080 replaced the lane-header sketch with lane tabs; and "a mission contains exactly one workstream that nothing in the interface can add to" stopped being true the day `approach.create` shipped. The 2026-08-04 evidence for what this row *does* claim — one project's missions listed once, in the rail, with no centre tab strip — still stands |
| Execution providers and cloud workspaces | Not started |
| Runner heartbeat and lease expiry | Partial — a sweep ends an execution whose runner has been silent past a 90-second recovery threshold, as an explicit *interrupted* outcome with an attributed event, and a returning runner cannot resurrect it. A control lease expires after its holder has been silent past its 30-minute time to live, leaving the workstream with no controller and claimable through the existing request path. **A pending approval survives that expiry** (D-073): the question stays pending, the harness process is never touched, the expiry event records that a question was waiting, the state line says *no one holds the baton — claim it to answer*, and whoever claims the lease answers it once with their name on it while the previous holder is refused. Evidence: `reliability.test.ts` (2 tests — the question, the execution and the absent stop command after expiry; then the claim, the answer, the single carried command, and the refusal of the departed holder). Expiry removes future authority only: an execution already authorized keeps running (D-034). **Corrected 2026-08-03**: the previous entry said Implemented on the strength of `reliability.test.ts` calling the sweep directly, and nothing renewed a lease's heartbeat — `touchLease` had no caller — so every lease expired thirty minutes after it was created however present its holder was, and every direction after that queued behind a controller who no longer existed. The heartbeat is now the holder's own read of the mission, which is what the room polls while it is open (D-051). Evidence: `reliability.test.ts`, 11 tests, including a lease backdated past its TTL that survives its holder's read and does not survive a bystander's. A runner's liveness has always been renewed by its polling and is unaffected. Still Partial: neither path has been observed against a machine that actually died in the app |
| Stopping an execution | **Proven in the app** (D-053). A participant with `execution.stop` stops a running harness turn: the command bypasses the workstream's command chain instead of queueing behind the thing it interrupts, reaches the turn while it is still in the active set, and terminates the harness process *group* gracefully and then forcibly. It is matched to the execution it names, so a stale stop re-offered after a relaunch cannot kill whichever turn is running now; it is remembered, so a stop that lands before its turn has spawned prevents the turn rather than being swallowed; and a stopped execution's queued direction is discarded with it rather than starting a second harness process in the worktree. Only the harness is affected — a dev server a participant started outlives it (D-045). The room says *Stopping* while the interrupt is in flight and stops offering a control that has already been pressed. Refused server-side for a role without the capability, with no state change and no command enqueued. A terminal execution accepts no further projection, so a late transcript event cannot go on talking, overwrite the workstream's resume point with the session the turn was killed in, or commit a checkpoint after the stop. **Proven in the app** (`e2e/runtime.spec.ts`, 2026-08-04): a real window shows *Running — Claude Code is working* with Stop, the click lands while the harness is mid-turn, and the trace ends *Stopped* rather than *Turn completed*, with the control gone (screenshots `59-`, `60-`). Evidence also: `workspace-agent.test.ts` (5 stop tests through the agent, two of which fail against the previous code), `runner.test.ts` (the refusal, the late-event guard), `execution.test.ts` (the turn's own kill path). **The crash window is closed** (D-070): a command is recorded as begun *before* the work starts, so a machine killed mid-turn comes back, declines to run it a second time, acknowledges it failed, and reports the execution *interrupted* — where Restart is the human's choice. Evidence: `runner-agent.test.ts` (2 tests, both failing against the previous code — one that no turn starts and the execution is reported interrupted, one that the memory survives a second relaunch) |
| Runner protocol (events, commands, reconnection) | Partial — the host desktop registers as a workstream's runner and receives a credential stored only as a hash, scoped, expiring, and revocable; only that credential may write harness-, runner-, execution-, workspace-, or verification-attributed events, which closes D-033. Commands are durable, ordered, idempotency-keyed, and acknowledged, and a workspace command now carries the immutable snapshot it was authorized against (D-043). The runner keeps its own at-most-once record of what it has begun, written before the work rather than after it, so a machine killed mid-command settles it as interrupted instead of running it again (D-070). Events carry a monotonic per-execution sequence, are de-duplicated server-side, and are buffered in a local outbox with backoff, bounded storage, and a gap marker on overflow. **The outbox no longer goes silent (D-110, 2026-08-11, Tested)** — three audit finds closed: a parked outbox (≈32 s of control-plane downtime) stayed parked until the app quit, so a finished turn could read *Running* for a whole session — every 15 s discovery pass now re-offers delivery to any outbox holding events; a buffer restored after a relaunch was never pumped until new activity appended — startup now flushes every persisted buffer this machine is still enrolled for; and a permanent 4xx dropped the batch with a console warning and no record — the server's refusal now leaves a gap marker exactly where the batch stood (a refused *marker* is given up out loud rather than looped), and a corrupt buffer file states its loss. Evidence: `outbox.test.ts` (12 — the refused-batch marker with its exact range, the refused-marker bound; parking/relaunch/overflow as before), `runner-agent.test.ts` re-run green (7). The transport is authenticated polling, not the persistent connection D-021 chose. There is deliberately **no heartbeat verb**: liveness is a side effect of that polling, which refreshes the runner's last-seen time, and the control plane's sweep reads it — the previous claim that "there is no heartbeat watchdog" was stale and wrong about the consequence, and is corrected in the row below. **A diagnostic can no longer kill the app** (found by the owner in live use, 2026-08-08): the outbox's `console.warn` about a delivery problem raised EPIPE into the dead pipe that had launched the app, and the unhandled stream error crashed Electron's main process with the uncaught-exception dialog; stdout/stderr errors are swallowed at the main process's entry now, because a log line must never be load-bearing |
| Presence | Partial — **Tested** (D-091, 2026-08-08). Per-participant connection state is on the wire: the participant row carries `last_seen_at`, written (throttled to ten seconds) by that person's own read of the mission — the same read that is the lease heartbeat — and `ParticipantSchema` carries a required `connection` projected at read time: connected within 30 s, reconnecting to 90 s (the runner sweep's own recovery window), offline past it or for a participant who never read at all. No event, no receipt, no stored word; ARCHITECTURE.md's persistence section says exactly this now. The Overview's participant list dims a disconnected person to 40% with the state in words beside the role, never a dot (DESIGN.md#component-behavior). Evidence: `attention.test.ts` (never-read is offline while the reader is connected; a backdated read decays to reconnecting then offline at the thresholds), `authority.test.ts` (two clients agree on presence once both have polled), `room-presentation.test.ts` (the contract refuses a participant without the field and admits only the three states). Still Partial: the dimmed rendering is not in the screenshot set — every deterministic capture polls both clients, so nobody is ever away at capture time — and focus/typing presence deliberately does not exist |
| Changes (diff) presentation | Partial — per-file change rows with path, state, and addition/deletion counts, and the real unified diff fetched on demand, all derived from git. Rename and binary detection exist. Inline comments and review threading do not |
| Verification evidence | Partial — **participant-run verification is executable and proven in the app** (`e2e/runtime.spec.ts`, 2026-08-03), end to end: the Run control asks, the control plane authorizes `workspace.command` and pins the declared snapshot, the runner executes it in the worktree, and the check lands with origin `participant`, who asked, start and completion, duration, exit code, how it ended, bounded redacted output, and the exact checkpoint SHA read *before* the command started. A check whose revision the workspace has moved past is presented as stale rather than as current proof, and stale checks do not count toward readiness. **Declared checks now run themselves at the checkpoint (D-082, 2026-08-06)**: a committed checkpoint that changed files queues every declared verification command on the lane's own chain behind the turn's terminal report, each bound to that checkpoint's revision under the project's own deadline, reported with the new origin `automatic` and `requested_by` null — the wire admits only `participant` and `automatic` from a runner, so a runner cannot claim `harness` or `external`; a stopped turn triggers nothing; `autoVerify = false` declines. The ledger renders the fourth origin in words ("Run by Novus at the checkpoint"), and a failed automatic check is re-runnable like any other. Evidence: `workspace-agent.test.ts` (21, 4 new — order, origin, revision binding, terminal-first, verification-only, nothing-declared silence, the opt-out) and control-plane `workspace.test.ts` (20 — stored with null requester even when a participant command for the same name exists, served with origin `automatic`, staleness computed both ways). A check that timed out or was cancelled is recorded `errored` with the ending named — never a pass and never a plain failure (D-043). The previous entry said both "a participant can now run a declared check" and "not yet executable" in the same cell; the first was correct. Harness-observed checks are still recorded only from a structured tool call correlated with its own tool result, and assistant prose never becomes a check. The room says "No checks observed" when nothing ran. Re-running a failed check from its own ledger row now exists (D-057, row above). CI ingestion and manual confirmations do not |
| Competing approaches | **Tested** (D-074) — a second lane is forked from a recorded checkpoint of the one beside it, with a required intent the server refuses empty, its own server-allocated branch, its own worktree, its own runner enrolment and credential, and its own baton held by whoever created it. Forking is `approach.create`, held by Mission Admin and Operator and never granted by the baton; a Contributor is refused. The index that made a second workstream impossible is dropped, direction and control name the lane they act on, and a check now records which lane it proves so staleness is per lane rather than per mission. **Proven in the app** (`e2e/decision.spec.ts`, 2026-08-06): in a real window the room offers *Try another approach* only once the lane has something to fork from, the dialog's primary action stays dead until an intent is typed, the new lane gets its own branch, and directing it produces a checkpoint in **its own worktree** — asserted on disk, along with the absence of the mission-keyed directory the old build used (screenshot `83-`). **The fork point is the shared checkpoint (D-079, 2026-08-06)**: forking beside a lane that is itself an approach starts from that lane's recorded origin rather than its own later work, the dialog shows the mission goal and the exact shared checkpoint with "changes made only in {lane} stay there", the creation request pins the revision the person was shown and is refused `origin_moved` when it has moved, and lanes are named Current work / Alternative / Alternative 2 rather than `main` / `Approach 2`. **Approaches are now operable, not only comparable (D-080; re-presented by D-084 on 2026-08-08, owner-directed)**: the rail's tree carries one row per lane with the blue/amber identity dot and a mission-level Compare row (`Compare · decision recorded` once one stands) — the lane tabs, Compare tab, and *Approaches ▾* switcher D-080 first shipped are gone, and Start approach now lands the room in the lane it just made; the canvas is the selected lane's own room with "Alternative · isolated workspace · forked at {sha}" under the title; the mission's row keeps "2 approaches" for when the tree is folded; an open **file tab is one lane's worktree's copy** — it leads with its lane's identity dot, the same path opened from a sibling approach is a second tab (the path-keyed dedup that silently showed one lane's copy for both was found by the owner in live use 2026-08-08 and fixed; no spec pins it), and choosing a tab moves the room to its lane; the strip is the room's canvas switcher — Mission, Compare while it is open (a closable sibling tab, so reading a file no longer silently swallows the comparison — the owner's second live catch, 2026-08-08, unpinned by any spec), and the open files, side by side; the composer's foot names its target and **every direction carries the lane on screen** — the server resolves the named lane's own lease, refuses a foreign lane 404 and a role without the verb 403; `missions.get` is lane-scoped so control, capabilities, workspace, **state and overlays** are the selected lane's own (a fresh Alternative reads as its own empty room, not its sibling's finished work — found on the first screenshot of this slice and fixed); files, terminal, setup, secrets, checks and output all act in the active lane's worktree; and the selected lane survives a relaunch with the composer target intact. **A fork inherits its sibling's workspace (D-081, 2026-08-06, proven in the app)**: Git-ignored files a person already consented to for the repository are remembered on that machine (bounded store, 0600) and copied into the new lane's worktree through the same validating chokepoint, and the project's declared setup auto-runs as an ordinary authorized `workspace.command` attributed to the forker once the runner publishes the lane's declared list — the dialog says so before the click. Nothing happens for a machine that does not hold the checkout, which keeps the existing pending-lane gap honest. Evidence: `workspace-consents.test.ts` (12 — copied-only recording, dedup, bound with oldest-first eviction, malformed-as-empty, per-repository isolation), and `e2e/decision.spec.ts` (three consecutive green runs): the fixture declares a witness-writing setup, and the witness appears in the fork's worktree with no setup interaction while the baseline lane's worktree stays clean. **An approach's branch belongs to the machine that holds the checkout, whichever provider the repository came from** (fixed 2026-08-06, found in a real session): forking in a GitHub-backed mission run locally asked *GitHub* to cut the branch at a checkpoint only this machine's checkout has ever seen, so every such fork failed with "the base revision no longer exists on the provider" and the worktree with `invalid reference`. The provider is no longer asked for approach lanes — the lane stays `pending`, the desktop cuts the ref in whichever checkout this machine holds (a picked folder and a runner-fetched repository are the same map entry), and **Retry** now performs that same local cut instead of retrying a server call that deliberately does nothing. A fork made from a machine that does *not* hold the checkout still waits as `pending` with no one to cut it — a real gap, named here. **And the pre-D-074 worktree layout now migrates itself** (fixed 2026-08-06, found in a real session): worktrees used to be keyed by mission, so a mission from before the rekeying still held its branch checked out under the old name and every workspace act failed with *already checked out*. Preparation now retires Novus's own mission-keyed worktree when it is clean and retries; a legacy worktree holding uncommitted changes is refused by name rather than deleted, and a conflicting checkout outside Novus's root is never touched. Evidence: `workspace-worktree.test.ts` (3 — the clean migration, the dirty refusal with the file still on disk, the user's own worktree left alone). Evidence: `approaches.test.ts` (18 — the new test forks a GitHub-backed lane at a local-only commit and requires `pending` with no invented failure, an idle retry, and a report that settles it). Evidence: `e2e/decision.spec.ts`, rewritten from tabs to tree rows under D-084 and re-run green 2026-08-08 (1/1, ~9s), driving all of it in a real window — the composer direction sent while the Alternative tab is active checkpoints in the Alternative's worktree, and a reload comes back reading the Alternative with the receipt still on screen (screenshots `83-` … `89-`, reviewed at 1440); `approaches.test.ts` (17 — the routing set: a direction naming a lane lands in that lane's row, events, execution and checkpoint; the lane-scoped read returns that lane's own control and state and refuses a foreign lane; a viewer is refused on a named lane; the rail's lane count), `working-set.test.ts` (19 — the stored lane restored, changed per tab, malformed lanes dropped), `runner-agent.test.ts` (two lanes on one machine get two enrolments and two worktrees, on their own branches). Narrow and medium widths are in the evidence set as the rail overlay (`91-`, `92-`, 2026-08-08): below 1200 the tree is one toggle away, and opening it there is part of the spec — which also closed a dead end this relayout exposed, an overlay nothing could open because `railOpen` had no setter and the topbar toggle's CSS targeted a class the button never carried. **And the routing rule had four holes for requests that arrive through a row rather than a mission id — found in this slice's audit, fixed 2026-08-07 (D-083):** a Stop named no lane on the wire, so one pressed while reading an Alternative was a no-op on the idle first lane while the Alternative's turn kept running under the very control that claimed to stop it; resolving or cancelling a queued direction and answering an approval were judged against the *first* lane's baton, so an Alternative's own controller was refused on their own lane's rows and the first lane's could reach across; and enrolment-time dispatch resolved the enroller's standing against the wrong lane, so a direction queued on a second lane before its runner enrolled could never dispatch. All four are pinned by `sessions.test.ts`'s routing-regression describes, and the renderer now sends the active lane with Stop |
| The Decision Room | **Tested** (D-074) — one surface per mission comparing every lane in the same shape: name, intent, state, baton, changed files with the diff's own arithmetic, checks passed and failed, **what was not verified**, the directions and approvals and stops people spent on it, what the harness said it cost, and the revision it is all about. Columns render in creation order, and a test asserts the order does not move when one lane's checks pass and the other's fail — the shape a helpful sort would take. Files more than one lane changed are named as contested and resolved by nobody. **Proven in the app** (`e2e/decision.spec.ts`, re-run 2026-08-06): with one lane the room offers no comparison at all; with two, the columns render in creation order with the approach's intent on it, *Not verified — everything, no check has run against this* in words, and the contested file named with **Inspect contested changes**. The rendered text is asserted to contain no *recommend*, *winner* or *score* (screenshot `84-`). **Refined the same day**: the header reads *Compare approaches* with *Shared checkpoint · {sha}* beneath it when every fork genuinely started from one revision, the way out reads **Keep exploring**, and with three or more lanes the columns stay two — a row of lane chips picks the compared pair, one click swapping one column, proven in the app with a third approach forked mid-run (screenshot `90-`). Fixed on the way: `branch/report` and `branch/retry` answered with the mission's *first* lane rather than the lane they were asked about, so a created approach came back wearing the baseline's identity — visible only once a second lane existed |
| Recording a decision | **Tested** (D-075) — a decision names the chosen lane and the exact checkpoint, requires a rationale in the person's own words (refused empty by the route *and* the database), carries the risks they accepted, and captures the checks that were unresolved **at that moment** rather than recomputing them later. It is `review.approve`, the capability PRODUCT.md's table has always carried and nothing implemented. A later decision supersedes an earlier one and neither is erased; asking for a revision withdraws the current one and records why. The mission then reads *Decision recorded*, whose whole job is to say nothing has been published. Evidence: `approaches.test.ts` (5 decision tests) and **proven in the app** (`e2e/decision.spec.ts`, re-run 2026-08-06): the confirm control is disabled until a rationale is typed, the dialog shows **the evidence it decides on** — the lane's changes, its checks, and the exact revision the decision will pin — alongside what it is about to record as unverified, *before* the decision is made, and the state line afterwards reads **Decision recorded — {who} chose {approach} — not published yet**, with no button of its own since D-084 — the way in is the rail's Compare row; a reload keeps the decision, the receipt, and the lane that was being read (screenshots `85-`, `86-`) |
| Publishing a decision | Partial — **the pipeline is proven in the app against the fake host, and the adapter's verbs are proven live against real GitHub** (D-099, 2026-08-09; live run below). The prepared projection (D-075) still stands for the un-opened case and for local folders, whose receipt says a folder cannot receive one. For a GitHub-backed mission the receipt now **publishes**: the branch's remote standing in words, **Push branch** — a `push_branch` runner command that pushes the **decided revision itself** (`{sha}:refs/heads/{branch}`), never the branch head, with a write-scoped per-operation credential minted separately from the read-scoped clone one, no `--force` anywhere, and a non-fast-forward surfaced in words (`workspace-push.test.ts`, 4, against a real smart-HTTP remote that answers 401 until the fd-3 helper delivers the token, the token then found nowhere on disk) — then **Create draft PR**, refused by the server in words until `workstreams.remote_head_sha` (written only by an observed `workspace.pushed`, branch-matched) equals the decided checkpoint. What is sent is exactly the prepared projection, snapshotted verbatim on the row. All of it `pr.manage` (Mission Admin, Operator), server-enforced; a contributor is 403 and a non-participant 404 (`pulls.test.ts`, 7, real PostgreSQL). **Proven in the app** (`e2e/pr.spec.ts`, 2026-08-09, 1/1 in ~19s): fork, decide, push from the receipt — the loopback bare remote then genuinely serves the decided SHA, asserted with git itself — draft opened, reviewed, readied, and merged by the fake host's own hand (screenshots `108-` … `110-`, reviewed) |
| Review and revision requests | Partial — `review.approve` is implemented as the two acts PRODUCT.md's table names: recording a decision, and asking for a revision instead, both attributed and event-recorded (D-075). **Pull-request review now operates in-house (D-100)**: threads arrive by poll with the host's own thread ids and resolution state (GraphQL, which retired D-099's REST-only always-open reading on the live adapter's `getPullRequest`); comments are written *from* Novus — inline on a diff line or on the conversation — authored by the App and attributed in the body as "{login} via Novus" until user-token identity exists; threads are **resolved from Novus** and reflected immediately; and **Send to chat** turns a review comment into a prefilled direction the decided lane's conversation genuinely takes (proven in the app: the fake-harness turn ran on it), with **Add all comments to chat** sending every open thread as one numbered direction once more than one stands (Conductor's own move, owner-asked; bounded at 20 with the remainder counted). **A comment from Novus now wears the person's own name where their OAuth token is held (D-101)**: sign-in stores the exchange token under the widened `read:user repo` scope, the comment route performs the call with it — author = them on the host, body unprefixed — and falls back to App-authored with the in-body attribution for a person from before the scope, with the recorded event saying which; the signed webhook receiver lands the host's story without waiting for the poll (`pulls.test.ts`, 10). **Both D-101 halves are proven live** (2026-08-09): a comment sent through the provider's own asUser path with the person's runtime token is authored by *them* — KJ14GOD, not the App — on the scratch repository's PR #4 (`live-ingest.test.ts`), and GitHub's own signed delivery of a human's squash-merge of PR #6 reached the control plane through a disposable cloudflared tunnel, verified, and moved PostgreSQL with no poll (`live-webhook.test.ts`). **The reverse direction is proven live** (`live-ingest.test.ts`, opt-in, 2026-08-09): a real human's inline review comment and squash-merge, made on real GitHub with their own `gh` hand, ingested by the poll into real PostgreSQL — the thread with their login, path, open state and host thread id on the row, live readiness beside it, and `pr.comments` / `pr.merged` as external-actor events. what remains of attribution is storage, not capability — the owner's own token lands at their next sign-in under the widened scope; threading beyond the bounded reflection still does not exist |
| Pull-request creation and tracking | Partial — **Proven in the app against the fake host** (D-099; `e2e/pr.spec.ts`, 2026-08-09, and `pulls.test.ts`, 7). The `pull_requests` row starts existing when a draft is actually opened — the decision it publishes, the host's number and URL, state, mergeability, requested reviewers, ingested threads, the verbatim sent snapshot, and the pushed head SHA; at most one non-closed request per lane by partial unique index. Stewarding is `pr.manage`: request reviewers (named, at least one), mark ready (drafts only; on the live adapter this is the codebase's first GraphQL call, because REST cannot un-draft). A poll (15s deployed, `NOVUS_PR_SWEEP_MS` in tests) ingests the host's story as external-actor events — `pr.comments`, `pr.conflict`, `pr.merged` with who merged, `pr.closed` — and the mission state follows: `pull_request_open` while a draft or ready request stands, back to `decision_recorded` once resolved, with the state line naming how publication ended ("published as PR #1, merged by maya on GitHub" — pinned pure in `pull-presentation.test.ts`, 5). **The request is its own tab since the same evening (owner-directed on sight of Conductor's page)**: a `PR #{n} · {state}` row in the rail's tree and an Open control on the receipt each open it — never by itself (D-089) — onto a page shaped as Conductor's: headline and sent description, one action row, and Comments / Checks {p}/{n} / Changes as segments; the receipt keeps only the headline. Proven in the re-run e2e (tab, rail row, checks count on the segment label, the whole merge flow driven from the tab; screenshot `114-`, reviewed). **Since D-100 (same day, owner-directed) completion is in-house and the merge verb exists, gated instead of absent** — the D-099 absence tests were rewritten deliberately under D-100's entry: the aggregated readiness projection rides the poll onto the row (`readiness` jsonb — host checks with required flags, review decision, behind/ahead, the repository's own allowed merge methods, read never assumed); the merge is two-tier — a draft, a conflict, or a failing *required* check is refused in the host's tier outright and no acknowledgement reaches past it, while every second-tier blocker (change requests, unresolved comments, non-required failures, running checks, a stale branch) is named in one sentence, refused until `acknowledgeBlockers`, and recorded on `pr.merge_performed` as exactly what was accepted; update-branch, close, delete-branch (refused in words while the request is open), App-authored comments attributed in the body as "{login} via Novus", GraphQL thread resolution reflected immediately, and title/label edits that never rewrite the sent snapshot — all `pr.manage`, all event-recorded as the person's act. The runner vocabulary still contains no merge, which `contracts.test.ts` still pins. **The adapter's chain is proven live** (`live-pr-adapter.test.ts`, opt-in `NOVUS_LIVE_PR=1`, run twice 2026-08-09, the second after D-100): against the real `novus-kartik` App and the scratch repository — clone and push through separately-minted read and write credentials over the fd-3 helper, a real draft opened (`pull/1`, later `pull/2`), reviewers requested, mark-ready un-drafting via GraphQL, **readiness read from the real host** (its own allowed merge methods), **a real merge performed by GitHub on the explicit call** (`pull/2`, squash, merge commit `7845296c…` — `mergedBy: app/novus-kartik` verified independently with `gh`), and the branch deleted as its own act. **Still not live**: comment/merge *ingestion* into a mission's own record (the poll ran only against the fake host), and the full in-window flow against real GitHub — see [Known gaps](#known-gaps). E2E: `e2e/pr.spec.ts` re-proven 2026-08-09 for the whole in-house flow — readiness ledger on screen, update-branch, the merge confirm restating "check lint failing" and "2 commits behind" and the merge landing with `acceptedBlockers` recorded, a review comment sent to a chat whose fake-harness turn genuinely ran on it, thread resolution from the page, branch deletion, and archive emptying the rail (1/1 in ~18s, screenshots `108-` … `113-` regenerated 2026-08-09 after the confirm dialogs were recomposed onto the dialog anatomy — head, sub, segmented method picker, quiet fact rows — with the merge confirm and readiness ledger reviewed by eye) |
| Receipts | Partial — a decision's receipt is rendered from durable state: what was chosen, by whom, when, at which revision, the rationale verbatim, the accepted risk, what was unverified, and the approaches that were not chosen, kept with their own intents. **Not built**: the receipt as a durable snapshot over an event range (ARCHITECTURE.md#persistence), a mission-level receipt, and export |
| Missions surface | Partial — the project-first shell (D-032): projects sidebar with a needs-attention lens, an Add project dialog with search, keyboard navigation, and a scrollable result region proven against 120 repositories at three window sizes |
| Mission Room surface | Partial — full workstream title, a persistent state line naming the state and the next action, the controller named in words rather than marked (D-047), a participant stack, one coherent direction trace per direction with human and harness identities distinguished, collapsed technical activity, checkpoint and changed-file evidence, an evidence ledger that stays honest when nothing ran, and a compact composer sharing the trace's content axis. Changes and Verification dock to the right edge as a toggleable column beside the trace rather than over it, a full-height column owning the window's right edge, shown and hidden by one control in the top row, and the projects rail discloses a project's missions from anywhere on its row (D-039, D-055). The panel's own participant-and-control header is gone, because who holds the baton is the room's sentence and was being said twice (D-066). **Approvals have a surface** — the question, what it would do, and Approve once / Deny, proven live and in two clients (D-062, row above) — and when nobody holds the baton the state line says so and the card offers Request control (D-073). A turn that has reported nothing for ten minutes says *no progress reported since {time}* rather than going on claiming to work. Pause/resume and review have no surface because they have no implementation. **The multiplayer projections are now pinned by their own renderer suite** (2026-08-08): `room-presentation.test.ts` (19 tests, plain Node, fixtures parsed through `MissionDetailResponseSchema` so they cannot drift from the wire) covers who holds the baton and the viewer's standing, the approval line naming the act and the claim-the-baton suffix, the runner-offline line replacing every claim, the handoff-in-flight suffix, the stalled suffix with its last-heard time, "· needs you" firing only on a blocked turn, and the lane/session views refusing a sibling's rows. **And a waiting direction now states its queue position in words** — "2 of 3 in the queue" on the thread's waiting row, only while more than one direction waits — which DESIGN.md's *Direction queued* overlay has specified all along; computed against the lane, because sessions share its one workspace (D-083). Tested in that suite; not yet in the screenshot set, which would need two directions queued at capture time. **The suite now also pins the offer countdown** (D-092): minutes then seconds then nothing past expiry, open offers only — and the countdown is on screen in the regenerated `24-handoff-offer.png`. The rail's needs-attention row states its destination (D-093): the lane's name while the mission holds more than one, the blocked conversation's title when it has one, in the meta style beneath the goal — tested by `attention.test.ts` on the wire side and not in the screenshot set, which has no frame with a background lane blocked at capture time |
| Review surface | Not started |
| Design token system and primitives | Partial — tokens.css carries the DESIGN.md values on rendered screens: the Light/Dark/System theme system, 6px buttons, sanctioned brand glyphs (D-029, D-030), and read-only local-harness detection on setup. **The theme choice is reachable after first run (D-102 → D-103, 2026-08-10)**: the sun-or-crescent at the rail's foot — the glyph is the resolved theme, System wearing whichever it resolves to — and `⌘,` open a block centered on its trigger carrying three glyph segments — sun, crescent, display, the active one washed — never a dialog and never words (D-103 reversed D-102's dialog, D-104 the worded segment, each on the owner's sight of the rendered screen); a flip repaints live terminal panes; the light palette is **screen-proven** — populated room, rail, popover, and a relaunch resolving light before first paint (`theme.spec.ts` driving the block, `⌘,`, the glyph, System-follows-OS, and persistence across a real relaunch; evidence 115–118, the popover shots re-taken as the icon block). **A person wears their own GitHub picture, and Sign out is a glyph (D-105, 2026-08-10, owner-directed on sight)**: every human identity mark takes the avatar behind the login it already shows — fetched in the main process from GitHub's own `https://github.com/{login}.png` and handed to the renderer as a `data:` URI, so nothing is stored, no contract or user row grows a field, and the window's `img-src 'self' data:` is untouched — with initials remaining the fallback whenever there is no picture; the rail's account corner is now three marks and no words, `Sign out` living on the button's `aria-label` and `title` beside the theme glyph. **The face is never late** (amended the same day on the owner's sight of it arriving a beat after the initials): the picture is kept on this machine under `userData` and answered from there without awaiting the network, and the signed-in person's is resolved *before* the main process announces `signed_in`, riding on the auth status and seeded into the renderer's identity cache — so their mark is a photograph on the first frame the shell paints, at sign-in and at relaunch alike. Seen in a real window at 1440 wide (evidence `119-identity-photo-and-sign-out.png`: the photo in the circle, the door-and-arrow beside the crescent), captured by a throwaway probe signed in as a login that has a real picture, which asserted the mark held an `img` and no initials at the instant `project-shell` appeared, again after a real relaunch, with the machine-local copy on disk between them — then deleted, because **the photo is a live network fact and no deterministic spec asserts it**; `theme.spec.ts` re-run green on this build covers the corner's fallback path, where the fake login has no picture and the initials stand. **Reworked on rendered screens 2026-08-03 (D-047)**: the ground drops to near-black so surfaces have somewhere to rise from, text and edges strengthen, status becomes words rather than a coloured dot beside them, the baton stops being a mark and stays a sentence, and the dock stops carrying views at all — it is the terminal alone (D-049) — so only the evidence panel says *Verification*. A terminal tab is a glyph, a name, and a close. **Chrome quietened 2026-08-06 (D-076)**: one button size across every variant at the 32px target floor, a 1px focus ring with text fields showing focus on their own edge rather than wearing a white slab, dialogs as one plane instead of three, and a real `Dialog` primitive so `Esc` and a click outside close every one of them. **Focus handed back by that primitive wears no ring (D-106, 2026-08-10, owner-directed on sight)**: `Esc` out of the new-mission dialog returned focus to the row's hover-only `+` and the browser, counting the key press as navigation, ringed it — a white box clipped by the rail's edge around a control nobody aimed at. The opener now keeps the focus and carries `data-focus-quiet` until the **next key press**, which restores the ring in place; nothing is blurred and focus order is untouched. Seen in a real window at 1440 (a throwaway probe, deleted: after `Esc` the `+` is `document.activeElement` with computed `outline-style: none`, and one `Tab` clears the attribute). Reviewed by eye on the approach dialog at 1280 wide; the other dialogs inherit it and have not been re-screenshotted. The scale has since gained `--type-speech` for what the agent said (D-065) and the token set a `--traffic-inset` for the window's own buttons (D-067, D-068). The light palette remains provisional — still not screen-proven. Several named primitives are still composed ad hoc rather than extracted, and the newest surfaces named in the top-row row are not in the screenshot set. **The 2026-08-18 consistency sweep landed (D-146)**, each fix with a before/after pair rendered through the real stylesheet: the attach dropdown wore an undefined class and drew the OS select — now the one `.select` idiom shared with the invite role picker (`177`/`178`); the decision room's citation checkboxes were native — now the task-glyph look, live (`179`/`180`); setup's theme control was the last bordered-chip segment — now `segment-tab` like every other switch (`181`/`182`); worker reports and the receipt's rationale and risks rendered Claude's markdown raw — now through the D-145 renderer (`183`–`186`); and the unstyled hooks (`file-note`, `board-card-repo`, `dock-run-name`, the decision dialog's evidence sections, `contested`, `worker-inspector-section`) got their rules, with the dead `is-approach` modifier removed outright (`187`/`188`) |

## Documentation

| Item | Status | Evidence |
| --- | --- | --- |
| Canonical documents (README, PRODUCT, DESIGN, ARCHITECTURE, PROGRESS, DECISIONS, AGENTS, CLAUDE symlink) | Implemented | Files exist at repository root; `CLAUDE.md` is a symlink (`ls -la CLAUDE.md`); `scripts/gate.sh` exits 0 (run 2026-08-02) |
| Executable repository gate | Implemented | `scripts/gate.sh` exits non-zero on seeded violations and 0 on the current tree (D-016, D-026); it runs build, typecheck, lint, and the deterministic suites. **Last run: 2026-08-15 (after the architecture pass, behavior intentionally unchanged throughout)**: (1) the control plane's runner lookup and runner-command transport concentrated into `apps/control-plane/src/runners.ts` — the lane's-active-runner query (previously written independently in seven places), the room's deliberately different display lookup (previously an eighth, undocumented variant), the stable-key `enqueueCommand`, and the state-answered `enqueueRepeatable` (previously duplicated inserts in `pull-requests.ts` and `workspace.ts`), with the kind vocabulary now the contract's own `RunnerCommandKind`; (2) the per-mission advisory lock made structural — `lockMission`/`withMission` in `db.ts` are now the only home of the lock SQL, with all 18 handler copies and `authority.ts`'s private helper converted; (3) the room's detail projection made honest — `observeMission` is the poll's explicit write half and the per-lane holder N+1 is one query; a `withSnapshot` repeatable-read wrapper was also tried and **reverted on 2026-08-16**: one connection serializes its queries, the room's polling endpoint measurably slowed, and the full Electron run caught it (`runtime.spec.ts`'s rail test failing on main and passing at the pre-pass commit), so cross-query consistency stays best-effort until the projection becomes one composite query — and the rail helper's own read-then-click race with the restore path, which the slowdown had exposed, is fixed by converging (a full `pnpm run e2e` passes 56/56 runnable on 2026-08-16); (4) `@novus/contracts` split — product policy (profiles, models, evidence claims) into `policy.ts` and the D-097 scope algebra into `scope.ts`, both re-exported so importers are unchanged; (5) the desktop's ~650-line feed projection moved from `direction-trace.tsx` into `derive-feed.ts`, pure and JSX-free, with the component re-exporting it; (6, 2026-08-16) the first store-seam slice — `apps/control-plane/src/workstreams.ts` now owns the Workstream row shape (the two divergent `toWorkstream` mappers unified), `listWorkstreams`, and `laneRepository` (the lane→repository join previously written four ways across `repo-clone.ts` and `pull-requests.ts`); `standingDecision` in `approaches.ts` is the one reading of "the decision nothing superseded" (previously re-derived in `pull-requests.ts` and `close.ts`); and `lastEventAt` joins the event log's owned reads in `events.ts`; (7, 2026-08-16) the publication surface split — `apps/control-plane/src/publication.ts` is the domain module (the tracked row and its reads, `acceptableBlockers`, and eleven verbs — push, open-draft, review, ready, the two-tier merge, update/close/delete-branch, comment, resolve-thread, metadata — each returning a typed outcome, plus the host-sync sweep and webhook body), while `pull-requests.ts` shrank to the HTTP shell: parsing, authorization, one provider-error translator, and the outcome→status-code maps. The split's one behavioral wobble — routes parsing before authenticating, so a bad body answered 422 where 401 was owed — was caught by the suite and reverted to auth-first. Suites after the pass: control-plane 287/287, desktop 479/479, contracts 39/39. After the pass was committed (198ae73, 2026-08-16) — with the installed engineering skills and their lock file tracked so nothing sits in the tree undecided — the gate says `GATE PASS` unconditionally. Earlier run 2026-08-15, after the artifact phase's reconciliation: every check passes — build, typecheck, lint, the full deterministic suites, links, anchors, terms, colors — except check 12, which correctly names files the machine's operator left untracked (`.agents/`, four `.claude/skills/` directories, `skills-lock.json`); they are not this repository's to decide, and the gate is doing its job by refusing to decide for them. The gate also earned its keep in that run: it caught a DESIGN.md bullet matching the domain-term pattern and a raw hex in an e2e fixture, both fixed before this record. Last unconditional `GATE PASS`: 2026-08-16. Seeded-violation behaviour last exercised 2026-08-02 |
| Decision record | Implemented | [DECISIONS.md](DECISIONS.md) contains the numbered product and design decisions through D-121 — the newest being the terminal lifecycle: a mission ends by a person's own act, completion gated on its meaning, the receipt snapshotted as the room frozen (D-121), before it Home as the board: missions grouped by what they need in computed columns, opened at the thing that is asking, with the owner's unused bullets recorded and the create-vs-poll duplicate fixed at the source (D-120), before it project MCP servers governed as a published registry with Mission Admin activation, admission-as-network-restriction, a strict Novus-authored config, and tools that always ask (D-119, owner-directed on D-118's surfacing), before it project skills reaching the agent published for review, enabled by digest, and composed alone (D-118), before it the profile picker taking Codex's shape at the owner's direction, the dangerous row warn whole (D-117), before it the slider cut it replaced, with evidence captured over a working room (D-116), before it permission profiles, where the harness always asks and a profile changes who answers (D-115), before it the turn's pulse that is never progress (D-114), before it the lane stating what it has spent so far (D-113), before it the queue that neither strands nor hides (D-112), before it force interrupt as the escalation of an unanswered stop (D-111), before it the outbox that never goes silent (D-110), before it a turn ending the way the harness said it ended (D-109), before it workers as the turn's own rows entered with Enter (D-108) atop the recorded worker join (D-107), before them focus handed back by a closing dialog wearing no ring (D-106), before it the identity mark wearing the person's own GitHub picture with Sign out as a glyph (D-105), before it the newest being the theme block at the rail's foot whose glyph is the resolved theme (D-103, reversing D-102's dialog) with the screen-proven light palette; before it comments authored as the person themselves via their stored OAuth token with the App fallback pinned, and the signed webhook receiver with the poll demoted to fallback (D-101) — the newest being completion moving in-house on the owner's direction: Novus operates the pull request end to end and a person merges from inside the room, explicitly, against stated readiness, with GitHub performing the act underneath (D-100, reversing D-099's structural absence while keeping its never-silent rule); before it the tracked pull request itself (D-099) — the newest being presence as a projection over the participant's own read (D-091), the ten-minute handoff-offer expiry with its countdown (D-092), the needs-attention row naming the lane and conversation (D-093), every chat's own state word, footprint, and overlap warning (D-094), read-alongside turns — writes exclusive, reads beside them (D-095), checks attributed to the chat whose checkpoint they ran at (D-096), and chat file ownership with provably disjoint parallel writers (D-097), and the preview as a surface inside Novus rather than a hand-off to the browser (D-098) — the latest being the approaches set (D-079 … D-081), checks that run themselves at the checkpoint (D-082), sessions with the turn-taking rule (D-083), and the rail-as-tree relayout that superseded the tab presentation on the owner's direction (D-084), and the working set holding views (D-085) and its same-morning reversal into approaches as the room's own colour-dotted sub-tabs (D-086), then sessions joining that row as openable, closable tabs (D-087), dotted with their approach's colour, cross-lane and reorderable (D-088), and the approach tab becoming its own landing page — overview with linked conversations, no implicit tabs (D-089), with the drag rebuilt as live browser-style reordering for session and file tabs after synthesized input proved a misleading witness (D-090) — including the workspace-runtime set (D-040 … D-046), files in the room (D-048), the dock as terminal (D-049), output in the evidence panel (D-050), the two uncalled verbs (D-051), the credential policy (D-052), Stop reaching the harness (D-053), missions in the rail (D-055), the approval-protocol probe (D-056), running a check again (D-057), repository recovery (D-060), the working-set and transcript decisions (D-061 … D-065), shell composition (D-066 … D-068), mission search (D-069), the at-most-once command memory (D-070), what a turn cost and the harness's own workers (D-071), the refusal to load project skills (D-072), and the stall overlay with the question that outlives its answerer (D-073) |
| Novus UI quality workflow | Implemented | Project skill at `.claude/skills/novus-ui/SKILL.md` points to `DESIGN.md` and requires a Surface Taste Brief plus rendered screenshot review |
| Canonical-truth reconciliation | Implemented | Run 2026-08-08 (multiplayer-UI slice, owner-directed): DESIGN.md's five `ParticipantStack` mentions — signature element 4, the primitives list, the inspector's header, the Participants bullet, and the *New mission* state row — still described the panel identity header D-066 removed in favour of Overview's participant list; each is now aligned to D-066 (participants as Overview's list, the baton as the room's sentence per D-047, connection-state rendering kept and marked as waiting on the wire), and the component itself — dead code whose own CSS selector never matched its class — is deleted with its rules. Earlier run 2026-08-05, against the committed tree at `bccf441` and the evidence on disk rather than against commit messages. Corrected: approval routing was still called Not started in the milestone and "not built" in three rows and the verbs table, while it is proven live against real `claude 2.1.221` and proven in the app for two clients; the repository's `CLAUDE.md` was still recorded as unreachable after D-064 restored it through `--append-system-prompt-file`; `needs_approval` was recorded as never entered; re-running a failed check was listed as non-existent in the verification row while the row above it proves it; the evidence header's suite totals were presented as current when five slices have landed since that run; and the shell rows claimed the whole top-row surface as proven when the last three UI commits (the rail's switch, search as a palette, the workspace drawer) landed after the screenshot set with no regenerated evidence. Earlier run 2026-08-03. Four contradictions the previous revision carried are corrected against executable behaviour, not against commit messages: "GitHub repositories run locally — Implemented" claimed a capability eight places in the interface still refused, and is now Partial with those gates removed and the missing live proof named; "there is no heartbeat watchdog" was stale — the sweep is wired into the real server and ends a silent runner's execution in ≤105s — and the two rows now say the same thing; "Verification is observation-only … it cannot run a check itself" was false, and the row that said both "a participant can now run a declared check" and "not yet executable" in one cell is resolved to the first; and the terminal, listed as **Not built** while 17 real-PTY tests passed on disk, now has its own row saying what is tested and what is proven. A tiered evidence rule is stated in [Status rules](#status-rules) so "a suite passes" can no longer be written as "it works". Earlier reconciliation run 2026-08-02: The contradictions recorded against the previous milestone are resolved: local execution is stated as the first shipped surface in README, PRODUCT, and ARCHITECTURE rather than a footnote; repositories are GitHub **or** local in the domain model; the runner is the host desktop first; the local-repository question closed by D-032 no longer appears as an open extension point; disconnection no longer ends authorized work; and the mission's primary state is documented as a projection, not a stored field |
| Vertical slice — desktop workflow, live | Implemented | `pnpm run e2e` (2026-08-01, still passing): the actual Electron app signs in, creates a mission, closes fully, relaunches, restores its session from OS-backed storage, and reconstructs from PostgreSQL |
| Workspace runtime — terminal, setup, verification, through the interface | Implemented | `pnpm --filter @novus/desktop exec vitest run --config e2e/vitest.config.ts e2e/runtime.spec.ts` — **re-run 2026-08-06, 16/16 in ~35s**, after its draft-flow setup was rewritten against the ask-dialog (D-077) and its worktree paths rekeyed by workstream (D-074). Original run 2026-08-03, 13/13 in ~41s. A real Electron window, a real PTY running a real shell, real project commands in a real worktree. Proves: the dock opens straight into a focused shell with no switch and no rename control (D-049); a session opens in the mission worktree and not the user's own checkout; colour is painted and a clear genuinely clears; Ctrl-C interrupts; tabs named `{repository} {n}`, a `+` that selects and focuses what it made, switching back to an earlier tab with its session still on screen, and close; setup, the app, and checks each reachable from their own surface; a session survives the dock being closed from its toggle; a second desktop is refused the terminal both in the interface and by name at the bridge; setup runs from a reviewed proposal and reaches `ready`; a declared check runs on a click, passes with exit code 0 bound to the worktree's HEAD, and a failing one is recorded `failed` with its output; a relaunch shows no sessions rather than a dead tab. Screenshots `45-` … `58-` in `apps/desktop/e2e/evidence/`. The spec has since grown to 16 tests — missions in the rail (D-055), Stop (D-053) and running a check again (D-057) were added to it on 2026-08-04 and are recorded on their own rows; the 13/13 figure belongs to the 2026-08-03 run and has not been superseded by a recorded whole-spec pass |
| Two-client multiplayer — desktop workflow (fake harness) | Implemented | `pnpm --filter @novus/desktop exec vitest run --config e2e/vitest.config.ts e2e/multiplayer.spec.ts` — **re-run 2026-08-08 a second time, 1/1 in ~34s after the D-091/D-092 rebuild**, regenerating screenshots `20-` … `29-`, with `24-` (now carrying the offer's "expires in 10m" countdown) reviewed by eye against DESIGN.md's prohibited patterns; `e2e/approval.spec.ts` re-run in the same batch, 3/3 in ~25s. Prior same-day run and review noted below. Prior re-run 2026-08-06, 1/1 in ~33s (one correction: the workspace drawer opens on Overview already, and the spec's tab click was folding it away). Original run 2026-08-02, 1/1 in ~29s. Two real Electron clients, two separately authenticated people, one control plane. Kartik opens a local project, directs a turn, and invites Maya; she redeems from a second desktop **with no checkout of her own**, is refused the mission before redemption, cannot replay the invitation, reads the identical participants, directions, and checkpoints, submits a direction that queues attributed to her, requests control, is offered it, accepts, and takes the baton at the idle boundary. Kartik is then denied `direction.apply` and `control.offer` by the server. Maya applies her queued direction and **Kartik's runner executes it**. Both clients relaunch and reconstruct. Screenshots `20-` … `27-` in `apps/desktop/e2e/evidence/`. The harness is the deterministic fake: this is proof of the pipeline, not of Claude Code |
| Live Claude Code through the runner | Implemented | `NOVUS_LIVE_CLAUDE=1 pnpm --filter @novus/desktop exec vitest run --config e2e/vitest.config.ts e2e/live-claude.spec.ts` — re-run 2026-08-03 against `claude` 2.1.220, 1/1 passed in ~23s, still green after the runtime slice. Original run 2026-08-02 (~50s). The real `claude` binary, no fake harness. Observed in the durable record: two executions on session `89462e53-…`, the second with `resumed_session = true`; checkpoint `27b246bf…` with outcome `committed`, one file changed, `+1`; `file_changes` row `HELLO.md added +1 −0` with its stored diff; `verification_checks` count `0`. The test independently verified the claim against the repository itself — `git show <mission-branch>:HELLO.md` contains `hello` and the branch head equals the recorded checkpoint SHA — and the follow-up turn, told not to use tools, answered `HELLO.md`, which is only possible if the session was genuinely resumed. Opt-in because it spends the machine owner's Claude quota |
| Harness feasibility — documentation-level | Implemented | Official-docs verification for Claude Code and Codex recorded with sources in D-017 |
| Harness feasibility — hands-on, local + clean-Linux container | Partial | Run 2026-08-01. Claude Code 2.1.220 stream-json, `--resume`, SIGTERM semantics, allowlist enforcement; Codex 0.145.0 `exec --json`, resume, app-server handshake. Clean Ubuntu 24.04 install paths verified for both |
| E2B provider spike — live run | Implemented | Run 2026-08-01, results in `spikes/e2b/spike-results.json` (D-024) |
| Repository slice — desktop workflow (fake provider) | Implemented | Run 2026-08-01 on `slice/repository-missions`, still passing: repository selection, base pinning, branch allocation, relaunch reconstruction, and the failure/retry path through the real Electron app |

## Critical verbs, and what proves each one is wired

Three faults have reached real sessions because a verb was written, tested, and
never called (D-051). The check that would have caught all three is a test that
enters where the product enters, so every verb below names one. A unit test that
imports the function and calls it is not in this column, on purpose — that is
the kind of test all three faults already had (D-054).

| Verb | Entered through | Evidence |
| --- | --- | --- |
| Secret storage | the bridge's `supplySecret` | `workspace-policy.test.ts` — supplied, listed by name with no value, refused below the length Novus can redact, refused where the OS offers no encryption |
| Runner heartbeat | the runner's own poll | `runner.test.ts` — liveness is a side effect of polling; a runner silent past the threshold has its execution ended by the sweep |
| Lease heartbeat | reading the mission | `reliability.test.ts` — a lease backdated past its TTL survives its holder's read and does not survive a bystander's (D-051); `attention.test.ts` — reading the *default* lane renews a background approach's baton its reader holds, and a non-holder's read renews nothing |
| Participant presence | reading the mission | `attention.test.ts` — a reader is connected, a never-reader is offline, a backdated read decays through reconnecting at the projection's own thresholds (D-091) |
| Read-alongside dispatch | the direction route with `alongside: true` | `alongside.test.ts` — a read execution starts beside the running write turn and the non-controller is refused in words; `e2e/approval.spec.ts` — the read turn answers to completion in a real window while the write turn stays blocked (D-095) |
| Read-turn containment | the turn's own control channel, and event ingestion | `execution.test.ts` — the permission request is denied at the machine with nothing published and nothing written, no checkpoint, no boundary; `alongside.test.ts` — a boundary reported for a read execution anyway moves no baton (D-095) |
| Scope enforcement | the turn's own control channel, by policy | `execution.test.ts` — an in-scope Write is allowed with no card and no false boundary, an out-of-scope Write is refused with nothing written (D-097) |
| Profile answers | the turn's own control channel, by the lane's pinned profile | `execution.test.ts` — each profile's answer at the router with the recording, containment and ownership outranking trust; `execution-approval.test.ts` — plan's flag and manual's through a real spawn; `e2e/approval.spec.ts` — set from the chip in a real window, a turn writing with zero cards and the grant recorded (D-115) |
| Skill composition | the turn's own spawn, from the pinned enabled list | `execution-approval.test.ts` — the composed skills-only directory on the real spawned argv, holding exactly the approved bytes and nothing else, gone with the turn; a stale digest passing no directory with the drop reported; `e2e/skills.spec.ts` — enabled from the inspector in a real window and carried by the next turn (D-118) |
| MCP composition, and the always-ask rule | the turn's own spawn and its control channel | `execution-approval.test.ts` — `--mcp-config` + `--strict-mcp-config` on the real spawned argv with exactly the approved entries; `execution.test.ts` — `auto` handing an `mcp__` tool to the room, `dont_ask` answering it on the record; `e2e/skills.spec.ts` — enabled by the Admin in a real window and carried by the next turn (D-119) |
| Open at the asking thing | a board card's or lens row's click, through `openMissionAt` | `working-set.test.ts` — the named lane and conversation land on the tab, an open tab re-aims rather than duplicating; `e2e/home.spec.ts` — the Needs-you card's click puts the blocked conversation and its approval card on screen in a real window (D-120) |
| Ending a mission | the close route, then every operating verb's own guard | `close.test.ts` — the tier, both completion gates, archival's refusals, the snapshot's determinism, the refusals after; `e2e/home.spec.ts` — cancelled from the Overview in a real window, the room freezing into the receipt and the board filing it under Complete (D-121) |
| Parallel writer admission | the direction route, under the mission lock | `scoped.test.ts` — disjoint scopes yield two live write turns, overlap defers with the overlap named, an unscoped party restores exclusivity; `e2e/parallel.spec.ts` — the second direction dispatches immediately in a real window |
| Scoped checkpoint capture | the turn's boundary, under the worktree's capture lock | `evidence.test.ts` — the commit holds exactly the scope's paths, drift is named and left on disk, a sibling's territory stays silent; `e2e/parallel.spec.ts` — both files end on one clean branch |
| Offer expiry | the reliability sweep, and the accept route | `authority.test.ts` — an overdue open offer lapses with its event and frees the slot; a lapsed offer is refused at accept before any sweep has moved it (D-092) |
| Repository clone and fetch | the runner agent's discovery pass | `workspace-clone.test.ts` — a second workstream's branch is fetched into a repository this machine had already cloned (D-051) |
| Command dispatch | the runner agent against a stubbed control plane | `workspace-agent.test.ts` — setup, run, stop, and verification each produce their real effect |
| Stop execution | a `stop_execution` command through the agent | `workspace-agent.test.ts` — two of its five tests fail against the pre-D-053 code |
| Check execution | an authorized command through the agent | `workspace-agent.test.ts`, and `e2e/runtime.spec.ts` on a click |
| Workspace setup | an authorized command through the agent | `workspace-agent.test.ts`, and `e2e/runtime.spec.ts` through the dialog |
| Checkpoint creation | `captureCheckpoint`, which the turn calls | `evidence.test.ts` — including a commit that genuinely fails, where the room is told what changed rather than nothing (D-054) |
| Event publication | the agent's outbox to a stubbed control plane | `workspace-agent.test.ts`, `outbox.test.ts` — ordering, de-duplication, backoff, gap marker |
| Approval response | Approve once / Deny in the room, answered back over the harness's own stdio channel | `e2e/approval.spec.ts` — two clients, the wrong one refused, the baton moving while the harness blocks; `execution-approval.test.ts` — a real process with a stub `claude` asserting the exact argv; `live-approval.test.ts` — the real binary, opt-in; `sessions.test.ts` — the answer judged against the *approval's own* lane, not the mission's first (D-083) |
| Session resolution | the direction route, inside the submit transaction | `sessions.test.ts` — absent lands in the lane's first, `newSession` creates and titles from the words, a foreign session is 404, both at once is 400; `e2e/sessions.spec.ts` — created and routed through the real composer |
| Turn-taking across sessions | `/runner/events` ingesting a terminal report | `sessions.test.ts` — a sibling's direction defers with the running session named; `execution.completed` dispatches the holder's oldest queued direction into its own session, `execution.stopped` dispatches nothing, and a direction an execution already carried is never re-run; `e2e/sessions.spec.ts` — the queued direction starts on its own in the window |
| Stop, on the lane being read | the stop route, with the lane on the wire | `sessions.test.ts` — `{workstreamId}` stops that lane's turn and its absence touches only the first lane; `e2e/sessions.spec.ts`'s room sends the active lane |
| Project instructions | the turn's own spawn, on every attempt | `execution-approval.test.ts` — `--append-system-prompt-file` is present with the worktree's `CLAUDE.md`, absent when there is none, and refused when the path leaves the worktree (D-064) |
| Branch push | a `push_branch` command through the agent, with a real `git push` | `workspace-push.test.ts` — the decided revision lands on an authenticating smart-HTTP remote through the fd-3 credential helper, refusals before git is asked, no force; `e2e/pr.spec.ts` — the bare remote serves the decided SHA after the receipt's own button (D-099) |
| Remote-head guarantee | the create route, against the recorded remote head | `pulls.test.ts` — never-pushed, wrongly-pushed, and exactly-the-decision each answered in words; `workspace.pushed` moves the head only for its own branch |
| Pull-request stewarding | the pr routes, gated on pr.manage | `pulls.test.ts` — reviewers and ready on a person's ask, a contributor 403, a non-participant 404; `e2e/pr.spec.ts` — driven from the page itself |
| Host-story ingestion | the pull-request sweep | `pulls.test.ts` — comments, resolution, conflict, and a human's merge each land as external-actor events and move the row; `e2e/pr.spec.ts` — the room follows within the sweep's own cadence |
| The merge, past its two-tier gate | the merge route, then the host's own API | `pulls.test.ts` — a draft, a conflict, and a failing required check refused outright with no acknowledgement reaching past them; named blockers refused until acknowledged, then recorded verbatim on `pr.merge_performed`; `e2e/pr.spec.ts` — the confirm restates them in a real window and the merge lands; `live-pr-adapter.test.ts` — GitHub itself performs one on the explicit call (D-100; rewritten from D-099's absent-verb test under D-100's entry) |
| The runner still cannot merge | the vocabulary itself | `contracts.test.ts` — no merge word parses as a capability, a runner command, or a state; the host API is the only path and pr.manage the only key (D-099, D-100) |
| Completion's tail | the close, delete-branch, and archive verbs | `pulls.test.ts` — delete refused in words while the request is open, allowed after; `e2e/pr.spec.ts` — branch deleted and the mission archived from the page, the rail emptying |

## Known gaps

- **The live GitHub runtime proof has now been run** (2026-08-16, 5/5 — the row above carries the evidence). The first-turn race those debugging runs observed — the mission's first direction dispatched the moment the machine takes the lane, while the fetch that enrolment makes possible has not begun, failing a real user's very first turn with `fatal: invalid reference` or the misdiagnosed "lives on another machine" — **is closed (2026-08-16)**: the dispatch path now waits for the checkout the way D-060's guard already made announce wait, resetting any fetch backoff because a person just asked, with checkout mutations serialized per repository (the D-058 shape). Proven by seeding both failure shapes (`runner-agent.test.ts`, "a GitHub lane's turn waits for the fetch it raced": a refused first credential mint leaves no checkout, and a prior mission's checkout lacks the branch — both fail without the guard with the exact production reasons, both complete with it), and by two consecutive live runs (2×5/5) whose first turns completed. The spec's original text follows for what the run requires. **Original requirements:** `apps/desktop/e2e/live-github.spec.ts` selects a real repository through the interface, creates a mission, waits for the runner to fetch it, checks the worktree is on the mission branch at the exact recorded base, runs a declared command and a check on it, then independently asserts that no installation token is in `.git/config`, anywhere under the machine's Novus state, in any event payload, or in any recorded check — and that a user session cannot ask for one. It is opt-in and cannot be otherwise: repository access comes from a GitHub App installation bound to a real organization, and that binding is created by a person signing in through their own browser, so the run borrows a profile that is already signed in.

  ```
  NOVUS_LIVE_GITHUB=1 NOVUS_LIVE_PROFILE="$HOME/Library/Application Support/Novus" \
  NOVUS_LIVE_REPO=owner/name \
  pnpm --filter @novus/desktop exec vitest run --config e2e/vitest.config.ts e2e/live-github.spec.ts
  ```
- **The live two-client proof has now been run** (`apps/desktop/e2e/live-multiplayer.spec.ts`; written 2026-08-08, run 2026-08-16: 1/1 in ~28s, `NOVUS_LIVE_MULTI=1`, screenshots `32-` … `34-`). It is `multiplayer.spec.ts`'s wedge with the fake harness removed: two Electron clients under two authenticated identities, Kartik's live turn stopping at its real permission question and being approved in his window, Maya joining by invitation, taking the baton through the request-offer-accept handshake, applying her own direction, and answering the real question *her* turn raises from *her* client — then the repository itself asked whether both people's files are commits on the mission branch, resumed in one harness session. Opt-in (`NOVUS_LIVE_MULTI=1`) because it spends the machine owner's Claude quota. Two honest limits, stated in the spec itself: both clients run on this one machine under separate user-data directories, so a green run proves two real clients and a live harness, **not two physically separate machines** — the Golden V0 line stays owned by a genuinely two-machine run that only people with two machines can perform; The 2026-08-16 run proves the one-machine form: both people's live turns landed as commits on the mission branch in one resumed harness session.

  ```
  NOVUS_LIVE_MULTI=1 pnpm --filter @novus/desktop exec vitest run --config e2e/vitest.config.ts e2e/live-multiplayer.spec.ts
  ```
- **The desktop suite's intermittent failure was a real bug, and is fixed.** It runs files one at a time, and 239 desktop tests passed on a quiet machine while `workspace-clone.test.ts` failed intermittently under load. The gate used to run every step into `/dev/null`, so a failure named the command and nothing else; that was fixed first, and the output then said `fatal: cannot change to '.../worktrees/msn_…': No such file or directory` — a worktree deleted while its creator was still using it. Two callers ask for the same worktree by design (the runner's turn path, and a terminal or file listing the person just opened), and preparation read "is it there" and then deleted what it found, so the loser removed the winner's worktree. The user-visible form is *"This workspace could not be created"*, reported from a real session and read at the time as a GitHub problem. Preparation is now serialised per worktree path (D-058). Proven by seeding the failure — bypassing the serialisation makes the new six-caller test fail with that exact message — and by four consecutive clean runs of a file that had failed one in three.

  Three more failures of the "only under load" family have since been caught **with** their output and fixed, which is the standard this line holds things to. A full `pnpm run e2e` failed the *first* terminal test at its poll ceiling while every later terminal test in the same run passed in under five seconds: the readiness helper decided a shell was ready after ~600 ms of silence, and a cold login shell — this machine's loads conda — emits its startup in bursts whose gaps exceed that window under load, so the keystrokes went into a shell that was not listening. The command is retried now, which addresses swallowed input rather than slowness. The same thing then happened in the *unit* suite, in `workspace-terminal.test.ts`, with the same remedy. The third was not a test problem at all: `workspace-agent.test.ts` failed teardown with `ENOTEMPTY`, because `shutdown()` returned while work queued on a workstream's lane was still running `git` inside the repository — publishing what a project declares reads the repository and can create a worktree. Shutdown now drains those lanes, which also makes quitting the app honest rather than merely quick.

  **The first of the three is not closed.** It recurred on 2026-08-05 in a full `pnpm run e2e` run immediately after a build: the retry ran out of attempts rather than the shell ever answering, and the same spec passed alone on the next run. The retry budget is raised, and that is a mitigation and not an explanation — retrying more often is not the same as knowing why a cold shell under load sometimes never reaches its prompt at all. Nothing has captured that yet, so this stays open.

  **And the family is wider than the terminal.** On 2026-08-05 a full `scripts/gate.sh` run failed three tests in `workspace-agent.test.ts` — a check that never completed inside its 30 s ceiling, and then two `beforeEach` hooks that timed out behind it — while the same file passed alone in 35 s immediately before and the gate passed on the next run with no change in between. So the shape is now known to be: real processes under a loaded machine, first failure cascading into the hooks after it. Still an explanation nobody has, and still open; what is new is that it is not specific to cold shells.
- **A project's `.claude/skills` now reach the agent, person-approved and composed alone (D-118)** — the slice D-072's revisit clause named, built on 2026-08-12; the Project skills row carries the evidence. What remains honestly open: a skill's supporting files are not composed (SKILL.md only — a skill that reads sibling files finds the worktree's copies through ordinary routed tool calls, not the plugin's); the operator's user-level skills still load under the pinned settings, which is the CLI's own behavior about whose machine the runner is (measured in D-072, unchanged); read-alongside turns carry no skills; and plugins as such and hooks stay refused. MCP servers, surfaced by D-118 as an unmade product decision, were directed by the owner and built the same day (D-119): registry-as-manifest, Mission Admin activation, strict Novus-authored config, and tools that always ask — the Project MCP servers row carries the evidence and its own residue.
- **The harness turn has no wall-clock bound**, which is D-034's intent, but the same chaining means a wedged `claude` process holds the lane: every subsequent workspace command from every participant queues behind it indefinitely rather than failing loudly. **Now visible rather than silent** (D-073): ten minutes with nothing reported carries the *Execution stalled* overlay and the state line says *no progress reported since {time}*, and the recovery is the Stop every non-viewer already holds. **And a stall can now be told from a long tool call (D-114, 2026-08-11, Tested — D-073's own revisit clause)**: the runner states the turn's pulse every three minutes while the harness process is alive (`execution.heartbeat`, recorded-and-nothing-else), the stall watch excludes it from the progress clock on both sides of the wire so a wedged-but-breathing turn still stalls, and the stalled suffix gains one clause — *— the process is alive* — while a beat is fresh, absent when none is, which is when Stop is the right instinct. Evidence: `execution-endings.test.ts` (4 — pulses emitted through the real spawn path during a quiet stretch and stopping with the turn), `reliability.test.ts` (19 — a fresh pulse resetting nothing), `room-presentation.test.ts` (36 — the alive clause only on a fresh pulse, the since-time staying the transcript's). Still open: the queue behind it is not shown. The dead end past the Stop is closed (D-111): a stop that goes unanswered for a minute surfaces *Force interrupt* on the Stopping line, and a stop that finds no live process on the runner now reports honestly — `stopped (never_started)` for an execution merely open between turns, `interrupted` in words when nothing was there — instead of leaving `stopping` forever.
- **Approval routing pins the permission policy; the project's instructions are handed over explicitly.** `--setting-sources ""` is what stops a `.claude/settings.json` in the worktree deciding what gets asked — and the agent can write that file, so it cannot be an input. It also stopped the repository's `CLAUDE.md` loading, which is now restored with `--append-system-prompt-file` (D-064): measured against the real CLI with tools disabled, a codeword in `CLAUDE.md` is unknown to the model with settings pinned and known once the file is passed. Safe because the two files differ in kind — a settings file grants authority, `CLAUDE.md` grants nothing, and every tool call still reaches the permission router.
- **A controller cannot hand the baton back.** PRODUCT.md's lease model has a `released` state — no recipient, claimable by anyone — and there is no verb that reaches it: control moves by offer, by revocation (which is a force-take, not a release), or by the 30-minute expiry. So a considerate controller who is done cannot say so, and the only path to an unheld lease is silence. The consequences of an unheld lease are now handled and proven (D-073); the way *into* one deliberately is not built, and adding it is a capability-table change rather than an implementation detail.
- **Both transports are told now; the queue is still polled (D-149, D-154, 2026-08-18).** D-021 chose control-plane WebSockets and D-035 records polling as the shipped transport for the runner leg, with its semantics — idempotency keys, ordered delivery, acknowledgement, de-duplication — defined in the queue rather than the wire. Both of those still stand. What changed is that neither side waits on a timer to find out: the client holds an event stream and re-reads on signal (D-149), and the runner holds one too and asks for its commands on signal (D-154). **The runner's signal is bare** — `event: work`, empty payload — so a command can never be lost in flight and every guarantee stays exactly where D-035 put it. Its poll survives as a floor that adapts: 15s while a stream is live, the original 2s without one, so a network that will not carry SSE degrades to the previous behaviour rather than to something worse. Its events already travelled immediately and are unchanged. One stream per enrolment, because a runner credential is scoped to one workstream and that scoping is what makes the endpoint safe to expose. `pollNow()` — the path a person's own click takes — clears the floor before polling, or the case it exists for would have started waiting. Evidence: `mission-stream.test.ts` (10, real PostgreSQL — the work signal arriving, one lane never told about another lane's work, and a user session refused because the schemes are deliberately distinct), control-plane 314/314, full Electron e2e 58/58 with both streams live.
- **The needs-attention lens reads every lane and now names the one that is asking** (D-093, 2026-08-08): the list projection that decides the mission's word also carries the deciding lane and — where the attention is an execution's — the blocked conversation with its title, and the lens row renders both beneath the goal (Tested: `attention.test.ts`; the two-line row is on screen in `94-queued-behind-the-running-session.png`, reviewed — a one-lane mission naming its blocked conversation; **not proven in the app** for the background-*lane* shape, which no end-to-end run drives). The open-at half is closed by D-120 (2026-08-13): the lens row and the Home board's cards both open the mission **at** the named lane and conversation through the working set's new `openMissionAt`, proven in a real window by `e2e/home.spec.ts`. Still deliberate: a failed workspace or failed verification names no conversation, because attributing a lane's infrastructure failure to whichever conversation last ran would be a small lie.
- **The harness's workers are joined on the wire, and what remains unknowable stays unclaimed.** After D-107's protocol slice: the spawning Task call's own id is recorded on `harness.tool`, a worker's end — outcome flag and handed-back report — is recorded as `harness.worker.ended` from the Task's own tool result, and an end whose start was never seen emits nothing. Still genuinely unavailable from the stream, and therefore shown nowhere: per-worker usage or cost (`harness.usage` is turn-level, and a computed figure would be a number the vendor did not state); a worker's own shell checks (`verification.observed` carries no worker tag, so a worker's command reads as the turn's evidence); which worker raised an approval (the control request has no parent field); and nesting deeper than one level. The renderer's Workers view (D-107's presentation half) shows exactly what the join proves and nothing more: a worker with no recorded spawn stays flat grouped activity, and a worker whose turn settled without its end arriving wears no state word at all.
- **The pull-request surface is in-house through completion (D-099, D-100, 2026-08-09), and this is what remains.** Built and proven: the row and lifecycle, the remote-head guarantee with a real push, draft create / review / ready, the aggregated readiness gate, in-house comments, thread resolution, send-to-chat, metadata, and the completion verbs — merge behind the two-tier gate, update-branch, close, delete-branch, archive — with the adapter's whole chain including a real merge proven live on the scratch repository. Still open: **the in-window flow against real GitHub** — every half is proven live separately (the adapter chain, and the poll: `live-ingest.test.ts`, opt-in `NOVUS_LIVE_INGEST=1`, run 2026-08-09 — a real human's inline comment and squash-merge on the scratch repository's PR #3, made with their own `gh` hand, ingested into real PostgreSQL as `pr.comments` and `pr.merged` external events with their login, thread ids and live readiness on the row), but no single Electron window has driven a real GitHub request end to end; **user-authored comments, proven live** (D-101, `live-ingest.test.ts` run 2026-08-09) — a comment sent with the person's own runtime token through the exact provider path the comment route uses is authored by KJ14GOD on real GitHub (PR #4), with the App fallback pinned; the residue is storage, not capability: durable in-app attribution starts at the owner's next sign-in under the widened `read:user repo` scope; **branch-protection blind spots** — required-check flags degrade to none where the App cannot read protection, so a protected repository's host-tier refusal can arrive only from the merge call itself (named in D-100's revisit clause); **visual evidence on the PR page is real since D-122** (2026-08-15, its own row): attached screenshots and recordings render on the page's Checks segment with the boundary in words — inside Novus only, no copy and no public link to GitHub; **webhooks in anger, proven live** (D-101, `live-webhook.test.ts`, opt-in `NOVUS_LIVE_WEBHOOK=1`, run 2026-08-09) — the control plane went public through a disposable cloudflared tunnel, a repository hook carried a secret minted for the run alone, and GitHub's own signed delivery of a human's squash-merge of PR #6 verified against the raw bytes and synced into real PostgreSQL with no poll — the concurrent double-delivery that first run exposed (two knocks for one merge, both measuring change against the same stale snapshot) is fixed by reading the comparison baseline under the advisory lock; the residue is a long-lived deployed instance carrying the App's own manifest hook rather than a test's tunnel; **visual evidence on the PR page** (D-022's store is unbuilt; the page says so in words); **a terminal mission lifecycle now exists** (D-121, 2026-08-13) — a merged request still returns the mission to *Decision recorded* with the merge named in its sentence, and a person may now end it properly: `mission.close` completes the mission once the request is resolved, snapshotting the receipt (its own row); and **reaching the receipt in a single-lane mission** — the publish surface lives on Compare, which the rail offers only once two lanes exist (pre-existing shape, now carrying more).
- **The 2026-08-08 multiplayer-UI audit's wire gaps are closed.** The offer expiry countdown has its field and its enforcement (D-092), and per-participant connection state is on the wire and rendered (D-091) — the Presence and control-lease rows above carry the evidence. What the closure did **not** produce: a screenshot of a dimmed participant, which needs a lapsed client no deterministic capture holds at frame time (the lens row's two-line form has since been photographed — `94-`, reviewed). The dim rendering is pinned by its own test instead, and this clause stays until it is looked at on a screen. (The audit's third finding — DESIGN.md's `ParticipantStack` — was resolved 2026-08-08; see the reconciliation row.)
- **DESIGN.md's terminal palette and `tokens.css` disagree**, found by this slice's read-only audit and predating it: `--term-bg` is `#0d0d10` on screen against the document's `#17181b`, and several hues (`--term-red`, `--term-green`, selection opacity) differ likewise. Both claim to be canonical. Not corrected here because the fix is a deliberate D-046-territory choice of which side is right, not a transcription.
- **The ownership layer's honest residue (D-097).** Bash inside a scoped turn is human-gated, not scope-checked — a shell command declares its targets nowhere, so the alternatives were enforcement theater or no shell at all; what a scoped turn's bash touched outside its ground surfaces as drift at the checkpoint instead. A participant-run check while parallel writers are mid-flight exercises a moving tree; the staleness model marks it against head and the ledger's origin rules keep it honest, but nothing refuses it. Settling drift is manual by design: an unscoped turn adopts whatever sits uncommitted, which is the integration a person chose to run. And disjointness is conservative literal-prefix divergence — `src/a/**` vs `src/b/**` parallelize, but a pattern set the prover cannot split waits its turn even when a human can see it is safe.
- **CI results are not ingested.** Novus can run a check a project declared, and re-run one from its own ledger row (D-057); it has no second attributed source of verification, so a row's origin is always Novus and the control can always offer to run it again.
- **Redaction removes values Novus was handed, and detects nothing.** Closed in this slice: the harness transcript is now value-redacted as well as path-masked, and proxy passwords are harvested out of the forwarded variables and folded into the same set (D-052). What remains open is the shape of the protection, not a bug in it. Novus can only remove values it holds, at least eight characters long, in the literal form it holds them — so a credential inside a supplied `.env` *file* rather than supplied as a *value*, one a project prints, one an agent mints mid-turn, an encoded or line-wrapped form of a held value, and a value split across two delivery chunks all survive. Path patterns cover conventional names only; a credential in `notes.txt` is invisible to them, and a template filled in with real values is exempted by name with its contents never read. **Novus does not detect arbitrary secrets and nothing here should be read as claiming it does.**
- **One repository per mission.** The terminal lifecycle exists since D-121 (2026-08-13): a mission is completed or cancelled by a person's own `mission.close`, with the receipt snapshotted — its own row below. Archival (D-063) remains the different act it always was — filing the record away, not ending the work — and a mission can be both, in order. Still open here: receipt export (the artifact store itself is built since D-122), and any retention sweep of terminal missions.
- **Evidence diffs are stored inline** in PostgreSQL rather than in the artifact store D-022 selected, bounded to 150 files and 12 000 characters per file per checkpoint (D-037). A turn touching more files reports the first 150; the commit SHA remains the complete record.
- **A migration bug that only a second start would show, found and fixed 2026-08-06.** `schema.sql` widened the runner-command vocabulary in two places, and the earlier one re-added the list *as it stood when it was written*. So any database holding a command kind added later — the first `respond_approval` row a real deployment writes the moment somebody answers an approval — could never migrate again: the rerun dropped the good constraint and re-added a narrower one, which Postgres refused against the existing rows, rolling the whole migration back and leaving the control plane refusing to start. The vocabulary is now widened in exactly one place. Proven by seeding the failure: a scratch database carrying a `respond_approval` row fails to re-migrate against the old file and succeeds against this one.
- **A closed mission gives its workspaces back (D-155, 2026-08-18)**, and the remaining gap is narrower than it was. Closing a mission — a person's own act, never archival and never an idle timer — enqueues one `release_workspace` per lane **inside the closing transaction**, so a mission that ended is one whose machines have been told; the queue is durable, so a laptop that is shut does the work when it comes back. The machine stops what is running there, removes the staged attachments unconditionally (the artifact store holds them, so the disk copy is a cache), and then **refuses if anything else is uncommitted** — the lane is kept, the count is said in words, and nothing is deleted, following the position `retireLegacyWorktree` has held since before this existed. The **branch is never removed**: it carries every checkpoint, the receipt names it, and a pull request may point at it. `kept` is a real outcome on the event, not a failure. Enrolment ends with the checkout, and its command stream closes with it. Evidence: `workspace-release.test.ts` (6, **real git worktrees** — a clean lane removed with its branch intact, a dirty one kept with the count and the file still on disk, attachments removed and never counted as work, a lane that is both dirty and attached counted honestly, an absent lane quiet, and a second release settling rather than erroring because the queue can re-offer a command), `close.test.ts` (+3 — enqueued once per lane in the closing transaction, not twice on a second close attempt, and not at all while the mission is open). **Still open**: missions that ended before this existed have worktrees on disk that no close will ever fire for, and a `kept` workspace has no surface of its own — a person reads the event rather than finding it in the room.
- The desktop app runs from `pnpm dev` only; there is no packaged, signed, downloadable build, and no Windows or Linux run has happened.
- The light theme is screen-proven as of 2026-08-10 (D-102/D-103, `theme.spec.ts`, evidence 115–118) on the populated room, rail, and theme popover at 1440 wide; the remaining screenshot gaps are macOS-only coverage, three window sizes, and the newest surfaces (PR page, preview) not yet re-shot in light.
- **Every approach is a column (D-138, 2026-08-16, owner-directed — "there are 3 approaches but we only check 2")**: the pair rule reversed; all lanes render as columns in creation order, the sheet scrolling sideways past what fits with the label gutter sticky at the left. Pair chips and their machinery deleted (`decision-pair`/`pair-chip` testids gone); the decision spec's pair passage became a three-column assertion with evidence renamed `90-three-approaches-compared.png`, run green; the spec also captures the page whole — `154-decision-room-head.png` (chrome, title, meta line, sheet top) and `155-decision-room-foot.png` (actions, contested files, composer) — while the post-decision receipt (`86`) still wears the pre-D-137 dress (tracked-caps `field-label`s shared with the dialogs), on record as the next coherence candidate; DESIGN.md Decision Room bullet updated; D-138 records the reversal.
- **Open missions all grow trees, the diff gets real green and red, the ghost dialog dies (D-142, 2026-08-17, owner-directed)**: every working-set mission renders its tree (fold per mission; only the focused tree washes and carries singular testids and the `+`; background rows navigate by focusing their tab first; background detail is last-fetched, not live — stated). The diff drops the brown-reading `--ok`/`--danger` washes for its own `--diff-add`/`--diff-del` pair (GitHub-family hues, 16%/14% + inset edge), recorded in the token table. The Try-another-approach ghost — the fork-ask counter replayed by every keyed room remount — is fixed by consumption: the room opens the dialog and resets the ask. One regression caught by the pr spec (openPullTab passed as a direct handler received the click event as a tab id) and fixed; unit 483, pr spec 9/9, full Electron e2e rerun green; DESIGN token table and rail paragraph updated; D-142 records the batch. Follow-up same day: two surfaces still ended the tint before the text — the split view's 1fr cells (now subgrid columns over a max-content body) and, owner-caught, the room's own Changes panel, whose lines sat directly in the scroller and were never wrapped; both now carry the max-content body so every wash and the one horizontal scroll reach the widest line (fixture-verified mid-scroll at both sites; pr spec 1/1 against rebuilt dist).
- **A moved base can be followed (D-144, 2026-08-17, owner-directed)**: the drift words D-139 put on the workspace row now carry the mission's one **Sync** action for a base that *moved* — a merge, never a rebase, so every recorded checkpoint SHA stays true; all lanes together or none, prechecked so a dirty worktree or a conflict anywhere refuses the whole act with the lane and files named; refused while any turn runs, both on the machine (every lane's work queue held for the act) and at the control plane's route, which also refuses a stale pin and a lane set that is not exactly the mission's before moving every `base_sha` in one transaction and recording `workspace.base_synced`. A refusal after the merges walks every lane back. Rewritten or gone offer no Sync. Proven: workspace-sync unit suite 5/5 against real git (clean merge keeping ancestry, already-current, conflict refusing all lanes, dirty refusal, walk-back); control-plane route suite 5/5 (pin moved + event, stale pin, lanes mismatch, live turn, viewer refused); Electron e2e `sync.spec.ts` 2/2 driving the real app — drift said, Sync clears it, the merge lands in the worktree with the old head still an ancestor; a conflicting sync refused with README.md named and heads unmoved (evidence 167–169).
- **The file pane shows pictures (D-146, 2026-08-18, owner-directed)**: a bitmap opened from All files renders as the image itself over a data: URI — binary stays binary, nothing edits, SVG stays text — with a 10 MB image cap beside text's 1 MB. Proven: workspace-tree reader tests 20/20 (data URI + mime, uppercase extension, over-cap refusing as too-large), and a real-app probe opening a png from the tree with the picture on the canvas (evidence 176). Full suites deliberately not run — owner's standing instruction; the reader tests and the live probe are the evidence. Field fix the same night: a real sync against a base 140 commits ahead died at "git took too long" — the credentialed git path gave 10 minutes to clone but only 60 seconds to fetch; fetch now shares the network timeout (clone suite 15/15). And the working row stopped crushing its tabs: the ellipsizing names gave flex permission to shrink every tab — names vanishing, close controls clipped — instead of engaging the scroll the row already owned; tabs now keep their size and a full row scrolls sideways like the mission strip (fixture: twelve tabs in a 900px row, every close at its full 16px, content scrolling at 2166px), with the bar itself hidden — the mission strip's D-067 reasoning. The shrink floor was not the whole story, owner-caught twice: the base .tab rule's 30ch budget (the mission strip's) still clipped a long-named file tab's close — the same clipping .session-tab already documents opting out of — and the first fixture missed it by omitting the glyph. File tabs now carry the same opt-out; proven in the live app, where every close on four image tabs ends inside its tab and the long names show theirs in the pixels.
- **The preview's chrome gets out of the page's way (D-161, 2026-08-18, owner-directed)**: one slim head — origin chip, state word, four stroke-set glyph actions with their words as tooltips; the captured-pixels sentence rides the capture controls verbatim instead of standing as a row; the corner's running cluster is one pill (name and port in mono, Preview only while the tab is not already open, Stop as text). GatedAction gains `icon` and `hint`. Amended same evening (owner, relayed): the composer does not render while the preview tab is selected — the running app's view stands alone; file tabs keep theirs. Artifacts spec's warning assertion updated deliberately to the control's title. Verified by renderer typecheck/build and a rendered fixture of the head and pill; the preview and artifacts specs are the slice checkpoint's to run.
- **The diff readable again (D-141 batch, 2026-08-17, owner-reported)**: add/delete tints raised from 12% to 22% with a 2px inset edge in the same token — 12% vanished on the owner's screen — and the classic scroller bug fixed: lines were viewport-wide blocks, so on horizontal scroll the tint stopped while text ran on; a `diff-body` sized to the widest line now carries every row, one straight column edge, in both the Changes panel and the PR page's unified view. Verified in a pixel fixture at rest and mid-scroll; pr spec 9/9 protects the unified-view structure; DESIGN.md's Diff bullet updated.
- **The flow carries you to Publish (D-141, 2026-08-17, owner-experienced live)**: recording a decision left the publish controls above the owner's scroll with nothing pointing there. The state line now carries a `Publish` action beside "not published yet" (navigating to the receipt — the verbs stay on their one surface), and the receipt scrolls itself into view the moment a decision records while the surface is open, once per decision. Decision spec asserts the action; desktop unit and decision spec green; DESIGN.md's Decision-recorded state entry supersedes its own "no button" clause; D-141 records the reversal.
- **Push became an agent capability, and a one-lane mission publishes (D-140, 2026-08-17, owner-directed — "if Claude Code can do it, we need a strong reason not to")**: the rail offers **Publish** on a single-approach mission once a checkpoint exists — the decision surface with one column; record the decision and the existing push-and-draft-PR flow lights up unchanged (the server never had a two-lane guard; the gap was pure interface). The `novus` loopback endpoint carries a second tool, `push_branch`, under D-123's two-key lock with per-(execution, tool) grants — a capture allow spends nothing of push — pushing the lane's worktree HEAD through the same hardened path publishing uses; local-folder lanes are refused by the credential mint in the server's own words; the PR click deliberately stays a person's act, reason recorded; a sole lane's action reads **Accept this work** — "Choose this approach" with nothing to choose against confused the owner on first contact, and the accept dialog now names publishing as the next step. Zero control-plane changes. Verified: endpoint unit suite 9/9 (two new push cases incl. cross-tool refusal), desktop unit green, sessions spec proves the Publish row (`158-single-approach-publish.png`), full Electron e2e rerun after a true dist-electron build; PRODUCT/ARCHITECTURE/DESIGN reconciled; D-140 records the decision.
- **Branches at birth, a grown-up picker, and drift said out loud (D-139, 2026-08-17, owner-directed)**: the new-mission dialog gains a `Base · default` chip with a filterable branch menu (GitHub via the control plane, local via this machine's git), the pin resolved at Enter; the prepared PR already targeted the workstream's base, so PR-follows-base held free. The provider boundary grows `listBranches`, `baseStatus` (current/moved+count/rewritten/missing from the host's ancestry answer), and `pushedAt`; GitHub lanes get status on the detail read (60s cache, background refresh, `unknown` stated), local lanes ask their own git over the bridge. The workspace row warns in `--warn` words only when attention is demanded; Overview names every non-current state. Add-project re-dressed: words not logos, owner groups freshest-first, recency at the row's end, keyboard hints named. Sync-a-moved-base affirmed as direction and deferred by name to the D-025 continuity slice (a sync button without a conflict story would be a lie) — recorded in PRODUCT.md. Verified: contracts 39/39, control-plane 287/287, desktop unit 481/481, slice spec green after one deliberate order-independence fix, runtime spec proves the chip (`156-base-branch-picker.png`), full Electron e2e run for the cross-package slice — twice, honestly: the first run passed against a stale `dist-electron` (only the renderer had been rebuilt; the e2e has no auto-build), caught when the chip's menu came up empty in a probe, and rerun green after a true build; the drift warning itself proven live in a seeded probe — a mission pinned at main, main moved two commits, the room said `base moved — 2 commits ahead` (`157-base-drift-moved.png`); evidence `12`–`14` regenerated and reviewed. All seven canonical docs reconciled; D-139 records the decision.
- **The Decision Room re-dressed the same day (D-137, 2026-08-16, owner-directed on sight — "feels rendered out of the 2010s")**: D-136's anatomy stays (goal as title, shared row axis, subgrid, standing badge); its dress changes to one quiet `--surface-1` sheet at the dialog radius with columns as regions, not boxes. The fork diagram removed the day it landed; the lane hues retired for good — names, dots, and the pre-existing 1px `--lane-alt` column edge the owner had named three times; the tracked-caps eyebrow and gutter go sentence-case; Choose demotes to secondary (two identical primaries violated the one-primary rule all along). Decision spec passed unmodified again; evidence `84`/`88`/`90` regenerated and reviewed. DESIGN.md Decision Room bullet rewritten a second time today, both lane-token rows marked retired; D-137 records the reversal beside D-136.
- **The Decision Room revamped (D-136, 2026-08-16, owner-directed with a target mockup)**: a *Decision room* eyebrow with the mission's goal as title and "{n} approaches · Shared checkpoint · sha" beneath; the fork drawn as a checkpoint chip, curved hairline branches with lane identity dots, and a *Human decision* chip; the comparison on one shared row axis — gutter labels left, each column a subgrid over the same tracks so rows align regardless of wrapping, in-cell labels returning under 980px; lane hues home on this one surface (column names + fork dots, re-scoping D-133), with a Verified/Not-verified standing badge in words beside each name. Every testid and string kept — the decision spec passed unmodified on its first run against the revamp, regenerating evidence `84`, `88`, `90`; reviewed by eye at 1440. Full suite deliberately not run at the owner's direction. DESIGN.md Decision Room bullet and lane-token rows rewritten; D-136 records the decision.
- **Every approach shows its conversations (D-135, 2026-08-16, owner-directed — "when I swap don't close what was open")**: the tree renders each approach's sessions beneath its row always; selection changes the wash and the canvas only. The selected approach's row toggles its own conversations on click (D-134's gesture, whole chip the target); a session under another approach moves the room there and opens it. Folds per-approach, session-local. Focused sessions and decision specs green; full suite deliberately not run at the owner's direction; the bright path had a real gap: a before-open approach's OWN continuation (`.side-spine-below`) and OWN children block (`.side-children-more::before`) had no bright-color override, so the elbow lit up and the line immediately beneath it faded to grey — found on the owner's screen after a fresh build, fixed with `.side-approach-before-open > .side-spine-below` and `.side-children-before-open::before` overrides placed after their base rules for correct cascade order, verified pixel-exact against the production stylesheet in a static fixture (Chromium via Playwright, no app/CP): one uninterrupted `#DADADA` run from the mission's top curl through both intervening session rows to the open approach's own elbow; DESIGN.md rail paragraph updated; D-135 records the decision.
- **The tree folds from the mission's row (D-134, 2026-08-16, owner-directed — "I can't toggle it off")**: clicking the active mission's row — anywhere along its length, no twisty (amended same day; the chevron lived one commit) — folds and unfolds its tree; a non-active row's click still opens it. The mission row takes the wash while folded (D-084's deepest-visible rule), the room never moves, the choice is session-local. The whole chip is the touch target because a short goal leaves most of the row outside the name button — found when the spec's own click missed an 86px button at the row's center. Nine specs' "ensure open" clicks gained active-aware guards (a blind click on an auto-opened mission now folds); proven by the sessions spec's fold/unfold assertions, evidence `153-mission-tree-folded.png`, and a full Electron e2e rerun; DESIGN.md rail paragraph updated; D-134 (amended) records the decision.
- **The spine aims at approaches, the open path brightens, the dots retire (D-133, 2026-08-16, owner-directed on sight)**: elbows now run from the mission into each approach row — conversations indent with no connectors, fork/Compare hang on nothing — and the open approach's elbow plus the vertical above it render in the new `--edge-selected` token (85% after same-day calibration — 55% read as the same line), so where the canvas stands is visible at a glance. Every lane identity dot is gone: approach tabs, session tabs, file tabs, and Compare pair chips carry names, with foreign-lane tabs naming their lane in the title (D-088's guarantee by words and behavior); `--lane-current` retired, `--lane-alt` down to the comparison column edge. Spine states are renderer-computed classes (`:has()` failed to match in the shipped Electron); the through-vertical draws full-height per continuing row so the line runs unbroken with the elbows branching off it (same-day smoothing, verified by build and screenshot at the owner's direction; every branch is one and the same curled elbow, and at a middle branch the straight stroke shows only until the curve takes over — the arc alone carries the bend, with a dedicated element resuming the line behind the sweep — so straight and arc never draw side by side, and the arc paints above the resumption element — a real child otherwise paints over pseudo-element siblings and bit a chunk from the bend (the spine tokens are also precomposed opaque over `--bg`, so any residual overlap merges instead of stacking); an open middle approach's vertical is bright down to its branch and quiet below via a dedicated segment element (one border cannot be two colors; gradients are prohibited), and both bright-path cases sit in evidence, `89-the-rail-tree.png` and `152-the-rail-tree-first-lane.png`). No testid moves; desktop unit and full Electron e2e green after the change; tree evidence regenerated and reviewed; DESIGN.md token table, element 3, and rail paragraph updated; D-133 records the decision.
- **The spine pared to nesting (D-132, 2026-08-16, owner-directed beside the reference mockup)**: connectors draw only the selected approach's conversations — mission-level rows (approaches, Try another approach, Compare) indent with no lines, ending the two-parallel-verticals look — and the conversation chips carry their depth as margin instead of padding, so the selection wash starts after its elbow and never crosses a line. No testid or behavior changes; desktop unit and full Electron e2e green after the change; tree evidence regenerated and reviewed; DESIGN.md rail paragraph updated; D-132 records the decision.
- **The branch out of the rail, the spine visible (D-131, 2026-08-16, owner-directed on sight on the owner's own screen)**: the mission branch — placed by D-126, folded in by D-129, weighted by D-130 — is removed from the rail entirely; the approach row is the lane's name and nothing else, and the branch's home is Overview. The spine gets its own token, `--edge-strong` (white 30% / black 32%) at 1.5px, reversing D-129's no-dedicated-token stance — two strengthening steps still read faint on the owner's screen. `rail-branch` testid gone; the sessions spec assertion flips to count 0 deliberately. Desktop unit and full Electron e2e green after the change; tree evidence regenerated and reviewed; DESIGN.md token table, rail paragraph, and Workstreams bullet updated; D-131 records the decision.
- **The folder open, the branch legible (D-130, 2026-08-16, owner-directed on sight)**: the repository glyph becomes an open folder — the shape coding harnesses made familiar — and the branch line inside the approach chip takes weight 500 and `--text-2`, emphasis by weight and a text step, never color. Nothing moves; desktop unit and full Electron e2e green after the change; regenerated tree evidence reviewed by eye; DESIGN.md rail paragraph updated; D-130 records the decision. Honesty note: D-130's own commit said GATE PASS while the commit-time gate had in fact failed — a piped exit code hid four control-plane 5s timeouts under post-e2e load; the suite passed 287/287 rerun in isolation and the full gate printed GATE PASS immediately after, recorded in the follow-up commit.
- **The spine made legible (D-129, 2026-08-16, owner-directed on sight of D-128)**: the spine moves from `--edge` to `--edge-hover` — 11% white vanished against the rail — and the mission branch folds into the approach chip as its quiet mono second line (the mission chip's own "2 approaches" anatomy), off the spine where it read as one more clickable row and broke the tree's pitch. The two-line chip's elbow still meets the name line. `rail-branch` keeps its testid; the branch sits inside the approach's click target, whose one action — selecting the approach — is unchanged. Desktop unit and full Electron e2e green after the change; regenerated tree evidence reviewed by eye (`89-the-rail-tree.png`); DESIGN.md rail paragraph and Workstreams bullet updated; D-129 records the decision.
- **The rail says structure with a spine, not dots (D-128, 2026-08-16, owner-directed beside Conductor's rail)**: every project row leads with a repository glyph from the approved stroke set; the tree draws a hairline `--edge` spine with rounded elbows into each child — approaches, the selected approach's branch and conversations one level deeper, Try another approach, Compare, PR — and the lane identity dots leave the rail entirely, keeping their work on the room's tabs and Compare columns where D-086 built them. Presentation only — no testid or capability moved; indents deepen one step for the elbows. Verified: desktop unit and full Electron e2e green after the change, the regenerated tree evidence reviewed by eye (`89-the-rail-tree.png`, `92-lanes-at-760.png`); DESIGN.md's token table, rail paragraph, and Workstreams bullet updated; D-128 records the decision.
- **The strip shed the goal too (D-127, 2026-08-16, owner-directed on sight of D-126's result beside Conductor)**: the compact goal D-126 kept on the state strip sat directly above the first turn, whose words for the first turn *are* the goal — the same text three times in a glance. The strip now opens with the state sentence; the mission's name lives in the tab (full goal in its title), the rail, and the direction itself. The `room-goal` testid is gone: ten navigation/runtime assertions re-anchored deliberately — room-presence waits onto `state-line`, canvas-follows-rail polls onto the active tab's title — none weakened. Desktop unit and the full Electron e2e rerun green after the change; reviewed on screen (evidence `149-` … `151-slim-room-*.png` re-shot).
- **The room shed its heading and the rail always grows the tree (D-126, 2026-08-16, owner-directed on sight of D-125's result)**: the giant mission title repeated the tab and the rail and is gone — the header is one slim strip carrying the truncated goal, the state sentence, and at its end the baton sentence with Request control beside it; the rail renders Mission → Approach → Chat for the active mission always, with the lane sentence and `+` on the approach row, the mission branch beneath it in mono, a quiet Try-another-approach row while the lane can fork, and up to four participant marks on the mission row with an `--accent` ring on the controller's; `+` and Archive reveal on hover or their own focus only, and the open mission never also queues under Needs attention. Two e2e specs asserting the old no-tree rule were deliberately updated to assert the always-tree rule (`sessions.spec.ts`, `decision.spec.ts`). Presentation only otherwise — every capability gate and testid holds: desktop 481/481 and the full Electron e2e 56/56 after the change, reviewed on screen at 1440/1000/760 (evidence `149-` … `151-slim-room-*.png`); the passing run regenerated the deterministic screenshot sets against the new header and rail, not re-reviewed individually. DESIGN.md's type scale, elements 1/4/5, and rail paragraph updated, D-126 records the decision.
- **The spine reversed the same day it landed (D-125, 2026-08-16, owner-directed on sight)**: the four-node Direction/Execution/Checkpoint/Review timeline read as workflow theater — agent work loops — so the trace returned to grouped speech in a coding harness's shape, with checkpoints and verification as compact hairline milestone rows and tool activity as one quiet disclosure; D-124's header hierarchy, composer eyebrow, and rail rhythm all stay. Every testid and reading order unchanged a second time: desktop 481/481 and the full Electron e2e 56/56 after the reversal, reviewed on screen at 1440/1000/760 (evidence `146-` … `148-harness-room-*.png`); signature element 2 rewritten again and D-125 records the reversal beside D-124, the D-085/D-086 honesty.
- **The room's hierarchy retold as the turn spine (D-124, 2026-08-16, owner-directed — its spine reversed by D-125 the same day; header, dock, and rail survive)**: the header stacks tight — title, state line, then one context row carrying the lane fact, the baton sentence, a participant count past one, and the quiet actions after the facts; each turn's trace renders as labelled nodes on one hairline — Direction, Execution, Checkpoint, Review — filled when the stage happened, `--accent` while live, hollow while unreviewed, folding to stacked headings under 900px; and the composer names its whole target as a micro-step eyebrow on the box's own edge. Presentation only — every testid, capability gate, and reading order unchanged: contracts 39/39, control-plane 287/287, desktop 481/481, the full Electron e2e 56/56 after the change. Reviewed on screen at 1440/1000/760; evidence `143-` … `145-turn-spine-*.png`. DESIGN.md's signature element 2 and composer paragraphs updated in the same change; D-124 records the decision.
- **The file view's line numbers stay on their lines since 2026-08-16**: the source pane wrapped long lines (`pre-wrap`) while the gutter counted logical lines, so with the evidence panel narrowing the canvas the numbers ran out before the code did. Code no longer wraps — the block scrolls sideways inside its own pane with the gutter sticky at the left on the canvas token. Verified in a real window beside the open panel with 200-plus-character lines: gutter and body heights identical, horizontal scroll live, numbers pinned mid-scroll; evidence `141-file-view-gutter-beside-panel.png`, `142-file-view-gutter-scrolled.png`. (D-048's editing rule is untouched: markdown edits, source read-only.)
- **The rail's selected chip matched hover again on 2026-08-16**: the hover-revealed "+ new session" control mounted only on the active row with a 32px height of its own, so the selected chip stood 40px beside 28px hover chips; the control no longer sizes the row, and the mission chip's open button now clips its own name — a long goal printed straight across the quiet controls beside it. Measured in a real window (28px both states, name ellipsized, no overlap); evidence `140-rail-chip-standardized.png`.
- **Some renderer strings are not in the screenshot set.** The 2026-08-07 spec runs regenerated every deterministic screenshot set again with the session chrome in place; of that regeneration, the sessions screenshots (`93-` … `98-`, at 1440/1000/760) were reviewed by eye — including one real catch, a "· needs you" suffix clipped by a tab's width budget, fixed and re-shot — and the rest were not re-reviewed individually. The 2026-08-08 D-084 re-runs regenerated the decision and sessions sets again against the rail tree (`89-the-rail-tree` and `95-the-rail-tree-with-attention` replace the two switcher shots), reviewed by eye at 1440/1000/760; the other five specs' sets were regenerated by their re-runs and not re-reviewed individually. (Previously: the 2026-08-06 runs regenerated the sets with the rail's aligned switch, the search palette, and the workspace drawer, with `83-` … `92-` reviewed.) The 2026-08-09 preview slice re-ran `runtime.spec.ts` (16/16) and `slice.spec.ts` (5/5) as its regression check, regenerating their sets in passing — not re-reviewed individually; the new preview set `103-` … `107-` was reviewed at all three widths. Three lines from the harness slice are not there either, and two of them cannot easily be: what a turn cost appears only when a real harness reports figures (the scripted one reports none, which is why screenshots `74-` … `80-`, regenerated 2026-08-05, correctly show a machinery line with no cost on it), *no progress reported since {time}* needs ten minutes of silence, and *no one holds the baton — claim it to answer* needs a lapsed lease. Each is proven by its own test; none has been looked at on a screen. They were inspected by eye during iteration, which D-067 allows, but no screenshot in `apps/desktop/e2e/evidence/` shows them and none of the end-to-end specs that assert those surfaces has been re-run since — so the top-row row is Partial rather than proven.
- Egress default-deny via provider IP rules is proven unworkable (D-024); the egress proxy that replaces it does not exist even as a specification.
- The repository-continuity contract (D-025) is only partly implemented: mission branches, base pinning, and checkpoints exist; remote-update detection, explicit synchronization, conflict presentation, and replacement-workspace reconstruction do not.
- **The two emitting TypeScript configs state their `rootDir` since 2026-08-17.** `packages/contracts` and `apps/desktop`'s electron config set `outDir` without `rootDir`, which TypeScript 5.9 infers silently and TypeScript 6 warns about; both now say the directory that was already being inferred (`src`, `electron`). No compiler behavior changes on the pinned 5.9.3 — the emitted layout is byte-identical — so this closes an editor-visible warning, not a build gap.
- **The room is told rather than asking (D-149, 2026-08-18)**: the room's two-second re-read of the whole `MissionDetailResponse` becomes a live connection — `GET /missions/:missionId/stream`, server-sent events — with a 30-second re-read left underneath as the honest floor. What crosses is an address (mission, sequence, kind), never a payload: the watcher re-reads through the same authorized projection, so the stream widens nothing a person may see and a refusal is the mission read's own 404. The notification is `pg_notify` **inside the writing transaction** at `recordEventAtSeq`, the one path every mission event is written through, so it is delivered on commit and never on rollback — the ordering trap an in-process emitter would have had — and a second control-plane process fans out from the same channel. A signal arriving during an in-flight read is coalesced into one read after it, so a busy turn re-reads continuously instead of once per event. Two heartbeats moved onto the connection, and the second was a regression caught before it shipped rather than a planned move: the **lease** heartbeat is D-051's own stated revisit, but **presence** (D-091) projects *connected* from a read within 30 seconds, and a room re-reading only on change would have flickered its own watcher to *reconnecting* — the stream now refreshes presence every 10 seconds, which is the truer signal anyway. Evidence: `mission-stream.test.ts` (7, real PostgreSQL against a real listening server rather than `inject`, which cannot model a hijacked stream — delivery with the sequence named, **silence after a rolled-back write**, the non-participant's 404 and the anonymous 401, one mission's events reaching only that mission's watcher, the subscription dropped when the watcher goes, and presence set for a participant who never read); control-plane 305/305, desktop unit 490/490, contracts green.
- **A person can hand the agent a picture (D-150, 2026-08-18, proven live)**: a direction may carry up to four images. An attachment is an Artifact of kind `attachment`, capture source `upload` — D-122's whole lifecycle (promised digest, store-verified bytes, expiring view grants) with **every capture-provenance column null**, because Novus photographed nothing and the record says supplied rather than inventing a producer. Gated by `direction.submit`, never `artifact.capture` — illustrating what you mean is part of saying it — and needing no runner, so a participant whose laptop holds no checkout can still attach. The client normalizes before uploading: a closed image allowlist decided by the file's **own signature** rather than its name, longest edge to 1568px, a 5MB ceiling after that, with resizing stated rather than hidden. `direction_attachments` fixes what a direction carried inside the submitting transaction; a claim on an image that is not this mission's, not an attachment, not yet verified, or named twice refuses the submission in words. The runner fetches each blob under its own credential and hands it to the harness as an **image content block in the turn's first message** — nothing is written into the worktree, so no checkpoint can sweep an attachment in. **Live evidence, and it is the whole claim**: `live-attachment.spec.ts` (1/1, ~13s, opt-in `NOVUS_LIVE_CLAUDE=1`) uploads a 64×64 PNG of a red square on a blue ground through the real product, directs the real `claude` binary with it, and gets back **"Red blue"** — neither colour appears anywhere in the prompt, so the only place either could have come from is the image; the spec also asserts `git status` shows no `.png`, that the direction carried exactly that artifact, and that the row reads `attachment`/`upload`. Screenshot `194-attached-image-answered.png`, reviewed: the image in the trace under its filename, the answer beneath it, the composer's Image chip on the foot. **One real bug, found only by that run and worth recording**: the first live attempt answered *"I don't see any image attached to your message"* while every deterministic suite was green — `IpcDirectInputSchema` never listed `attachmentIds`, so zod stripped the field crossing the renderer→main bridge. The control-plane tests passed because they called the route, and the route was right; nothing proved the product reached it. That is D-051's lesson exactly, and the fix carries three tests at the boundary that failed rather than at the one that did not. Deterministic evidence: `attachments.test.ts` (8, real PostgreSQL — the carried image projected onto its direction, supplied-not-captured, the unfinished upload, the borrowed mission, the duplicate, the non-participant's 404, the refused MIME, and same-length wrong bytes caught by the digest), `attachment-upload.test.ts` (7 — signature sniffing including a zip named `.png`, a truncated header, a RIFF that is not WebP, plus the three bridge cases), control-plane 307/307, desktop unit 497/497.
- **What may be attached is decided by probing the harness, not by reading a format list (D-151, 2026-08-18, owner-directed — "everything they can do as well as whatever Claude can do")**: the allowlist widens to PNG, JPEG, GIF, WebP and **PDF**, with HEIC, TIFF and BMP converted to PNG before upload and everything else refused by name. Each verdict came from piping the format into the real `claude` binary and asking a question only the file could answer. Images travel as image blocks; a PDF travels as a **document** block, which is a different content block and therefore its own claim. **HEIC is why this is a rule rather than a list**: passed through it does not fail — one probe answered *"Red green"* about a red square on a blue ground, another refused — so a format that makes the model confidently wrong is converted or refused, never sent hopefully. Conversion is macOS `sips`, because Chromium (and so `nativeImage`) decodes neither HEIC nor TIFF; on a platform without it the file is refused by name, stated rather than silently broken. A document carries its own 10MB ceiling against an image's 5MB: an image too large is resized, and there is no smaller-but-still-readable version of a contract. The trace now **shows images with no caption at all** (owner-directed — a screenshot's filename tells a reader nothing the picture does not; the name survives as the accessible label) and **names PDFs**, which have nothing to show. Three schema constraints have now needed widening for this family — kind, capture source, and mime type — the last found by a 500 rather than by reading, and the local store's content-type parser now reads the shared list instead of a second hand-written one. **Live evidence for both roads**: `live-attachment.spec.ts` (1/1, ~19s, opt-in) gets **"red blue"** from a 64×64 PNG and **"MAGENTA"** from a one-page PDF whose word is printed only inside it, in one run against the real binary; screenshots `194-attached-image-answered.png` and `195-attached-pdf-read.png`, reviewed. Deterministic: `attachment-upload.test.ts` (12 — the routing table including an mp4 that must not sneak through the HEIC branch, since both are ISO containers and only the brand separates them), `attachments.test.ts` (10 — a 6MB PDF accepted at the document ceiling that the image ceiling would have refused, and an 11MB one refused), control-plane 309/309, desktop unit 502/502.
- **Attaching is a gesture, and a picture opens where it already is (D-152, 2026-08-18, owner-directed)**: **paste** an image from the clipboard (a pasted paragraph stays words — only a clipboard actually carrying an image is intercepted), **drop** files anywhere on the composer box (the whole box is the target; `webUtils.getPathForFile` behind a bridge verb, since Electron 32 removed `File.path`), or use the picker — all three end in **one upload path** in the main process, so the entrances cannot drift in what they verify. Clicking an image in the trace **opens it over the room** at full size: it navigates nowhere, so Escape (captured, because while a picture covers the surface Escape means only *put it back*) or a click on the ground restores exactly what was there; no close button, because the ground is the control. A dropped run uploads **sequentially** — the bound is checked against what is already held, and four concurrent uploads would each read the same empty list — and one refusal among several does not discard the rest. **Mixed sequences are proven, not reasoned about**: the live spec attaches an image and a PDF to one direction and the real binary answers **"Red blue MAGENTA"** — three words from two files, carried as two different content block types, with neither colour nor word appearing in the prompt (`196-image-and-pdf-together.png`); the same run still proves the image alone and the PDF alone (1/1, ~29s, opt-in). `attachment-upload.ts` stopped importing `electron` at module scope — a rule this suite already had and this module had broken — so the routing table and the accumulation rule are both exercised in plain Node: `attachment-upload.test.ts` (16, four of them the bound and the keep-the-good-files rule). Desktop unit 506/506, control-plane 309/309.
- **A second road for what the model cannot read (D-153, 2026-08-18, owner-directed after seeing Codex answer questions about an MP3 and a video)**: images and PDFs still travel **inlined** in the turn's message; **every other file is staged** in the lane's worktree under `.novus/attachments/` and its path named to the agent, which opens it with its own tools. That is the whole of what looked like multimodality in Codex — their source ships a `view_image` tool ("View a local image file from the filesystem… already available on disk"), and the audio answer came from `mdls`/`afinfo` reading tags while the video answer came from frames generated by macOS's own preview tool and then viewed. **A shell plus a file path, not a model that hears** — and Claude Code has both halves already. MIME becomes **open for attachments and closed for captures** (the shape is checked, the vocabulary is not; the capture routes still take the closed enum, and the database constraint splits by kind). A staged file's ceiling is 50MB against an image's 5 and a document's 10, because its bytes never enter the harness's context — they sit on disk until the agent opens them. **A person's own file is kept out of the mission's history twice**: `.git/info/exclude` (per clone, never committed — the project's own `.gitignore` is never edited to make Novus work) and `isAttachmentPath`, which the checkpoint refuses outright, proven against a real repository with **no exclude entry at all** — git sees the file, the commit holds only the mission's work, and the file is still on disk afterwards. Written `0600` and never executable, because it lands where an agent runs shell commands. **Live evidence, one run, four answers**: `live-attachment.spec.ts` (1/1, ~46s, opt-in) gets `Red blue` from a PNG, `MAGENTA` from a PDF, `Red blue MAGENTA` from both on one direction, and **`Grimsby Interlude` from an MP3** whose title exists only inside its ID3 bytes — with `git status` asserted clean of `.novus` and of the file. **The turn asks, once per command, and that is the product rather than a fault**: the run's own event log shows the agent request `ffprobe`, be approved, find it is not installed, and fall back to a `python3` one-liner that reads the tag bytes itself — the same adaptation the owner saw Codex make. A first attempt failed because the test answered one question and not the second; the helper now answers each as a person in the room would, and asserts at least one was answered. Deterministic: `evidence.test.ts` (+2 — the checkpoint refusing a staged attachment, and a turn that staged one and changed nothing recording no commit), `secret-policy.test.ts` (+2 — the path rule, including that `.novus/attachments-of-mine.txt` is the project's own file), `attachments.test.ts` (12 — a zip now **accepted** as a staged file where it was previously refused, a malformed MIME still refused, an oversized MP4 refused). One hand-written list removed on the way: the local store's content-type parser enumerated the allowed types and rejected `audio/mpeg` before the bytes reached disk — it is a wildcard now, because the store hashes what arrives rather than trusting a header, and the list was only ever another thing to forget to widen. Control-plane 311/311, desktop unit 510/510.
- **A checkout opens where a person already works (D-159, 2026-08-18, owner-directed with a Conductor capture)**: one icon in the room's top-right, beside Run and the evidence toggle, opening one menu — Finder, whichever editors and terminals this machine actually has, and Copy path. **The renderer names a lane, never a path**: the worktree is resolved in the main process from the workstream id, the application comes from a closed list, and the launch is `execFile` with an argument vector rather than a shell line — so no window can name a directory, no window can name a program, and a directory's own name can never become a command. The list is **detected rather than declared**: an entry appears only when its application is on disk, because a menu offering Cursor to somebody without Cursor is a control that does nothing. **Amended the same day, owner-directed** — *"do these hot keys work also show the icons there"*: the digits are now live while the menu is open (scoped to it; a bare digit bound globally would take every digit in the room), and each row carries the **application's own icon**. The first attempt at those icons rendered as three blank squares: `app.getFileIcon` returns a *generic document icon* for a `.app` on macOS — the identical 1181 bytes for Finder, Cursor and Terminal — so the icon is now read from where it is (`Info.plist` names it, `Resources/` holds it, `sips` converts the `.icns`), never bundled, and a failure drops the icon rather than the row. The original words-not-logos call cited D-139, which is about add-project's repository rows; an application is recognized by its icon before its name, and the rule had been borrowed onto a surface it did not describe. Two further owner-caught rounds the same evening: icons to 20px with the menu sized so no label wraps and the digit sits beside its label rather than a column away (a 150px floor made *Copy path* wrap, so 176px with `nowrap`); and **the white box around the trigger after coming back from Cursor** — `--accent` is near-white in dark mode, the button still held focus, and returning to the window made Chromium re-decide it had been reached by keyboard. D-106 hit this on dialog openers and named it in the same words; the same suppression applies here, **without its blur listener**: leaving for another application blurs the element, so clearing on blur would lift the suppression on the way out and leave the ring waiting on the way back. It clears on the next key press instead, and a Tab to the button still shows the ring because the keydown fires before focus lands (`runtime.spec.ts` asserts both halves). Visible and disabled with the reason when the workspace is on another machine. macOS only, said in the menu rather than offered and failing. Evidence: `runtime.spec.ts` (17, +1 — the control enabled in a real window, the menu listing Finder and Copy path, and Copy path putting a path containing `worktrees` on the clipboard, which is the proof the main process resolved it; screenshot `198-open-in-menu.png`, reviewed — Cursor is listed because it is installed on this machine and VS Code, Zed and iTerm are absent because they are not), `workspace-open.test.ts` (5 — detection, the empty case, Terminal in either of its two OS locations, and that every entry names its own application so nothing downstream assembles one from an id).- **The room is a conversation with two sides, centred (D-162, 2026-08-18, owner-directed from three captures)**: the trace was pinned to the room's left edge, so a wide window made every turn a ribbon against the edge with an empty half beside it — and, the part that actually cost a reader, the question and the answer were indistinguishable until both were read. The feed, the state line and the composer now share one **centred** measure (`--measure-room`, 900px — a code block at the mono size without wrapping, a line of prose in one eye movement), reversing D-066's left pin while keeping its one axis. **What a person said sits on the right**, in a bubble at the button radius on `--surface-2`, bounded to 78% so it never spans the measure; **the harness answers on the left with no bubble at all** — one side is a short thing somebody typed, the other is prose, code, checkpoints and evidence, and a box around the second would be a box around the room. Attribution stays on the person's side, because this room can hold several people and which of them spoke is the product's subject, where a two-party chat has nothing to attribute. Attachments follow the words they were sent with. Evidence: `runtime.spec.ts` (19, +1) measures both facts **on screen** rather than trusting the stylesheet — the column's left and right gaps agree within a pixel, and the person's words end hard against the column's right edge while staying narrower than it; regenerated `59-running-with-stop.png` reviewed, showing the bubble on the right and Claude Code's answer on the left. **Screenshots owned by other specs still show the old left pin** and will until those specs next run — stated rather than quietly re-shot.

## Blocked

- The authenticated-harness leg of the provider spike is blocked on explicit opt-in to transmit provider API keys to a disposable sandbox and on an Anthropic API key that is not present on this machine. Only the account holder can provide these.

Next step: the opt-in live GitHub runtime proof, which is the one thing standing between "GitHub repositories run locally" and a live claim; then a regenerated evidence set for the shell changes that landed after it, so the top row can be proven rather than described; then the demo path, rather than expanding uncontracted review or cloud features.
